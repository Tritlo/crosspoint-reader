# Emulator Implementation Log

## 2026-07-12

- Started implementation from `fdd1ad30` on branch `emulator`; worktree was clean and matched `origin/emulator`.
- Confirmed `NOTES.md` does not yet exist.
- Confirmed `uv 0.9.5` and FFmpeg 6.1.1 are installed; `pio` is not currently on `PATH`.
- Began phase 1 from `docs/emulator-plan.md`: native execution spine, framed protocol, deterministic time, and scheduler compatibility.
- Added the PlatformIO `emulator` native target and a small host executable with explicit `--device` and artifact-directory configuration.
- Added Content-Length framed JSON-RPC over stdio, protocol/version negotiation, an uncalibrated timing marker, deterministic clock controls, and canonical manifest/event artifacts.
- Installed PlatformIO's native platform and native ArduinoJson dependency through the existing PlatformIO cache. Fixed the resulting missing `<optional>` include.
- Verified an X3 protocol smoke run: four ordered responses, a 125 ms simulated-time advance, and matching manifest/event files.
- Verified the existing `default` ESP32-C3 firmware still builds successfully (`5267981` bytes flash, `49532` bytes RAM reported).
- Added a deterministic, logically single-core scheduler with serialized host task dispatch, simulated delays/timeouts, notifications, priority/creation-order scheduling, task diagnostics, and a 5 s wall-time watchdog.
- Added native PlatformIO tests for deterministic task order, notification timeouts, and watchdog termination. All 3 tests pass; the watchdog exits a stalled child with code 70.
- Verified X3 and X4 both handshake and that two identical initialize/state/shutdown runs produce byte-identical event traces for each profile.
- Read the phase-1 checkpoint in `NOTES.md`; its X4, determinism, scheduler, and watchdog reminders are now addressed.
- Added the narrow Arduino/FreeRTOS compatibility surface used by the app today: simulated `millis`/`micros`/delay/yield, pinned task creation, notifications, mutexes, recursive mutexes, mutex ownership/peek, and critical sections.
- Added a weak-linked shared `setup()`/`loop()` lifecycle runner. Setup may auto-advance through deterministic delays; normal loop execution is bounded by explicit caller time advances so periodic tasks cannot run forever.
- Changed protocol/trace sequencing so recorded events and responses consume one shared monotonically increasing sequence. Reverified byte-identical X3/X4 runs; response sequences are `2,4,5,7` around three trace events.
- Native emulator suite now has 7 passing tests covering scheduling, priority/creation-order ties, bounded time, lifecycle, FreeRTOS primitives, and watchdog behavior.
- Applied the phase-1 reviewer follow-up before commit: FreeRTOS task priority is now preserved and runnable tasks resolve by priority then creation order, matching the plan.
- Committed phase 1 as `fd6287eb` (`feat: add native emulator execution spine`) before continuing, as requested.
- Added `books-for-testing/` to `.git/info/exclude`; the local ebook corpus stays invisible to Git without changing the repository `.gitignore`.
- Began phase 2 with immutable directory fixtures copied into private `artifacts/sd` run storage. Fixture traversal rejects symlinks and parent escapes, sorts UTF-8 device paths, records file/directory/byte counts, and hashes paths plus contents into the manifest.
- Added directory-backed native `HalStorage`/`HalFile` implementations and tests for sorted enumeration, reads/writes, path confinement, fixture immutability, and fresh-run isolation.
- Replaced four host-sized `std::hash<std::string>` cache-path uses with a fixed `devicePathHash32` implementation matching 32-bit libstdc++ Murmur hashing, preserving existing X3/X4 cache directory names. Added known-vector tests and reverified the normal firmware build (`5268421` bytes flash, `49532` bytes RAM).
- Audited shared persistence call sites: the core serializer uses fixed-width lengths and current book/settings cache writes do not persist host-sized `size_t`, `long`, or whole native structs. The cache-path hash was the confirmed host-ABI mismatch.
- Added the native Arduino compatibility required by the real application sources (`String`, `Print`/`Stream`, serial, deterministic RNG, SPI, and explicit offline Wi-Fi/HTTP/OTA/flash boundaries). The production `src/` application, activities, EPUB/TXT/XTC, rendering, settings, and persistence graph now compiles and links under PlatformIO Native.
- Selected native HAL implementations at compile time while retaining the embedded HAL sources unchanged for firmware builds. PNGdec/JPEGDEC and the existing QRCode dependency now build in native mode.
- Booted the real X3 application against `test/epubs`: shared `setup()` detected X3, initialized display/fonts, entered Boot, then the first explicit 10 ms advance ran the production loop and transitioned to Home. The scheduler was quiescent with `app-main` sleeping and `ActivityManagerRender` waiting for notification.
- Made firmware-style static initialization safe before the emulator session exists (`millis()`/`micros()` report zero) and allowed the host process to destroy the otherwise process-lifetime `ActivityManager` singleton cleanly.
- Added canonical `storage.operation` trace events with device path, operation, byte count, success, simulated time, and the shared response/event sequence.
- Verified both X3 and X4 boot through the production lifecycle to Home. Two identical runs per profile produced byte-identical 14-event traces, including boot storage activity.
- Expanded the native suite to 11 passing tests. It now recreates a runtime over the same run storage to prove reset preservation, starts a fresh run from the immutable fixture, builds a real `book.bin` from `test_text_decorations.epub`, reloads it through `Epub::load(false)`, and round-trips `progress.bin` through the shared reader path.
- The EPUB round-trip exposed a recursive `HalFile::write(uint8_t)`/`Print::write(buffer)` overload on native builds; added the exact bulk-write override and reverified the embedded firmware build (`5268369` bytes flash, `49532` bytes RAM).
- Recorded the migration-free 32-bit cache-hash choice in the plan's Decision section rather than adding an ADR.
- Began phase 3 by making the FreeInk display library host-selectable at its existing `EpdBus` seam. The real `FreeInkDisplay`, `Uc8253X3Driver`, `Ssd1677Driver`, LUT selection, refresh promotion, grayscale policy, and controller command sequences now run in the native target; only the physical SPI/GPIO bus implementation is replaced.
- Kept the `freeink-sdk` changes isolated in the dirty submodule for independent review: hardware `EpdBus.cpp` is excluded only for `CROSSPOINT_EMULATED`, host `isBusy()` is supplied by the native bus, and non-selected M5/Murphy sources are gated before hardware-only includes. No submodule pin has been moved or committed.
- Added a native panel model for UC8253 and SSD1677 RAM planes, controller commands, power/BUSY state, refresh generation, uncalibrated simulated refresh durations, grayscale composition, retained visible state, and deterministic approximate fast-refresh residue.
- Added time-resolved full-refresh black/white flashes and non-full drive/settled transition frames to the panel history. Phase timestamps are emitted in the canonical trace for later replay/video sampling.
- Replaced the temporary high-level native `HalDisplay` implementation with the shared production `HalDisplay` and real FreeInk facade.
- Added dependency-free deterministic 8-bit grayscale PNG encoding plus `capture.panel` and `capture.framebuffer` protocol methods. Verified valid X3 792x528 and X4 800x480 PNGs with distinct framebuffer/panel hashes.
- Bumped the panel-model protocol/manifest version to 1 and added explicit white/black initial-panel configuration; a fresh run defaults to white while reinitializing the same bus retains visible pixels.
- Fixed scheduler shutdown ordering exposed by the render task: app tasks now unwind while the FreeRTOS runtime remains installed, and semaphore cleanup becomes non-blocking once shutdown begins.
- Native suite now has 12 passing tests, including direct real-driver command checks (`UC8253 DTM2` versus `SSD1677 BW RAM`) and visible panel retention across deep sleep and bus reset for both profiles.
- Added native-geometry PNG initial panel seeding through PNGdec. Fixed a false read failure caused by checking `eofbit` after `istreambuf_iterator` consumption; invalid files now report whether opening, geometry, or decoding failed, and valid supplied images emit an `initial-png` panel phase.
- Completed the phase-3 determinism gate with two fresh black-seeded boot-to-Home runs for each profile. X3 panel/framebuffer captures were byte-identical across runs (`0ca00c…`); X4 panel captures matched (`157de1…`) and framebuffer captures matched (`0058af…`). All four files were valid native-geometry grayscale PNGs.
- Re-ran all 12 native tests after partial-window and supplied-PNG support, then rebuilt the normal ESP32-C3 firmware against the guarded FreeInk SDK changes (`5268369` bytes flash, `49532` bytes RAM). Phase 3 exit criteria are met; calibrated device timing/ghosting profiles remain deliberately deferred.
- Added `capture.screenshot` for natural human review: it derives a clockwise-rotated presentation PNG while preserving controller-native `capture.panel`/`capture.framebuffer` artifacts as regression truth. Verified upright X3 528x792 and X4 480x800 Home screenshots; the handshake and manifest now record the 90-degree review rotation.
- Began phase 4 by compiling the SDK's real `InputManager` in the native target. Protocol press/release changes feed realistic ADC-ladder/power-pin samples, then pass through the existing two-sample 5 ms debounce, native `HalGPIO`, and `MappedInputManager`; the FreeRTOS compatibility layer now also supplies the queue calls required by the SDK.
- Added stable shared activity IDs for the supported slice (`boot`, `home`, `file_browser`, `reader.epub`), completed-render generations, debounced input edge/hold tracing, panel BUSY state, and explicit activity/render/panel/storage wait commands with simulated and wall timeouts.
- Verified physical Down selects Recent Books on Home, and a 800 ms physical hold reports a debounced 780 ms live hold / 800 ms released hold. A fixture swapping Back and Confirm proved physical Back traverses the real remap path and opens File Browser on both profiles.
- Added mapping-aware `input.pressAction`/`input.releaseAction`: logical Confirm resolved to physical Back under the swapped fixture, then the application independently remapped that physical edge back to Confirm. This is the Playwright convenience path without bypassing firmware input behavior.
- Re-ran the 12 native tests after the input and observability changes; all pass.
- Added reset preparation plus Python-owned relaunch: the native protocol snapshots visible native panel pixels and exposes the writable SD directory, exits, then the client starts a fresh process using those as initial inputs. Verified the relaunched run records `initial-png`, returns through Boot/Home, and preserves its SD fixture identity/state.
- Added the `uv`-managed, zero-runtime-dependency synchronous Python package under `emulator/python`, with context-managed lifecycle, typed physical/logical controls, waits, captures, natural screenshots, reset, optional pytest fixtures, and automatic retention of temporary artifacts on failure. Strict Pyright reports zero errors/warnings.
- Added the real EPUB vertical slice: each profile boots, opens File Browser with mapped physical controls, opens the existing 4 KB text-decoration EPUB, waits for `reader.epub`, turns a page through the side-button mapping, and captures panel/framebuffer/review images. Two runs per profile produce byte-identical normalized traces and panel PNGs.
- Visual review of the naturally rotated EPUB captures caught two panel-model bugs before goldens: grayscale strips are overlays on the displayed BW base rather than absolute 2-bit frames, and SSD1677 window-register bytes arrive through four individual `data()` calls. Corrected both, recognized powered custom-LUT grayscale sequences, and reduced the deliberately uncalibrated fast-refresh residue from an unrealistic 25% to a subtle 1/16.
- Switched deterministic PNG encoding from stored DEFLATE blocks to system zlib level 9. Canonical page captures fell from roughly 400 KB to 28–31 KB while remaining byte-identical across runs.
- Added visually reviewed compact X3/X4 native-orientation page-2 panel goldens and an Ubuntu CI job that builds the emulator, runs strict Pyright, executes both Python scenarios, and runs the native suite.
- Persisted every visible panel transition as a trace event referencing a content-deduplicated frame PNG. The X3 EPUB scenario records 32 transition events over 15 unique frames; X4 records 20 events over 19 unique frames.
- Added trace-derived fixed-FPS MP4 export through the system FFmpeg CLI. Video presentation rotates native panel frames 90 degrees clockwise for natural review, records encoding/probe metadata, and validates the exact output frame count with FFprobe.
- Verified deterministic replay by exporting each profile's same trace twice at 8 FPS and comparing sampled frame identities, frame count, and simulated duration. X3 produces 61 frames over 7.625 s at 528x792; X4 produces 63 frames over 7.875 s at 480x800.
- Changed image golden assertions to decode PNG scanline filters and compare the grayscale pixel planes and geometry. Encoded artifacts remain compact, but zlib-version differences can no longer create false visual regressions.
- Added explicit Ubuntu CI dependencies for FFmpeg and zlib development headers. The workflow is configured locally but has not yet run on GitHub because these changes have not been pushed.
- Final local gates after phases 1-6: strict Pyright has zero errors/warnings, both X3/X4 Python EPUB/video scenarios pass, all 12 native tests pass, the native emulator builds, and the normal ESP32-C3 firmware builds (`5268463` bytes flash, `49548` bytes RAM).
- Formatted all modified/new C and C++ files, including the isolated FreeInk SDK patch, with clang-format 21.1.8. Both repository and submodule diff checks pass; `books-for-testing/` remains locally ignored.
- Remaining external checks are deliberately tracked rather than claimed: observe the new CI job on GitHub after an authorized push, and cross-check `devicePathHash32` against an actual device SD cache when one is available. Calibrated timing/ghosting profiles remain deferred.
