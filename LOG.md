# Emulator Implementation Log

## 2026-07-12

- Started implementation from `fdd1ad30` on branch `emulator`; worktree was clean and matched `origin/emulator`.
- Confirmed `NOTES.md` does not yet exist.
- Confirmed `uv 0.9.5` and FFmpeg 6.1.1 are installed; `pio` is not currently on `PATH`.
- Began phase 1 from `docs/emulator-plan.md`: native execution spine, framed protocol, deterministic time, and scheduler compatibility.
- Added the PlatformIO `emulator` native target and a small host executable with explicit `--device` and artifact-directory configuration.
- Added Content-Length framed JSON-RPC over stdio, protocol/version negotiation, an uncalibrated timing marker, deterministic clock controls, and canonical manifest/event artifacts.
- Installed PlatformIO's native platform and native ArduinoJson dependency through the existing PlatformIO cache. Fixed the resulting missing `<optional>` include.
- Verified an X3 protocol smoke run: four ordered responses, a 125 ms simulated-time advance, and matching manifest/event files.
- Verified the existing `default` ESP32-C3 firmware still builds successfully (`5267981` bytes flash, `49532` bytes RAM reported).
- Added a deterministic, logically single-core scheduler with serialized host task dispatch, simulated delays/timeouts, notifications, priority/creation-order scheduling, task diagnostics, and a 5 s wall-time watchdog.
- Added native PlatformIO tests for deterministic task order, notification timeouts, and watchdog termination. All 3 tests pass; the watchdog exits a stalled child with code 70.
- Verified X3 and X4 both handshake and that two identical initialize/state/shutdown runs produce byte-identical event traces for each profile.
- Read the phase-1 checkpoint in `NOTES.md`; its X4, determinism, scheduler, and watchdog reminders are now addressed.
- Added the narrow Arduino/FreeRTOS compatibility surface used by the app today: simulated `millis`/`micros`/delay/yield, pinned task creation, notifications, mutexes, recursive mutexes, mutex ownership/peek, and critical sections.
- Added a weak-linked shared `setup()`/`loop()` lifecycle runner. Setup may auto-advance through deterministic delays; normal loop execution is bounded by explicit caller time advances so periodic tasks cannot run forever.
- Changed protocol/trace sequencing so recorded events and responses consume one shared monotonically increasing sequence. Reverified byte-identical X3/X4 runs; response sequences are `2,4,5,7` around three trace events.
- Native emulator suite now has 7 passing tests covering scheduling, priority/creation-order ties, bounded time, lifecycle, FreeRTOS primitives, and watchdog behavior.
- Applied the phase-1 reviewer follow-up before commit: FreeRTOS task priority is now preserved and runnable tasks resolve by priority then creation order, matching the plan.
- Committed phase 1 as `fd6287eb` (`feat: add native emulator execution spine`) before continuing, as requested.
- Added `books-for-testing/` to `.git/info/exclude`; the local ebook corpus stays invisible to Git without changing the repository `.gitignore`.
- Began phase 2 with immutable directory fixtures copied into private `artifacts/sd` run storage. Fixture traversal rejects symlinks and parent escapes, sorts UTF-8 device paths, records file/directory/byte counts, and hashes paths plus contents into the manifest.
- Added directory-backed native `HalStorage`/`HalFile` implementations and tests for sorted enumeration, reads/writes, path confinement, fixture immutability, and fresh-run isolation.
- Replaced four host-sized `std::hash<std::string>` cache-path uses with a fixed `devicePathHash32` implementation matching 32-bit libstdc++ Murmur hashing, preserving existing X3/X4 cache directory names. Added known-vector tests and reverified the normal firmware build (`5268421` bytes flash, `49532` bytes RAM).
- Audited shared persistence call sites: the core serializer uses fixed-width lengths and current book/settings cache writes do not persist host-sized `size_t`, `long`, or whole native structs. The cache-path hash was the confirmed host-ABI mismatch.
- Added the native Arduino compatibility required by the real application sources (`String`, `Print`/`Stream`, serial, deterministic RNG, SPI, and explicit offline Wi-Fi/HTTP/OTA/flash boundaries). The production `src/` application, activities, EPUB/TXT/XTC, rendering, settings, and persistence graph now compiles and links under PlatformIO Native.
- Selected native HAL implementations at compile time while retaining the embedded HAL sources unchanged for firmware builds. PNGdec/JPEGDEC and the existing QRCode dependency now build in native mode.
- Booted the real X3 application against `test/epubs`: shared `setup()` detected X3, initialized display/fonts, entered Boot, then the first explicit 10 ms advance ran the production loop and transitioned to Home. The scheduler was quiescent with `app-main` sleeping and `ActivityManagerRender` waiting for notification.
- Made firmware-style static initialization safe before the emulator session exists (`millis()`/`micros()` report zero) and allowed the host process to destroy the otherwise process-lifetime `ActivityManager` singleton cleanly.
- Added canonical `storage.operation` trace events with device path, operation, byte count, success, simulated time, and the shared response/event sequence.
- Verified both X3 and X4 boot through the production lifecycle to Home. Two identical runs per profile produced byte-identical 14-event traces, including boot storage activity.
- Expanded the native suite to 11 passing tests. It now recreates a runtime over the same run storage to prove reset preservation, starts a fresh run from the immutable fixture, builds a real `book.bin` from `test_text_decorations.epub`, reloads it through `Epub::load(false)`, and round-trips `progress.bin` through the shared reader path.
- The EPUB round-trip exposed a recursive `HalFile::write(uint8_t)`/`Print::write(buffer)` overload on native builds; added the exact bulk-write override and reverified the embedded firmware build (`5268369` bytes flash, `49532` bytes RAM).
- Recorded the migration-free 32-bit cache-hash choice in the plan's Decision section rather than adding an ADR.
- Began phase 3 by making the FreeInk display library host-selectable at its existing `EpdBus` seam. The real `FreeInkDisplay`, `Uc8253X3Driver`, `Ssd1677Driver`, LUT selection, refresh promotion, grayscale policy, and controller command sequences now run in the native target; only the physical SPI/GPIO bus implementation is replaced.
- Kept the `freeink-sdk` changes isolated in the dirty submodule for independent review: hardware `EpdBus.cpp` is excluded only for `CROSSPOINT_EMULATED`, host `isBusy()` is supplied by the native bus, and non-selected M5/Murphy sources are gated before hardware-only includes. No submodule pin has been moved or committed.
- Added a native panel model for UC8253 and SSD1677 RAM planes, controller commands, power/BUSY state, refresh generation, uncalibrated simulated refresh durations, grayscale composition, retained visible state, and deterministic approximate fast-refresh residue.
- Added time-resolved full-refresh black/white flashes and non-full drive/settled transition frames to the panel history. Phase timestamps are emitted in the canonical trace for later replay/video sampling.
- Replaced the temporary high-level native `HalDisplay` implementation with the shared production `HalDisplay` and real FreeInk facade.
- Added dependency-free deterministic 8-bit grayscale PNG encoding plus `capture.panel` and `capture.framebuffer` protocol methods. Verified valid X3 792x528 and X4 800x480 PNGs with distinct framebuffer/panel hashes.
- Bumped the panel-model protocol/manifest version to 1 and added explicit white/black initial-panel configuration; a fresh run defaults to white while reinitializing the same bus retains visible pixels.
- Fixed scheduler shutdown ordering exposed by the render task: app tasks now unwind while the FreeRTOS runtime remains installed, and semaphore cleanup becomes non-blocking once shutdown begins.
- Native suite now has 12 passing tests, including direct real-driver command checks (`UC8253 DTM2` versus `SSD1677 BW RAM`) and visible panel retention across deep sleep and bus reset for both profiles.
- Added native-geometry PNG initial panel seeding through PNGdec. Fixed a false read failure caused by checking `eofbit` after `istreambuf_iterator` consumption; invalid files now report whether opening, geometry, or decoding failed, and valid supplied images emit an `initial-png` panel phase.
- Completed the phase-3 determinism gate with two fresh black-seeded boot-to-Home runs for each profile. X3 panel/framebuffer captures were byte-identical across runs (`0ca00c…`); X4 panel captures matched (`157de1…`) and framebuffer captures matched (`0058af…`). All four files were valid native-geometry grayscale PNGs.
- Re-ran all 12 native tests after partial-window and supplied-PNG support, then rebuilt the normal ESP32-C3 firmware against the guarded FreeInk SDK changes (`5268369` bytes flash, `49532` bytes RAM). Phase 3 exit criteria are met; calibrated device timing/ghosting profiles remain deliberately deferred.
- Added `capture.screenshot` for natural human review: it derives a clockwise-rotated presentation PNG while preserving controller-native `capture.panel`/`capture.framebuffer` artifacts as regression truth. Verified upright X3 528x792 and X4 480x800 Home screenshots; the handshake and manifest now record the 90-degree review rotation.
- Began phase 4 by compiling the SDK's real `InputManager` in the native target. Protocol press/release changes feed realistic ADC-ladder/power-pin samples, then pass through the existing two-sample 5 ms debounce, native `HalGPIO`, and `MappedInputManager`; the FreeRTOS compatibility layer now also supplies the queue calls required by the SDK.
- Added stable shared activity IDs for the supported slice (`boot`, `home`, `file_browser`, `reader.epub`), completed-render generations, debounced input edge/hold tracing, panel BUSY state, and explicit activity/render/panel/storage wait commands with simulated and wall timeouts.
- Verified physical Down selects Recent Books on Home, and a 800 ms physical hold reports a debounced 780 ms live hold / 800 ms released hold. A fixture swapping Back and Confirm proved physical Back traverses the real remap path and opens File Browser on both profiles.
- Added mapping-aware `input.pressAction`/`input.releaseAction`: logical Confirm resolved to physical Back under the swapped fixture, then the application independently remapped that physical edge back to Confirm. This is the Playwright convenience path without bypassing firmware input behavior.
- Re-ran the 12 native tests after the input and observability changes; all pass.
- Added reset preparation plus Python-owned relaunch: the native protocol snapshots visible native panel pixels and exposes the writable SD directory, exits, then the client starts a fresh process using those as initial inputs. Verified the relaunched run records `initial-png`, returns through Boot/Home, and preserves its SD fixture identity/state.
- Added the `uv`-managed, zero-runtime-dependency synchronous Python package under `emulator/python`, with context-managed lifecycle, typed physical/logical controls, waits, captures, natural screenshots, reset, optional pytest fixtures, and automatic retention of temporary artifacts on failure. Strict Pyright reports zero errors/warnings.
- Added the real EPUB vertical slice: each profile boots, opens File Browser with mapped physical controls, opens the existing 4 KB text-decoration EPUB, waits for `reader.epub`, turns a page through the side-button mapping, and captures panel/framebuffer/review images. Two runs per profile produce byte-identical normalized traces and panel PNGs.
- Visual review of the naturally rotated EPUB captures caught two panel-model bugs before goldens: grayscale strips are overlays on the displayed BW base rather than absolute 2-bit frames, and SSD1677 window-register bytes arrive through four individual `data()` calls. Corrected both, recognized powered custom-LUT grayscale sequences, and reduced the deliberately uncalibrated fast-refresh residue from an unrealistic 25% to a subtle 1/16.
- Switched deterministic PNG encoding from stored DEFLATE blocks to system zlib level 9. Canonical page captures fell from roughly 400 KB to 28–31 KB while remaining byte-identical across runs.
- Added visually reviewed compact X3/X4 native-orientation page-2 panel goldens and an Ubuntu CI job that builds the emulator, runs strict Pyright, executes both Python scenarios, and runs the native suite.
- Persisted every visible panel transition as a trace event referencing a content-deduplicated frame PNG. The X3 EPUB scenario records 32 transition events over 15 unique frames; X4 records 20 events over 19 unique frames.
- Added trace-derived fixed-FPS MP4 export through the system FFmpeg CLI. Video presentation rotates native panel frames 90 degrees clockwise for natural review, records encoding/probe metadata, and validates the exact output frame count with FFprobe.
- Verified deterministic replay by exporting each profile's same trace twice at 8 FPS and comparing sampled frame identities, frame count, and simulated duration. X3 produces 61 frames over 7.625 s at 528x792; X4 produces 63 frames over 7.875 s at 480x800.
- Changed image golden assertions to decode PNG scanline filters and compare the grayscale pixel planes and geometry. Encoded artifacts remain compact, but zlib-version differences can no longer create false visual regressions.
- Added explicit Ubuntu CI dependencies for FFmpeg and zlib development headers. The workflow is configured locally but has not yet run on GitHub because these changes have not been pushed.
- Final local gates after phases 1-6: strict Pyright has zero errors/warnings, both X3/X4 Python EPUB/video scenarios pass, all 12 native tests pass, the native emulator builds, and the normal ESP32-C3 firmware builds (`5268463` bytes flash, `49548` bytes RAM).
- Formatted all modified/new C and C++ files, including the isolated FreeInk SDK patch, with clang-format 21.1.8. Both repository and submodule diff checks pass; `books-for-testing/` remains locally ignored.
- Remaining external checks are deliberately tracked rather than claimed: observe the new CI job on GitHub after an authorized push, and cross-check `devicePathHash32` against an actual device SD cache when one is available. Calibrated timing/ghosting profiles remain deferred.
- Deep-reviewed the complete `develop...emulator` implementation after commit `440efd3d`, starting from the latest `NOTES.md`. The requested phase-sized history split can no longer be applied without rewriting the already-pushed branch, so this cleanup remains a separate reviewable change instead.
- Removed the `wait.storageIdle` protocol/client surface. Native SD operations are synchronous and complete before their response, so the method was a misleading unconditional-success wait; the plan now states the actual contract.
- Removed dead panel write flags and unused public snapshot fields, collapsed duplicate scheduler dispatch and input-response construction, consolidated physical-button names, and exposed the session configuration through one storage accessor instead of four unrelated pass-through getters.
- Centralized trace, panel-model, and timing-profile metadata shared by the manifest and handshake, preventing two copies of version strings from drifting.
- Reused system zlib for PNG CRC and uzlib-compatible CRC/Adler wrappers instead of carrying local checksum implementations.
- Simplified Python lifecycle cleanup so every exit path reaps or kills the child process, and reduced the golden decoder to the filter-0 grayscale PNG format emitted by the canonical writer. Pixel-plane golden comparisons and natural review rotation remain unchanged.
- Removed the emulator-only global declaration from the shared `MappedInputManager` header, made the `ActivityManager` singleton destructor ordinary, marked `HalFile::flush` as an override, and compiled render-generation tracking only for the emulator. The implementation cleanup removed 84 net lines across 17 files before this log entry.
- PlatformIO cppcheck is not currently a valid emulator gate: it fails with 29 ArduinoJson macro-expansion errors and hundreds of native compatibility/unused-path style false positives. Recorded the failure rather than weakening cppcheck globally.
- Reverified the cleanup: clang-format 21, all 12 native tests, native emulator build, strict Pyright (zero diagnostics), both X3/X4 deterministic EPUB/golden/MP4 scenarios, and the normal ESP32-C3 firmware build all pass. The production build is back to `5268359` bytes flash and `49532` bytes RAM, saving 104 bytes flash and 16 bytes RAM versus the pre-review build.
- Queried the fork after the earlier push: no GitHub Actions run exists for the `emulator` branch because the workflow only runs pushes to `master` or pull requests. `books-for-testing/` contains no real-device `.crosspoint` cache, so the reviewer-requested hardware hash cross-check still needs an SD dump.
- Began the phase-7 measurement harness without creating timing profiles. Added a calibration-only firmware environment; normal and release builds do not compile its serial control surface.
- Reused FreeInk's existing button-state hook for serial-driven Back/Confirm/Left/Right/Up/Down/Power input. Injected states still pass through the production `InputManager` sampling, debounce, `HalGPIO`, and mapping path. The calibration protocol also reports X3/X4 identity, device `millis()` timestamps, and script markers.
- Added the `crosspoint-calibrate record` Python CLI. It opens the physical device through pyserial, starts FFmpeg against V4L2/DirectShow/AVFoundation, runs an ordinary Python `run(device)` scenario, applies explicit 0/90/180/270-degree natural-review rotation, and retains MP4, raw serial, timestamped host/device events, FFmpeg diagnostics, and a manifest.
- Added a ten-page-turn example scenario and documented the flash/capture command. No separate scenario DSL or FreeInk SDK change was needed.
- Verified the host serial client against a pseudo-terminal, including handshake, markers, button down/up, artifact logging, and clean shutdown. Strict Pyright reports zero diagnostics and the two X3/X4 EPUB/video Python scenarios pass.
- Verified the attributable calibration firmware (`5269679` bytes flash, `49532` bytes RAM), normal firmware (`5268359` bytes flash, `49532` bytes RAM), native emulator build, and all 12 native tests. PlatformIO's native test target replaces the emulator program, so the program was rebuilt before the Python scenarios.
- This WSL/Linux environment currently exposes no `/dev/ttyACM*`, `/dev/ttyUSB*`, or `/dev/video*`; an actual synchronized device/webcam run remains hardware-gated until both devices are passed through or the capture CLI is run on a host that can see them.
- Linux USB/IP later exposed the MX Brio as `/dev/video0`. Captured and visually checked a 3.0 s, 90-frame, 30 FPS smoke video at 640x480, then rotated it 90 degrees clockwise to a natural 480x640 review MP4. The 1080p MJPEG mode stalled and required stopping its exact FFmpeg child; raw 640x480 was reliable. The reader serial device is still not attached to Linux, so this run validates webcam capture and orientation but not synchronized CLI-driven input.
- Attached the Espressif reader interface through Linux USB/IP as `/dev/ttyACM0` (`303a:1001`), flashed the calibration build, and completed the first synchronized physical run. The handshake identified an X4 running `1.4.1-dev-emulator-f42919ca-calibration`.
- The host injected physical Down, Up, Down while the real reader displayed *The Silmarillion*. Serial traces recorded repeatable primary refresh waits of 505/506/506 ms and follow-up grayscale waits of 62/62/62 ms; final panel completion landed about 1.29 s after each press acknowledgement.
- The corresponding natural-review video is 19.133 s, 574 frames at 30 FPS, 480x640, SHA-256 `2b058b1f5af9d726d1cbfb7352f2ba84e124fc079214c330134cea5bad22ed7c`. Visual sampling confirms page 3/5 and page 4/5 transitions are upright, legible, and synchronized with the serial commands. Artifacts remain locally ignored under `artifacts/calibration/x4-reader-navigation-01/`.
- Extended the calibration-only protocol with buffered panel and SD benchmarks. Serial output occurs after each measured batch, so logging is outside the timed operation; the normal firmware remains unchanged at `5268359` bytes flash/`49532` bytes RAM, while calibration is `5272107`/`49532`.
- Captured a complete X4 timing set with the device fixed under the MX Brio and every MP4 rotated 90 degrees clockwise: 30 alternating real EPUB page turns, 10 black/white transitions in each fast/half/full mode, 20 iterations each at 4 KiB and 64 KiB SD I/O, and 5 iterations at 512 KiB. All accepted sources completed under the same commit-labeled calibration build.
- Real page turns split into 28 fast-primary samples (`1182.5` ms p50 total, `506` ms primary busy, `62` ms grayscale busy) and 2 periodic half-primary samples (`2562` ms p50 total, `1722` ms primary busy, `144` ms grayscale busy). The half sample is intentionally provisional because `n=2`.
- Controlled full-screen operations were highly repeatable: fast `2.4443135` s p50 total/`506` ms busy, half `4.3044015` s/`1722` ms, and full `6.3243125` s/`3743` ms. The 30 FPS video shows that full refresh retains the old image before passing through an intermediate gray state; total transfer, controller busy, and visible waveform therefore stay separate in the profile.
- The tested SD path is approximately `0.064 MiB/s` for sequential reads and large writes. Median total read/write times are `163.693/174.715` ms at 4 KiB, `1085.3845/1098.908` ms at 64 KiB, and `7913.163/8122.792` ms at 512 KiB. A first 20x512 KiB attempt exceeded the old 180 s host timeout and was excluded; the timeout now scales with requested bytes and iterations.
- Added `crosspoint-calibrate analyze`, which rejects incomplete/mixed-firmware runs and writes p50/p90/MAD summaries. Generated the provisional single-device profile `emulator/profiles/x4-hardware-2026-07-12-v1.json`; X3, temperature, device-population variance, and performance assertions remain deferred.
- Expanded calibration beyond the initial warm text case using `books-for-testing/`, which remains locally ignored. `stormlight.epub` is the largest fixture at `46,474,803` bytes and contains 844 HTML spine documents plus 834 images, so it covers large-container, cold-index, and image-heavy behavior in one attributable run.
- Added calibration-only per-book cache clearing and named EPUB entry. Clearing removes the complete derived cache and progress (the calibration SD is replaceable); named entry bypasses only file-browser selection, then uses the production `ReaderActivity`, EPUB load/index, section, image, render, and panel paths.
- The clean Stormlight cold run indexed in `23,201` ms (`5,151` ms OPF, `1,990` ms TOC, `16,059` ms `book.bin`). Cold-open marker to ready was `26,617` ms versus `4,303` ms warm. Four uncached image decode/cache operations had `556` ms p50 and `1,848` ms max; 50 cached image renders had `131.5` ms p50. Image-heavy fast-refresh pages reached `5,673` ms total versus roughly `1.1-1.3` s for later text pages.
- Drove Reader Settings through physical buttons from a deterministic Home selection, changed Medium to XL, recorded eight Stormlight page turns, restored Medium through the same UI, and visually verified the baseline. Seven XL fast-primary samples were `1,243` ms p50/`1,260.4` ms p90; the single periodic half-primary sample was `2,608` ms. The profile records these as a scenario, not a general font-size multiplier.
- Fixed two harness issues exposed by the broader workload: SD command timeout now scales with bytes/iterations, and the serial client discards queued replies before a new command so a late marker from an aborted run cannot satisfy a later same-named marker. Marker/button waits are long enough for blocking cold index and image renders without changing measured device time.
- Excluded the failed oversized SD capture, the first cold-run timeout, and three exploratory settings probes from the profile. Their locally ignored artifacts remain useful diagnostics, but only complete `x4-page-turns-30-v1`, `x4-panel-modes-v1`, `x4-sd-small-v1`, `x4-sd-512k-v2`, `x4-stormlight-cold-v2`, and `x4-stormlight-xl-font-v4` runs feed the generated profile.
- The captures added calibration-only commands between runs while the old version string stayed at the same commit SHA; production code and measured paths did not change, but the binary label alone cannot prove byte identity. Calibration builds now append a hash of tracked dirty changes (`dirty454773dc` in the final verification build, `5272793` bytes flash/`49532` bytes RAM), and the v1 profile records this provenance limitation explicitly.
- Final gates after the expanded profile: normal firmware remains `5268359` bytes flash/`49532` bytes RAM, strict Pyright is clean, the generated profile rebuilds byte-for-byte, all 12 native tests pass, the native emulator rebuilds, and both X3/X4 Python EPUB/golden/video scenarios pass.
- Compared the same Stormlight cover on the physical X4 and native X4 emulator. Both production paths place the decoded cover at `(8,14)` with `464x699` geometry and the same bottom `Cover` / `1/1 0%` status layout; the remaining status difference is live battery (`96%` physical, deterministic `100%` emulator).
- The settled digital content matches structurally, but not optically. A camera-cropped cover has normalized mean/stddev `0.421/0.117` versus emulator pixels `0.387/0.264`: the real panel is lighter, much lower-contrast, and spatially softened by e-ink plus camera optics, while the emulator exposes hard black/white halftone dots.
- The waveform comparison is the larger fidelity gap. Physical video holds the indexing/home image, shows a blank retained frame, drives a dark cover, and then settles through a softer grayscale state. The emulator reaches the same cover but uses a simple dark -> uniform light midpoint -> dark sequence; it does not reproduce the target-dependent optical fade or persistence. Its cold/index/image timing is also still uncalibrated, so it reaches the cover far sooner than the full physical cold path.
- Preserved ignored review artifacts under `artifacts/comparison/x4-stormlight-side-by-side/`: labeled full and rectified PNGs plus an 8.4 s, 30 FPS, 960x800 synchronized transition MP4. The clean emulator source trace/video is under `artifacts/comparison/x4-stormlight-emulator-cover/`.
- Made the provisional X4 JSON an executable timing profile. Python X4 launches now select it automatically; native launches accept `--timing-profile`, while X3 and standalone native runs remain explicitly `development-uncalibrated-v0`. Handshakes and manifests record the selected ID, calibration status, and source.
- Applied measured costs at existing boundaries: panel BUSY and transfer at `EpdBus`, controlled whole-operation pacing at `HalDisplay::refreshDisplay`, reader render phases at display/grayscale HAL calls, ordinary SD open/read/write/close at `HalStorage`, image decode/cache timing at `ImageBlock`, and the cold-index total at `Epub`.
- Raw SD throughput could not be applied blindly to derived `.pxc` images: on the same physical run the generic 4 KiB path was about `0.064 MiB/s`, but the 81,088-byte cover cache rendered in `132-134` ms. Treating derived image reads as the directly measured workload path reduced the emulator cover render from `24,910` ms to `5,657` ms, within `16` ms of the physical `5,673` ms sample.
- The calibrated Stormlight cold run now reports exactly the measured `23,201` ms total indexing duration (native unpadded work was `17,671` ms). Its navigation-to-reader observation is `27,900` ms versus the physical calibration command's `26,617` ms; those entry paths differ, so the phase boundary is canonical and the `1,283` ms end-to-end gap stays visible.
- Calibrated delays exposed automation assumptions hidden by host-speed I/O. `wait.activity` now completes after the activity's `onEnter` observation rather than when its object is merely installed, `wait.panelIdle` also waits for the render task, and Playwright-style taps last at least `100` ms so the reader's 50 ms power-saving poll cannot miss them.
- Replayed the naturally rotated calibrated X4 run at 30 FPS: `45.366667` s, 1,361 frames, 480x800, SHA-256 `5dba25cdca2c9dd1dfbf0bc9d33d6ef7d53d47ff94bf116bdcc66690`. The ignored `artifacts/comparison/x4-stormlight-calibrated/transition-comparison.mp4` aligns its cover transition beside the physical camera clip; timing is close, while the emulator still has the documented hard halftone and simplistic optical intermediate states.
- Verification after profile activation: all 12 native tests pass, both X3/X4 Python golden/video scenarios pass, strict Pyright is clean, the native emulator rebuilds, and normal firmware remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Extracted a first optical waveform from the 30 FPS physical cover clip using a fixed inner-screen crop (`x=90..359`, `y=70..519`) to avoid the bezel. The retained Home frame is stable through `3.0` s; it clears to a nearly uniform light frame by `3.167` s, drives the dark cover at `4.100-4.200` s, begins a second charge change at `6.600` s, reaches another light state at `7.200-7.300` s, returns dark at `8.167-8.300` s, and applies the softer grayscale state at `10.700-10.733` s. Frame quantization is `33.333` ms and camera exposure remains a confounder, so these are waveform evidence rather than controller timings.
- The aligned replay makes the remaining model error concrete: the emulator's first dark cover lands at the same visible point, but its second fast update occurs around two seconds too early and its grayscale transition is a single uniform midpoint. A later panel-model revision should represent the two fast-refresh charge cycles and target-dependent grayscale fade; the current timing profile intentionally does not turn these one-camera observations into a generalized waveform.
- The old `4,303` ms warm marker was scenario-coupled: the physical serial log spent about `3.4` s finishing Home's thumbnail. Added `calibrate_warm_epub.py`, which prebuilds the reader/thumbnail/CSS/section caches and waits for quiescent Home before a second open. The complete physical run measured `1,827` ms marker-to-ready and two identical `558` ms cached-metadata loads; its naturally rotated 30 FPS video is 36.5 s / 1,095 frames, SHA-256 `518350a5d9cffab45c923eb8d3548ebc3ba11fe465d2f87ff3c82d515d4c5d29`.
- That clean replay exposed a native HAL contract bug: `HalFile::available()` returned boolean readability instead of remaining bytes, so `CssParser` rejected and rebuilt every valid cache. Returning the capped remaining-byte count makes the existing 4,111-byte CSS cache load normally. The profile now paces large cached EPUB metadata to `558` ms and metadata-start-to-page-load to `1,586` ms; the native replay lands at `1,587` ms. The regenerated v1 profile includes the seventh attributable source and has SHA-256 `2a9911458fa77e0ff91f976b1587593fdbe664d334738e8a01933b32bda423d5`.
- Correct CSS-cache reload changes the small EPUB's pagination from two pages to four because its persisted text-decoration styles now take effect. Visually inspected the old/new X3 and X4 captures, then updated the two existing page-2 goldens to the correctly styled output (`d605d7…` X3, `15ff1a…` X4); repeated runs remained byte-identical.
- Restricted the single `23,201` ms cold-index observation to the firmware's existing large-spine path (`>=400` documents) so a 4 KB test EPUB is not made Stormlight-slow; the small fixture indexes natively in `1,926` ms and reaches Reader in `4,220` ms including scripted input.
- Added repeatable optical panel analysis around FFmpeg `signalstats`. The recorder now timestamps both the first decoded camera frame and the later encoder-progress observation; the latter lagged by about `1.157` s and is no longer accepted for profile input. The analyzer verifies the video hash, aligns serial operations to decoded-frame PTS, collapses adjacent changed frames into transition peaks, and preserves the crop/alignment limitations beside the measurements.
- Captured the canonical 30-operation X4 optical run `x4-panel-optical-sync-v4` at 30 FPS (video SHA-256 `223403c3cfacd6624b3953ac4fdd22f99d1186a98c3731395639f7aab0988bc3`). Fast visible settling is `970.538` ms black / `967.869` ms white p50; half is `2,358.207` / `2,404.809` ms. Full refresh is strongly target-dependent: black changes span `1,766.7` ms from `2,465.804` to `4,218.191` ms, white changes span `1,200` ms from `2,756.835` to `3,941.93` ms, with a `267` ms median charge-pulse interval.
- Regenerated `x4-hardware-2026-07-12-v1.json` with the optical run as its eighth attributable source (SHA-256 `088762185a5c83b96b10823c4692bf932d3fb5a394dff44ae33accdfa418b033`). Profile generation rejects non-decoded-frame alignment, mismatched capture hashes, mixed firmware/device sources, and missing black/white summaries.
- Bumped the panel model to version 2 and separated controller completion from camera-visible state. BUSY still controls firmware progress; trace/MP4 transitions now use the measured black/white fast and half onset/settling times, while full refresh alternates measured `267` ms black/white charge pulses until the target-specific optical end. X3 and uncalibrated native runs retain the earlier model, and grayscale optical behavior remains explicitly uncalibrated.
- Replayed the cold Stormlight path with the new optical model. The native trace remains `45.4` s / 1,362 frames at 30 FPS and the settled cover is unchanged, while intermediate frames now retain the old image until the measured onset instead of settling at BUSY. The ignored 8.4 s side-by-side review is `artifacts/comparison/x4-stormlight-optical-v2/transition-comparison-optical-v2.mp4` (SHA-256 `a1babc8591d080e0f78a5cac2e314302028bafe82a9a1a9b767a4a31364d83a0`); it also makes the remaining optical gap honest: the camera image is lighter, softer, and multi-stage, while the emulator still uses ideal uniform charge frames and a hard digital halftone.
- Added a controlled differential-grayscale benchmark with alternating light/dark targets after fast and half primaries. Per-operation device offsets keep the primary refresh and controller-RAM cleanup outside the timed optical window. The first run showed that only the first half-context sample retained half state; the benchmark now re-establishes the requested primary before every sample. A second run used a white base and correctly produced almost no drive—the production renderer paints grayscale candidates black before the LUT lightens them—so both runs remain excluded diagnostics.
- The accepted black-base `x4-grayscale-optical-v3` run contains ten samples per primary under firmware `1.4.1-dev-emulator-f42919ca-calibration-dirty56e8767e`; its 124.833 s / 3,745-frame video has SHA-256 `5bdc743ab4719760c4f6c4312349b2e77b037d4c5315f6e23aed8ef7068f9f15`. Grayscale activation is `84.832` ms p50 after fast and `166.774` ms after half. Camera-visible light/dark settling is `154.308/143.867` ms after fast and `249.249/230.392` ms after half, with five samples per target.
- Added `crosspoint-calibrate waveform-gray`; it verifies decoded-frame alignment and capture hash, combines serial batch receipt with device-measured operation offsets, and retains camera luma as evidence without mapping it onto canonical digital pixels. Profile sources now record their individual firmware labels so calibration-only command revisions remain attributable while base timing runs still must share one firmware.
- Regenerated `x4-hardware-2026-07-12-v1.json` with nine sources (SHA-256 `3a5321abcbc39e92d0daa771dbae41d5f0ee221ae9a18a41eaf2bb4b14942aac`). Panel model v2 now schedules grayscale drive/settling from the measured primary-context and target summaries while firmware state still advances at BUSY.
- Re-audited the physical/emulator cover comparison against serial phase boundaries. The apparent late physical grayscale was the next page render: `calibrate_large_epub.py` queued `cold-page-01` while the first cover was still painting, whereas the emulator comparison stopped after that cover. For the comparable first render, physical versus emulator visible onsets are `4.733/4.949` s for the image-blanking fast pass, `5.800/5.882` s for the restored-cover fast pass, and `7.500/7.625` s for grayscale—deltas of `216`, `82`, and `125` ms at 30 FPS. Replaced the misleading long review with the 7.8 s `artifacts/comparison/x4-stormlight-optical-v3/first-cover-comparison-v3.mp4` (SHA-256 `309f8e357d490503370a7bad32a72249af11d7f375066f5dbb20443b1abef934`).
- Strengthened the provisional half-primary reader timing through the physical Settings UI: changed refresh frequency from every 15 pages to every page, opened cached Stormlight, issued ten page turns, and attempted to restore the 15-page baseline. The complete natural-orientation `x4-stormlight-half-pages-v2` capture is 75.433 s / 2,263 frames at 30 FPS, SHA-256 `716dfe3eaf52733641375ce3051ff1cbe788a8b0128dcf257074da75ac866fe1`.
- Nine ordinary half-primary renders are tightly grouped at `2,622` ms p50 / `2,650.8` ms p90 total, `1,723` ms primary BUSY, and `144` ms grayscale BUSY. Two image-heavy renders used the fast primary path and remain explicit workload evidence; they do not contaminate the half-primary profile. This supersedes the original `n=2` half estimate.
- Added an explicit supplemental half-render source to profile generation, preserving the same-firmware rule for base timing runs while recording the calibration-only firmware label per source. The v1 profile now has ten attributable sources and SHA-256 `0f7cbaf417a8f250b0cd8ca277b3c00efd5100be2eef9b862bc7b6276d674a60`; deterministic regeneration and strict Pyright pass.
- A transient device stall exposed a five-minute cleanup hazard: calibration shutdown reused the public button-command timeout. Best-effort release now waits at most one second, so failed hardware runs preserve their artifacts and return promptly.
- Verification after the half-primary update: the profile regenerates byte-for-byte, strict Pyright is clean, all 12 native tests pass, the emulator rebuilds, both X3/X4 Python EPUB/golden/video scenarios pass, and normal firmware remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Comparing the new physical half-refresh capture against a runtime emulator replay exposed a visual-model error hidden by full-screen black/white calibration: a natural X4 half refresh shows the *new page inverted* before settling, while panel model v2 held the old page and used a uniform midpoint.
- Added `crosspoint-calibrate waveform-half`. Across all nine physical half-primary text renders, the inverted-target peak starts at `352.557` ms p50 (`4.601` ms MAD) after `displayBuffer`, and the target transition peaks at `1,097.179` ms (`6.853` ms MAD). The analyzer verifies the source-video hash and decoded-frame alignment and retains all per-page peaks in the ignored evidence JSON.
- Panel model v3 applies those natural reader peaks and renders the inverted target explicitly. On the clean `half-page-03` comparison, physical/emulator input-to-inversion differs by about `17` ms, input-to-target settling by `39` ms, and input-to-render completion by `16` ms. The natural-orientation 5.567 s side-by-side review is `artifacts/comparison/x4-half-primary-runtime/half-page-comparison-v3.mp4` (SHA-256 `c00ac469d7043b3071a2858498e60783c2108bd6752ff39abf2a86f2f2babaf3`). The deterministically regenerated ten-source profile is SHA-256 `a84aa137be76d545e2d5f63454d33ce654deb2991f89f1099bf2f52869f358ac`.
- Verification after panel model v3: deterministic profile regeneration, strict Pyright, all 12 native tests, native emulator rebuild, and both X3/X4 Python EPUB/golden/video scenarios pass. The normal firmware remains exactly `5268359` bytes flash / `49532` bytes RAM.
- A follow-up fast-page capture proved the first refresh restore had not persisted: all twelve turns in excluded diagnostic `x4-stormlight-fast-pages-v1` were still half-primary. Settings navigation had only a 500 ms settle while its repaint takes about 506 ms; a missed row plausibly opened the adjacent three-value popup, where three moves cycle to the original value. The reusable settings script now waits one second between rows.
- Repeated the restore with deliberately slower physical navigation and validated it behaviorally in `x4-stormlight-fast-pages-v2`: after the initial reader half refresh, all twelve marked page turns used `506-507` ms fast-primary and `62-63` ms grayscale BUSY. The normal 15-page cadence is now the verified device baseline. Its 30 FPS natural video has SHA-256 `1277c58a7ecdae562ad373acd9d6269af73fdfe79d4aa88b213794d727e7c6af`.
- Added `crosspoint-calibrate waveform-fast`. Twelve natural text pages have one direct optical transition: `266.196` ms p50 start, `276.226` ms dominant change, and `309.576` ms end after `displayBuffer`; unlike half refresh, no inverted state appears. Panel model v3 retains its brief old/new midpoint but uses these reader-content boundaries instead of the delayed controlled full-screen schedule.
- On clean `fast-page-03`, physical/emulator input-to-transition start differs by `39` ms, transition end by `49` ms, and render completion by `71` ms. The natural-orientation 4.067 s review is `artifacts/comparison/x4-fast-primary-runtime/fast-page-comparison-v1.mp4` (SHA-256 `d289367e6def1e5c985b53e8ef9e7fe66f7efe2ad160f709fd0c15ce30a87762`).
- The fast capture is the profile's eleventh attributable source. Final profile SHA-256 is `435fda421b1862142061d40a521b7ba86310052526ad21a4f61398cd9d2742cd`, and regeneration is byte-identical.
- Final gates after the natural fast/half waveform split: strict Pyright, all 12 native tests, native emulator rebuild, and both X3/X4 Python EPUB/golden/video scenarios pass. Normal firmware remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Calibrated repeated Home-to-File-Browser navigation over the attached X4 SD. Five accepted opens in `x4-library-navigation-v3` take `28` ms from activity entry to `displayBuffer`; input release to `displayBuffer` is `52` ms p50, and input release to controller wait completion is `640` ms p50. Its natural 480x640, 30 FPS capture is SHA-256 `d6cf16f9a42f264a1b3f009ad5958e6415d26e3ff0fd2967d83bd63588fd8037`.
- The first emulator replay took `1,332-1,333` ms from File Browser entry to display because every directory entry inherited the `102,500` us regular-file open cost. Added an isolated root benchmark: ten accepted samples enumerate 15 entries with `198` us p50 root open, `44,441` us p50 total enumeration, and `2,778` us p50 per `openNextFile` call. The complete accepted run is `x4-directory-benchmark-v2`; its firmware label is `dirty833702f8` and video SHA-256 is `ba77a17dd60ec0f089f50d9542791d4066828b13c24f8654476c5bfe6f67f5a7`.
- Split native directory timing from regular-file timing and traced each enumerated entry. The eleven-book emulator fixture now reaches `displayBuffer` `33-34` ms after File Browser entry, within `6` ms of hardware; the different root populations account for most of the remaining input-path gap.
- The aligned transition then exposed a second scope leak: the `66` ms EPUB prewarm/layout phase was applied in shared `HalDisplay` to every UI screen. Moved only that content-specific pacing to `EpubReaderActivity`; ordinary display transfer, BUSY, and optical scheduling remain shared. File Browser's modeled optical settle is now `343.376` ms after input versus about `361.576` ms from the physical display boundary plus the measured natural fast waveform, an `18.2` ms difference.
- Perspective-corrected the physical screen to the emulator's 480x800 geometry and reviewed the transition and settled frame side by side. Structure, title/header line, row geometry, icons, path bar, and soft-key layout agree; the real panel remains lighter/softer, while the emulator shows hard stippling and more obvious `1/16` retained-image ghosting. The physical SD is not raster-identical to `books-for-testing/`: it additionally shows `universalis/`, `enders-game.epub`, and `uy2026.epub`, and does not show `malazan-big.epub`.
- The ignored 5.033 s, 30 FPS, 960x850 review is `artifacts/comparison/x4-library-navigation-runtime/review-v4/library-screen-comparison-v4.mp4` (SHA-256 `f96fb3f3bc33af83e8af7ba824bf0aa07b9e42beb90e9006893909fbaf0d0d19`); its settled PNG is SHA-256 `627191c522085836761fc3e6dad8292aaf004003a8a6ca26d63c43c18fc9eb20`. The regenerated 12-source X4 profile is SHA-256 `155c62c3390d543ce7cd1f3e5c2753afaf4c3a0a40cf349004290dc99f255537`.
- Verification after library calibration: profile regeneration is byte-identical, strict Pyright is clean, both Python X3/X4 scenarios pass, all 12 native tests pass, the native emulator rebuilds after the test target, and normal firmware remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Extended the emulator protocol's lifecycle vocabulary with a stable `settings` activity name and added deterministic physical/emulator Settings scripts. The safe scenario enters Reader settings, selects Reader Font Size, moves within its popup, cancels without changing the value, and returns Home; the emulator can either wait after every action or reproduce the physical script's overlapping 500 ms cadence.
- Reused the already accepted `x4-stormlight-xl-font-v4` physical capture because the USB device was no longer attached. Its first Settings pass contains the same category, three row moves, popup open, and two popup moves. Physical popup input-down-to-`displayBuffer` boundaries are `439`, `277`, and `411` ms; the same overlapping emulator cadence reports `481`, `308`, and `435` ms, differences of `42`, `31`, and `24` ms. The preceding queued row-selection boundaries differ by `55-121` ms, dominated by main-loop sampling phase rather than panel throughput.
- No new timing constant was applied: once input cadence is reproduced, the existing `747` ms fast display operation preserves the queue ordering and popup responsiveness. Waiting after every automation action had hidden this by preventing the physical run's intentional render overlap.
- Added a perspective-corrected 2.233 s, 30 FPS, 960x850 popup review at `artifacts/comparison/x4-settings-navigation-runtime/review-v3/settings-popup-comparison-v3.mp4` (SHA-256 `a3f11622ceea755cc82607443ff5d25cb5f849cbd3d49e6fbc62e2873f416429`). Popup bounds, title/separator, option baselines, selected row, underlying Settings layout, and soft-key hints agree. The camera remains lighter/softer and the emulator retains hard halftone/ghost detail. Because this older recorder did not capture a decoded-first-frame timestamp, the clip is visually aligned at popup onset and remains review evidence rather than a profile source.
- The clean physical-cadence emulator replay is 36.033 s at 30 FPS, SHA-256 `37da6ea49bc0191627b9f9ff59a59e4a2f275c2b24413e79618f6b25030c7536`. Rebuilding with Settings detection isolated to the emulator protocol reproduced it byte-for-byte under `artifacts/comparison/x4-settings-navigation-runtime/run-v8-centralized/`; the production activity enum remains unchanged.
- Verification after the Settings comparison: strict Pyright is clean, both Python X3/X4 scenarios pass, all 12 native tests pass, and the native emulator was rebuilt after the test target. A stale PlatformIO firmware cache briefly reported an 8-byte flash increase; a full build with an isolated empty cache confirmed the unchanged production result of `5268359` bytes flash / `49532` bytes RAM.
- Closed the application-level sleep gap in the native HAL. `HalPowerManager::startDeepSleep()` and the GPIO fallback now trace the deep-sleep boundary and block only the emulated firmware task; the JSON-RPC session stays available for captures and retained-panel reset. Added the `sleep` lifecycle name and `examples/replay_sleep_reset.py`, which holds Power through the production path, asserts `app-main` is blocked, snapshots the visible panel, resets, verifies the snapshot byte-for-byte, and returns Home.
- The calibrated X4 sleep replay exposes the existing costs without adding a sleep-specific constant: Power action-to-debounce is `100` ms; debounce-to-state/popup display start is `517.101` ms; the popup fast operation is `747` ms; the sleep-screen full operation is `3,743` ms; and debounce-to-deep-sleep is `5,007.101` ms. The modeled optical full-refresh settles `475.191` ms after controller deep sleep, matching the e-ink property that the visible waveform continues after firmware stops.
- Identical final-script runs `x4-sleep-reset-runtime/run-v2` and `run-v3` have byte-identical normalized traces, panel PNGs, and MP4s. `sleep-entry.mp4` is 14.433 s at 30 FPS, SHA-256 `ce2b223c47bf94ed88d8212ef57dcbe4aa7af9b87983bf34ae4f571c7e41fc25`; `reset-to-home.mp4` is 7.300 s, SHA-256 `7fe15608a72370370570840f4d5bddd0895c5c8b18fd5edf41fff46232fba666`; the sleep and reset-snapshot PNGs share SHA-256 `df4847df27c7afb310a12aa4eee98b81bb5a28a2e238af04fee35cb9ca242d9b`. No physical sleep timing is claimed while `/dev/ttyACM0` and `/dev/video0` are absent.
- Verification after sleep/reset: strict Pyright is clean, both Python X3/X4 EPUB scenarios pass, all 12 native tests pass, the native executable was rebuilt after the test target, and a fresh cache-isolated production build remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Prepared the physical sleep-entry capture without weakening ordinary recorder failures. `HardwareDevice.wait_for_disconnect()` is an explicit terminal-scenario opt-in: it waits for the serial reader to observe USB removal, records the host timestamp in `events.jsonl`, and lets the manifest mark `terminalDeviceDisconnect: true`; an unapproved disconnect still fails the pending command immediately. The recorder omits only the impossible final serial marker and keeps the camera tail running while the e-ink waveform finishes.
- Added `examples/calibrate_sleep_entry.py` and its Linux command. It marks the Power-down boundary, holds the calibration firmware's physical Power control, and accepts only the resulting deep-sleep USB detach; the operator must wake the device before another run. A fake-serial smoke verified both branches and the recorded disconnect timestamp, explicit strict Pyright is clean for the hardware client and both sleep scripts, and the calibration CLI parser still starts normally. The actual physical capture remains pending because `/dev/ttyACM0` and `/dev/video0` are still absent.
- Verification after terminal-capture support: both Python X3/X4 EPUB scenarios pass, `crosspoint-calibrate record --help` exposes the expected camera-tail and rotation controls, and `git diff --check` is clean. Native and production sources were unchanged in this follow-up.
- A profile-consumption audit found that the runtime loaded the measured half-primary BUSY/display values but reused fast-primary prewarm/BW, grayscale-plane, grayscale-transfer, and cleanup fields. The profile already contains distinct half values, so this was an application omission rather than a missing measurement. Split `PrimaryRenderTiming` into fast/half instances, select the mode from the EPUB refresh cadence before layout pacing, and update it again at the actual display call so grayscale work cannot inherit stale state across readers.
- The corrected half replay now reports `2,625` ms total versus `2,622` ms physical p50; previously it was `2,563` ms. Its phases are `2,122` ms display (including the `79` ms pre-primary target), `135/138` ms grayscale planes, `146` ms grayscale display, and `80` ms cleanup. The fast control remains `1,186` ms versus `1,182.5` ms physical p50. Against accepted physical `half-page-03`, input-to-inversion error drops from about `22` to `9` ms and input-to-target error from about `44` to `31` ms under the existing camera/device alignment.
- Corrected half runs `run-v3-profile-complete` and `run-v4-profile-complete` have byte-identical normalized events, logs, screenshots, and 16.800 s MP4s. The MP4 SHA-256 is `b361276d51b3eb8b2fbff402bf6ef61d95046c11d5af02f9a3fb89e4e99c4ff9`; the naturally rotated settled PNG is `044fb9110c3f90c9e578c3e2e418077d5a6ab5f68a8068ab0a52a73218b57c17`.
- Verification after completing half-profile consumption: the 12-source profile regenerates byte-for-byte at SHA-256 `155c62c3390d543ce7cd1f3e5c2753afaf4c3a0a40cf349004290dc99f255537`, both Python X3/X4 scenarios pass, all 12 native tests pass, the native executable was rebuilt after the test target, and a fresh cache-isolated production build remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Audited the generic SD model against all three measured sizes before considering piecewise transfer timing. The current fixed open/close plus 512 KiB linear-rate prediction differs from physical totals by `-0.064%/-0.599%/-0.001%` for 4/64/512 KiB reads and `-4.506%/+0.697%/+0.004%` for writes. Kept the simple model: only the single-chunk 4 KiB write exceeds one percent, while correcting it would require stateful first-chunk and future-output-size inference that would make the accurately modeled large path less direct.
- Reused the accepted `x4-stormlight-cold-v2` serial log for the attributable Home-thumbnail boundary. Its successful large-EPUB JPG path takes exactly `3,394` ms from `Generating thumb` to `Generated thumb`, covering ZIP extraction, JPEG decode/scale, and BMP output. The generated profile records this as one sample; PNG, small-EPUB, device-population, and temperature behavior remain unmeasured.
- The first one-book emulator baseline took `4,382` ms because generic SD transfer pacing was charged inside the already whole-workload thumbnail boundary. Suppressing only transfer pacing while that large-JPG scope is active, retaining real host work and file open/close costs, and padding the remainder lands at exactly `3,394` ms. `wait_for_panel_idle()` now also observes a requested render and queued render-task notifications, so the replay cannot stop between Home's metadata and thumbnail paints.
- Final runs `x4-home-thumbnail-runtime/run-v5-profiled`, `run-v6-profiled-repeat`, and the small-EPUB-scope correction `run-v7-profiled-scope-final` have byte-identical normalized timing, final PNGs, and 52.800 s / 1,584-frame MP4s (SHA-256 `c16632c7503afc9dc3648f666820521487f37a03b01b358f463a613c34cc6d37`). The final natural-orientation PNG is `8e8cc9a14f5b84acb021d659433e26f91942b5359407f0b8cc628f04e63265e9`. A same-phase, perspective-corrected physical/emulator Loading still is `review-v1/home-loading-comparison.png` (SHA-256 `cf9715ea765c944d38e789e6b88a4eb9da82fa065c587d88052d0765700ad549`): geometry agrees, while hardware remains lighter and softer. The physical script exits Home as generation completes, so it contains no settled on-device thumbnail raster to compare directly.
- Verification after thumbnail calibration: the 12-source profile regenerates byte-for-byte at SHA-256 `8e507fee752f1cf3b59db3911c3331ef1da8e9382b049a5b836d797a37a73b1b`; explicit strict Pyright over source, tests, and examples is clean; both Python X3/X4 scenarios pass; all 12 native tests pass; and the runnable native emulator was rebuilt after testing. A clean normal-workspace build with an empty build cache confirms the unchanged production result of `5268359` bytes flash / `49532` bytes RAM; the ordinary stale cache again reported the known phantom 8-byte increase.
- Audited cache reset before treating the accepted cold-run marker as another profile constant. Its device-marker span is `390` ms and host command span is `287` ms, but Home loads the same 844-spine metadata cache during that interval and contends for storage, so neither isolates deletion. Kept it out of the profile. Calibration replies now include microseconds measured directly around `Epub::clearCache()`, and `calibrate_cache_clear.py` first enters File Browser to remove Home background work; a new physical run is required before applying any cache-clear timing. Strict Pyright and the calibration firmware build pass, while the production build remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Audited the already-profiled cold-index phases rather than accepting the exact total as sufficient. Physical OPF/TOC/book-bin is `5,151/1,990/16,059` ms, while the emulator was `3,840/7,281/12,080` ms: the `23,201` ms total pad hid an overcharged TOC and undercharged surrounding work. A diagnostic with generic transfer and total padding disabled measured raw native phases at `926/516/412` ms, so all three physical boundaries are reachable by padding.
- Applied the profile's existing phase fields. OPF and book-bin retain generic SD pacing and pad only their remainder; TOC alone suppresses generic transfer pacing because its many small writes are not represented by the bulk 512 KiB rate. Runs `run-v10-index-phases-final` and `run-v11-index-phases-repeat` report exactly `5,151/1,990/16,059/23,201` ms for OPF/TOC/book-bin/total, and their normalized events, final PNGs, and 52.800 s MP4s are deterministic. The MP4 and PNG remain byte-identical to the pre-split replay (`c16632c…` and `8e8cc9a…`), so this corrects observability and intermediate pacing without changing settled output or end-to-end duration.
- Verification after phase-level indexing: the profile still regenerates byte-for-byte at SHA-256 `8e507fee752f1cf3b59db3911c3331ef1da8e9382b049a5b836d797a37a73b1b`; strict Pyright over source, tests, and examples is clean; both X3/X4 Python scenarios and all 12 native tests pass; and the runnable native emulator was rebuilt after testing. Calibration firmware builds with the cache-clear duration field, and the normal firmware remains exactly `5268359` bytes flash / `49532` bytes RAM.
- Swept four representative ignored corpus books to test whether the single Stormlight cutoff generalizes. WOT (`831` spines/`785` TOC) and Sun Eater (`808`/`434`) cross 400 spines and therefore reproduce Stormlight's exact `5,151/1,990/16,059/23,201` ms phases. Mistborn (`370`/`205`) and LOTR (`334`/`147`) miss the guard; generic transfer pacing produces `31,449` and `14,654` ms totals, dominated by `26,966` and `10,408` ms TOC phases. Those are model artifacts, not hardware claims.
- A second corpus run zeroed transfer, phase, total, and thumbnail padding. Every book collapsed to identical `926/516/412/1,854` ms index phases despite sizes from 3.7 to 16.5 MB and very different manifests, confirming that host execution supplies ordering but no device-speed scaling signal. Kept the existing provisional Stormlight boundary and did not invent a byte/spine curve from one hardware point. Added `calibrate_epub_corpus.py` to gather WOT, Sun Eater, Mistborn, and LOTR cache-clear, cold-open, page, and Home timings in one physical/video run when the devices return.
- Visually inspected all four naturally rotated settled Home captures: covers, title/author layout, menu geometry, and soft keys render correctly; WOT's long author line truncates through the production ellipsis path. The active model gives large-book JPG thumbnails the measured `3,394` ms boundary, while unprofiled Mistborn/LOTR take `4,810/3,443` ms from generic transfer artifacts; the zeroed diagnostic makes all four `413-414` ms. These are further reasons to await physical corpus data rather than fit emulator-only output.
- The same sweep shows the warm-metadata cutoff effect: WOT and Sun Eater are both forced to Stormlight's `558` ms cached-load sample, while Mistborn and LOTR complete in `104/103` ms of host/open-close work. The physical corpus analyzer now keeps each Home metadata duration with its book instead of pooling them, so the next capture can calibrate warm scaling alongside cold indexing and thumbnails.
- Completed the profile-field consumption audit. Every canonical nominal p50 now reaches its intended runtime boundary. p90/MAD, camera luma and dominant peaks, directory aggregate totals, alternate-size SD samples, mixed cold pages, and XL-font pages remain tolerance or scenario evidence by design; applying them would double-count or overgeneralize. The profile now retains Stormlight's `1,686` manifest, `844` spine, and `776` TOC counts for later corpus scaling, changing the deterministic JSON SHA-256 to `5948849d438d4f2ed9a233057fe898af2e29570e459f223987276ce9cedc845c` without changing runtime timing.
- Verification after the offline corpus audit: strict Pyright, the corpus-parser smoke, deterministic profile regeneration, both Python X3/X4 scenarios, and all 12 native tests pass; the runnable emulator was rebuilt after testing. A repeat WOT run reproduces every phase timestamp and the settled Home PNG byte-for-byte (SHA-256 `2d3146987958645e97153a1b78893e6b94780c25586110b1cccc859a0922c4d6`). Production C++ was unchanged in this batch; its last clean isolation build remains `5268359` bytes flash / `49532` bytes RAM.
- Added `examples/compare_physical_emulator.py` as the reusable natural-orientation review path. It perspective-corrects the webcam screen, scales both sides to 480x800, labels and stacks them, and selects explicit frame ranges after FPS normalization. The frame-based selection matters: an initial time-trimmed two-input FFmpeg graph briefly replayed unrelated emulator frames even though exact source-frame inspection proved the panel was static.
- Aligned the accepted physical and emulator Home-thumbnail runs at their `Generating thumb` boundaries (`78.783` s camera / `48.709` s emulator). The comparable 3.3 s interval remains on the Loading screen on both sides; card, placeholder, Loading popup, title/author, menu rows, and soft keys agree. The physical panel is lower-contrast and softer, while the emulator has hard one-bit edges and a visibly regular stipple. The ignored 99-frame, 30 FPS, 960x850 review is `artifacts/comparison/x4-home-thumbnail-runtime/review-v3/home-loading-comparison.mp4` (SHA-256 `03f8bb31b8e91fa321ba1f3060674b0669ac2b232c6fe589d51bbf168e1d6d22`); its first-frame PNG is SHA-256 `31b3ee299c15b427ec7d4675afbd83c1f546476019989f72b30fb6d28841ff57`.
- Kept the comparison scoped to the common state. At 3.394 s the physical calibration script deliberately exits Home and paints the reader, while the emulator scenario stays on Home and paints the generated thumbnail; that post-boundary divergence is scenario control, not a visual-model error. The next physical corpus capture must dwell on Home after thumbnail completion to provide the missing settled cover-raster and repaint-waveform comparison.
- Verification after adding the visual-review path: two renders of the corrected comparison are byte-identical; strict Pyright over `src`, `tests`, and `examples` is clean; `git diff --check` is clean; and the X4 profile regenerates byte-for-byte at SHA-256 `5948849d438d4f2ed9a233057fe898af2e29570e459f223987276ce9cedc845c`. Native and firmware sources were unchanged in this visual-only batch. Physical cache/corpus/sleep capture remains pending because `/dev/ttyACM0` and `/dev/video0` are absent.
- Tightened the pending corpus capture for the visual-fidelity requirement. `HardwareDevice.wait_for_log()` retains a bounded recent serial window, filters matches after an explicit host timestamp, and waits without injecting another command. The corpus script now marks before returning Home, waits for that book's successful `Generated thumb` line, and holds the settled screen for two seconds. This replaces the guessed five-second delay and guarantees a comparable final-cover frame even when books scale differently.
- Strict Pyright over `src`, `tests`, and `examples` remains clean, and a non-persistent fake-device smoke verified timestamp filtering plus device-time parsing for `wait_for_log()`. No timing profile was changed: physical corpus evidence is still required before fitting content scaling.
- Made comparison MP4s attributable like the underlying hardware and emulator recordings. The helper now writes a deterministic JSON sidecar containing both input hashes, exact rounded frame ranges, perspective corners, FFmpeg version/arguments, output hash, and FFprobe dimensions/rate/count. It rejects an input that ends before the requested frame count; a negative 5 s smoke correctly rejected the 123-frame result instead of accepting it as the requested 150 frames.
- Regenerated the 3.3 s Home Loading review twice. The MP4 remains byte-identical at SHA-256 `03f8bb31b8e91fa321ba1f3060674b0669ac2b232c6fe589d51bbf168e1d6d22`, and its new deterministic provenance sidecar is SHA-256 `ce0fec22f1842c6727c9e4b2332afa6c259c02d266c11c9bae9c8cd28ace7b6c`.
- Re-read the successful physical Stormlight thumbnail boundary and preserved evidence the profile generator had discarded: `260,206` archive-compressed bytes, `290,426` decoded JPEG bytes, a `994x1498` source, and the `135x226` conversion target. The corpus parser records the same fields per book. Runtime still consumes only the measured `3,394` ms duration, so no byte/dimension multiplier was inferred from one point.
- Inventoried the ignored local corpus covers. WOT is `12,502` bytes / `553x700`, Sun Eater `256,811` / `935x1505`, Mistborn `305,810` / `1000x1500`, and LOTR `222,713` / `889x1371`; coupled with their spine counts, the next physical run can separate size/dimension effects from the current 400-spine guard. All eleven local EPUBs use JPEG covers, so PNG thumbnail calibration still needs a new physical fixture.
- The enriched 12-source profile regenerates byte-for-byte at SHA-256 `4678ad8ad2c8cb2a947c14fc2a440e6fa8ebb5912970bddbe535dd5092a87fcf`. Strict Pyright remains clean, both existing X3/X4 Python scenarios pass with the extra evidence fields, and `git diff --check` is clean.
- Audited the image timing scope against the physical log instead of trusting the pooled p50. `cachedRenderMs` measured only `Loading from cache` to completion, but `runtimeFinishImageRender()` started before file open; the generic `102.5` ms regular-file open therefore dominated physical small-cache totals of only `18-23` ms. Reparsed the same 50 samples from `Rendering image` through completion: small caches are `20.5` ms p50 (10 samples) and full-page caches `147` ms (40 samples).
- Preserved all four decode/cache samples with archive/image/cache bytes and source/output dimensions. Render-entry-to-success is `73/81` ms for the `1,073/1,256`-byte images, `1,079` ms for `66,507` bytes, and `1,871` ms for the `290,426`-byte cover. The old displayed-area split assigned the cover maximum to both full-page images and made the 66 KiB page `857` ms late.
- Applied the measured scopes without a fitted throughput curve. Derived `.jpg`/`.jpeg`/`.png`/`.pxc` paths no longer inherit generic SD open/transfer/close pacing; their enclosing workload target includes those operations. Cached renders select small/large by displayed area. Decode/cache selects explicit source-byte tiers at 16/128 KiB, covering the three observed clusters while leaving future corpus data free to replace them.
- Added `replay_image_pages.py` and ran six Stormlight pages twice. Cover, medium full-page, first small, and second small image pages are `5,713/4,922/2,111/2,111` ms versus physical `5,673/4,857/2,033/2,130` ms—absolute errors `40/65/78/19` ms. The previous medium result was `5,714` ms. Normalized logs and event traces are byte-identical across repeats (`bbe1a547…` / `b0cf4fc4…`), and the final natural PNG is byte-identical at SHA-256 `3113d3db509c954ae8667ab1fee8d91b6026818c0313d0be037bd8e24a477f24`.
- The regenerated profile is deterministic at SHA-256 `1d5ba3bf63c1e40605c38ba4a61d6a0f3c9ee20196eea9a9811c5d6e6e90dc29`. Strict Pyright, all 12 native tests, and both X3/X4 Python scenarios pass after rebuilding the runnable emulator. The ordinary production cache again reported the known phantom +8 bytes; a clean isolated build confirms the unchanged `5268359` bytes flash / `49532` bytes RAM.
- Continued the cold-open scope audit past the exact `23,201` ms indexing boundary. Hardware spends `1,098` ms from `Total indexing completed` through CSS parse/cache save and metadata reload; the emulator spent `3,545` ms because tiny ZIP/CSS operations inherited the bulk SD model. Added a large-spine post-index scope, suppressed generic open/transfer/close only inside it, and applied the existing physical boundary. The emulator now lands exactly at `1,098` ms; Reader-entry-to-EPUB-ready is `25,046` ms versus `25,148` ms physical.
- Preserved the earlier image-preparation evidence separately from decode/render. Physical `Found image` through decoded dimensions is `1,736` ms for the 290,426-byte cover, `443` ms for the 66,507-byte full-page image, and `158/161` ms for the 1,073/1,256-byte small images. The emulator was `4,050/1,165/191/193` ms because ZIP extraction still read the parent EPUB through generic storage pacing.
- Added an image-preparation scope around item-size lookup, ZIP extraction, cache-file sync, and dimension probing. It reuses the profile's explicit 16/128 KiB source tiers and does not alter production builds. The new emulator boundaries are `1,736/443/159.5` ms; complete section construction is exact for the cover (`1,743` ms), within `1` ms for the medium page (`451` vs `450`), and within `12-27` ms for the small pages.
- Final six-page runs `run-v5-prepare-scopes` and `run-v6-prepare-scopes-repeat` have byte-identical normalized logs (`7af4404a…`), events (`408532de…`), and final natural PNG (`3113d3db509c954ae8667ab1fee8d91b6026818c0313d0be037bd8e24a477f24`). Reader-entry through first painted cover is now `33,434` ms versus `33,216` ms physical, a `218` ms residual. The 12 native tests and X3/X4 Python scenarios pass after restoring the runnable emulator.
- The 12-source profile, now including `postIndexLoadMs` and four image-preparation samples, regenerates byte-for-byte at SHA-256 `e24b2084101fd721672ee1870945fac257d62f49b2a1ff1a99ea4832713fec7e`.
- Final gates after applying the post-index and image-preparation phases: strict Pyright, all 12 native tests, the restored native executable, and both X3/X4 Python scenarios pass. A clean isolated production build confirms the emulator-only hooks compile out exactly at `5268359` bytes flash / `49532` bytes RAM; `git diff --check` is clean.
- Completed the accepted physical four-book corpus run `x4-epub-corpus-v2` under firmware `dirty833702f8`. Its upright 171.867 s / 5,156-frame video has SHA-256 `c88fc1e5a0b1ddd3b7ec425bdb866dfcad5e22a09b1e162397251655b592be7b`; each book reaches a log-confirmed thumbnail and then holds settled Home for two seconds.
- Physical WOT/Sun Eater/Mistborn/LOTR indexing totals are `22,114/16,835/14,192/9,227` ms. Their TOC phases are `1,600/1,249/5,704/2,431` ms, so neither size nor spine/TOC count supports a monotonic phase curve. Kept the provisional cold-index model instead of fitting the five books.
- The four new JPEG thumbnail boundaries are `886/3,482/3,608/2,649` ms for `43,333/256,811/305,810/222,713` decoded bytes. Together with Stormlight's `290,426` bytes / `3,394` ms, a linear whole-workload model (`437,327 us + 10,573 ns * imageBytes`) has at most `9.459%` error. Emulator results are `895/3,152/3,670/2,793` ms, errors of `9/-330/+62/+144` ms.
- Generated four attributable thumbnail-transition reviews plus a settled contact sheet. After current-camera perspective correction, all covers, card bounds, title/author layout and truncation, menu rows, and soft keys agree. Hardware is consistently lighter and softer with irregular/noisy stipple; the emulator is crisp, high-contrast, and mechanically halftoned. The contact sheet SHA-256 is `b9db20c156abb36567a7545569e3d9e7a0888ace9c553cb717e2a57d8c77e159`; WOT/Sun Eater/Mistborn/LOTR comparison MP4 hashes are `8fda76b8…/9c5c70f4…/740b616c…/a9f43072…`.
- Added supplemental workload inputs to profile generation without weakening the same-firmware base set. The corpus and later calibration-command builds retain their exact fingerprints, and ordinary production workload inputs must still share one firmware label. Corpus production page turns preserve marker-to-ready and image evidence even when they do not emit the special tiled-page benchmark record.
- Flashed calibration firmware `dirty6da1149a` and measured isolated Stormlight cache deletion in File Browser. `Epub::clearCache()` takes `747,440 us`; the complete 30 FPS video SHA-256 is `2b76ae6e4330dbf1f48b6312d11748a5252a830dddd50ffbe90f6f518ffb901c`. It remains profile evidence until emulator automation can invoke the same production path.
- Two sleep diagnostics exposed that a host-held calibration Power bit prevents production deep sleep: `startDeepSleep()` correctly waits for release. Added a calibration-only timed button pulse whose deadline is enforced inside GPIO sampling even while the main loop renders, plus terminal capture that prefers a real detach and falls back to an explicit terminal log only when USB remains present.
- Accepted `x4-sleep-entry-v3` uses an 800 ms Power pulse and physically detaches USB. Device receipt to Sleep activity is `562` ms, to deep-sleep log `5,371` ms, and Sleep-entry to deep sleep `4,809` ms; fast/full controller waits are `506/3,741` ms. Its upright 60 FPS video SHA-256 is `7c1d77f083acbe2c8e455100fa67827142f32794bdb732ba6c35dabe1fd47451`.
- Applied the Sleep-entry boundary around the production activity and HAL deep-sleep edge. The emulator deep-sleep event is `5,426` ms after Power, within `55` ms of physical. The accepted 6.8 s comparison MP4 is `artifacts/comparison/x4-sleep-reset-runtime/review-v3/sleep-entry-comparison.mp4` (SHA-256 `716fbf40e70aba06429217507599ddcc87878d945a61f7bb07fa1662686a4bf9`); physical and emulator logo/text geometry agree, while the real dark screen remains gray/noisy and the digital one pure black/crisp.
- The 15-source X4 profile regenerates byte-for-byte at SHA-256 `95a71a7d0adaf72323a143ff5b24eebac51754820b4f1c8e040d7c3684ec1d24`. Strict Pyright is clean, both existing Python scenarios pass, all 12 native tests pass, and `git diff --check` is clean. The runnable native emulator was rebuilt after the test target. The ordinary production cache again reports the known phantom +8 bytes; a fresh `/tmp` build confirms unchanged production output at `5268359` bytes flash / `49532` bytes RAM.
- Final profiled sleep runs `run-v4-physical-profile` and `run-v5-physical-profile-repeat` have byte-identical normalized events (`c6080e54…`), logs (`d70a0e66…`), panel/screens, and MP4s. Sleep-entry remains SHA-256 `ce2b223c47bf94ed88d8212ef57dcbe4aa7af9b87983bf34ae4f571c7e41fc25`; reset-to-Home remains `7fe15608a72370370570840f4d5bddd0895c5c8b18fd5edf41fff46232fba666`.
- The physical WOT page exposed a distinct image class: a 12,502-byte source expanded to `464x587` and took `650` ms from decode entry to success. The runtime now selects that measured small-source/full-page target without slowing the `73/81` ms small-icon samples.
- Parsed cold section boundaries independently of image evidence. Twelve physical sections span `261-416` ms from `ERS Loading file` through newly streamed HTML (`327` ms p50, `14.5` ms MAD); five image sections span `101-234` ms from streamed HTML to `Found image` (`124` ms p50). Both scopes suppress generic storage only while active, and finalized/reused HTML cancels them.
- WOT `run-v8-section-stream` and `wot-v9-section-stream-repeat` now reach streamed HTML in exactly `327` ms and first display in `1,434` ms from section load versus `1,425` ms physically. That is a `9` ms residual, down from `933` ms before the two section scopes; the repeat settled PNG is byte-identical (SHA-256 `9e598896c10bb869414cb83bb989b5025d28033381a7660db35b75986b1ce65c`) and logs differ only by artifact paths.
- Split section-stream timestamp capture from storage suppression after review found that a successful section-cache read would otherwise be suppressed before the cold/reused decision. The final WOT run remains byte/timing-identical, while a same-session reopen deserializes its finalized section in `103` ms and performs the profiled `102.5` ms ordinary read-open delay instead of inheriting the cold-stream scope.
- The 15-source profile regenerates byte-for-byte at SHA-256 `e0906fa40a71fe848094503693773590f9b008ca2ab2cbe5c5dd073ac339a42a`. Strict Pyright is clean, both Python scenarios pass, all 12 native tests pass, the runnable emulator was rebuilt after testing, and `git diff --check` is clean. The normal production build succeeds and reports the known stale-cache phantom `5268367` bytes flash / `49532` bytes RAM; the last isolated clean baseline remains `5268359` / `49532`.
- Consumed the isolated physical cache-clear boundary through explicit automation. `Emulator.clear_epub_cache()` is accepted only from a settled File Browser, runs `Epub::clearCache()` on a one-shot scheduler task, and applies `747,440 us` only when the production cache exists; absent caches are zero-duration no-ops and unsafe paths/out-of-context calls are rejected.
- `replay_cache_clear.py` runs Stormlight cold, returns through Home to File Browser, clears it, and opens cold again. Runs `x4-cache-clear-runtime/run-v1` and `run-v2-repeat` both report exactly `747,440 us`, repeat the `23,201` ms indexing phase, and have byte-identical normalized logs (`bcb7d2be…`), events (`33430588…`), and final PNGs (`5bc0e13e076211dc78f2b800f1ebe2168bcc3333ef7e0b494130526616610cce`). A negative smoke verifies activity, missing-cache, and path-confinement behavior.
- Applied the four-book corpus indexing data without fitting the rejected monotonic curve. The profile generator emits exact device-path records for Stormlight, WOT, Sun Eater, Mistborn, and LOTR; the runtime selects them before phase pacing, including measured small books, while renamed/unmeasured books retain the old threshold fallback.
- Two runs of each exact corpus workload reproduce physical OPF/TOC/book-bin/total/post-index phases exactly: WOT `4,804/1,600/15,708/22,114/862` ms, Sun Eater `4,480/1,249/11,104/16,835/415`, Mistborn `3,091/5,704/5,396/14,192/496`, and LOTR `2,042/2,431/4,753/9,227/450`. Every pair has byte-identical normalized logs/events and settled PNGs; renaming WOT to `/renamed.epub` correctly selects the existing `23,201` ms large-book fallback rather than the exact WOT record.
- Replaced the special-case WOT small-source/full-page decode field with two exact evidence tables. The profile now retains ten image-preparation samples keyed by source bytes and ten decode/cache samples keyed by source bytes plus output dimensions; unmatched images still use the three coarse source-byte tiers. This removes one bespoke runtime branch while consuming the half-page and corpus measurements the tier model discarded.
- Corpus markers cut through cover construction, so per-page parsing had retained decode but lost `Found image` through dimensions for Sun Eater, Mistborn, and LOTR, and ended before Mistborn decode completion. Parsing the whole supplemental run under a distinct `corpusImages` key recovers exact physical preparation/decode spans: WOT `192/650` ms, Sun Eater `1,532/1,882`, Mistborn `1,845/1,978`, and LOTR `1,333/1,606`. Final emulator replays hit every one exactly.
- Applying WOT's exact `192/650` ms preparation/decode phases replaces the earlier median preparation offset: section load through first display is now `1,467` ms versus `1,425` ms physically (`42` ms residual). This supersedes the earlier `1,434` ms end-to-end observation while making both attributable image phases exact.
- Final exact-image repeats for Stormlight, WOT, Sun Eater, Mistborn, and LOTR have byte-identical normalized logs, event traces, and settled PNGs. Representative normalized event hashes are `48c10a7a…/900cefc…/7382d921…/25cbbba8…/95abfee0…`; settled PNGs remain `3113d3db…/9e598896…/fda76ff4…/87e75f92…/7790f88e…`.
- The 15-source profile, including five exact indexing records, ten exact image-preparation records, and ten exact decode/cache records, regenerates byte-for-byte at SHA-256 `956c0f3a3779af3c656e3efa0956678fb168d66b4505fe6d0e9ad1b5644e3824`. Strict Pyright, both Python scenarios, all 12 native tests, the restored runnable emulator, and `git diff --check` pass. The production build succeeds with the known stale-cache `5268367` bytes flash / `49532` bytes RAM report; the clean baseline remains `5268359` / `49532`.
- Repeated the profile-consumption audit after adding the exact tables. Native configuration requires and runtime code references every canonical p50 boundary: panel BUSY/operation, fast/half render phases, ordinary and directory storage, controlled and reader optical schedules, cached/decode/preparation image timings, section streaming/discovery, cold/exact indexing and post-index load, quiescent warm open, JPEG thumbnails, cache clear, and sleep entry. The exact indexing and image records are selected before their fallbacks. No additional delay was justified.
- Kept evidence-only measurements out of runtime pacing: p90/MAD are future tolerances; final camera luma and dominant peaks are optical review evidence; pooled/aggregate phases would duplicate selected subphases; 4/64 KiB SD results validate the 512 KiB linear model; mixed cold/XL pages are content scenarios; and the corpus `16-20` ms Home metadata samples overlap thumbnail work rather than representing quiescent warm reopen. The plan now says this explicitly and no longer implies that the deferred X3 capture is complete.
- Rechecked hardware outside the sandbox. `/dev/video0` is present and a frame captured under the newly granted `video` group confirms the X4 remains naturally framed but asleep on the retained CrossPoint screen. `/dev/ttyACM0` is absent, so there is no safe CLI action that can wake or keep the firmware active and the static frame is not promoted to timing evidence.
- Prepared the next corpus capture to close the warm-load gap without another ad hoc script. After each log-confirmed settled thumbnail, `calibrate_epub_corpus.py` now performs three warm reopens, returning to Home for five seconds between samples. The analyzer keeps per-book open-to-ready, cached-metadata, and metadata-start-to-page-load distributions under `corpus.<book>.warm`, separate from the thumbnail-overlapped Home metadata field. This changes no current profile until a new physical corpus run is accepted.
- Verified the pending warm-corpus path without adding a committed test: a synthetic three-sample parser smoke produces the expected `220/25/105` ms medians, direct strict Pyright reports zero errors or warnings, both edited modules compile, and `git diff --check` is clean. Regenerating from the 15 accepted physical sources remains byte-for-byte identical at SHA-256 `956c0f3a3779af3c656e3efa0956678fb168d66b4505fe6d0e9ad1b5644e3824`.
- Prepared the missing PNG-thumbnail workload as `books-for-testing/png-cover-calibration.epub`, which remains locally ignored. The minimal one-spine EPUB uses a real `963x1445` RGB PNG (`1,210,881` decoded bytes) and has SHA-256 `006424601cb1dc5f461837bb77e6cbd8f4331b4648460e78ce99b6d8c5371e2d`. ZIP integrity and stored-first `mimetype` pass, and an X4 emulator cold-open reaches the production PNG converter and generates the Home thumbnail without errors. Its naturally rotated review PNG is SHA-256 `ffb3dfe443b123e0b922ce880898fc10806ad1c01faf30e4f99aacbada96057a`.
- Added `calibrate_png_thumbnail.py` so the next hardware session can clear/open that fixture, wait on the exact successful PNG-thumbnail log, and hold the settled Home card on video. The current emulator generic-storage baseline is `18,329` ms from PNG generation start to success; it is deliberately not promoted to a timing model before physical measurement.
- Final offline checks after both capture preparations: strict Pyright over source, tests, and examples reports zero errors or warnings; the edited scripts and analyzer compile; ZIP validation, local-ignore checks, and `git diff --check` pass; and the accepted 15-source profile remains byte-identical at SHA-256 `956c0f3a3779af3c656e3efa0956678fb168d66b4505fe6d0e9ad1b5644e3824`.
- Resumed physical work after Linux regained `/dev/ttyACM0`; sent a reversible Down/Up pair immediately and started `x4-epub-corpus-v3-warm` with both `dialout` and `video` groups. The first WOT warm reopen completed, but the script pressed Back while its cached image page was still rendering for `3,636` ms. The second reopen then stopped after cached section restore and never processed the already-sent `wot-warm-ready-01` marker. After several minutes without serial progress, the recorder was stopped and preserved as a failed 416.167 s / 12,485-frame run rather than accepted measurement evidence.
- The failed-run manifest explicitly records `status: failed` and `KeyboardInterrupt`. Its MP4/serial/events/manifest SHA-256 values are `60d48756…/dc8f5db7…/54514f9f…/d61c42b2…`. Repeated post-run `CAL:HELLO` commands received no reply, proving a firmware hard hang rather than sleep or a host-camera stall.
- Recovered the X4 without changing flash or SD by using esptool's ESP32-C3 USB reset-and-run path, then immediately sent another reversible keepalive pair. Corrected `calibrate_epub_corpus.py` to wait for the production `Progress saved` log on every warm sample before leaving Reader or starting the next book. The metadata/page-load markers remain unchanged, strict Pyright and module compilation pass, and `x4-epub-corpus-v4-warm-safe` is now recording under the corrected cadence.
- `x4-epub-corpus-v4-warm-safe` also exposed a real cumulative lifecycle failure rather than producing acceptable corpus evidence. All three WOT warm reopens completed, then the first Sun Eater warm reopen stopped after metadata and section-cache restore; the queued `sun-eater-warm-ready-00` marker was never processed for more than four minutes. The failed manifest records `KeyboardInterrupt`; MP4/serial/events/manifest SHA-256 values are `bdad1117…/11379b21…/378bdfa3…/0777183e…`. Reset-and-run plus a Down/Up pair recovered the device again.
- Replaced the cross-book capture with an isolated three-reopen workload and an ESP32-C3 reset plus immediate keepalive between books. `calibrate_warm_epub_repeats.py` waits for production `Progress saved` before leaving Reader, while `calibration_profile.py` now parses the same exact phase boundaries from either a corpus run or one isolated book. A synthetic parser smoke, strict Pyright, module compilation, and `git diff --check` pass.
- Accepted isolated Sun Eater `x4-sun-eater-warm-v1`: cached metadata `755` ms p50 (`754-756`, three samples), metadata-start-to-page-load `1,973` ms p50 (`1,969-1,999`), and open-to-ready `2,252` ms p50 (`2,247-2,283`). MP4/serial/events/manifest SHA-256 values are `6af1da1c…/ef58e3a9…/405c8c42…/3a5f6a0e…`.
- Accepted isolated Mistborn `x4-mistborn-warm-v1`: cached metadata `586` ms p50 (`585-586`, three samples) and metadata-start-to-page-load `1,731` ms p50 (`1,729-1,759`). Open-to-ready is `2,001` ms p50, but one of three samples is a real `10,159` ms outlier; retain its `8,527.4` ms p90 as tolerance evidence and do not pace nominal runtime to it. MP4/serial/events/manifest SHA-256 values are `a87c83cd…/ef735f81…/2c38e8b6…/22df6232…`.
- Accepted isolated LOTR `x4-lotr-warm-v1`: cached metadata `595` ms p50 (`594-596`, three samples), metadata-start-to-page-load `1,749` ms p50 (`1,737-1,767`), and open-to-ready `2,019` ms p50 (`2,007-2,041`). MP4/serial/events/manifest SHA-256 values are `2f20f6fb…/5ef7c2d1…/8be1c515…/21c59fc3…`.
- Accepted isolated WOT `x4-wot-warm-v1`: all three open-to-ready samples are present at `3,441` ms p50 (`3,435-3,451`). The first reopen reused already-resident metadata and therefore did not emit the `Loading ePub`/book-cache boundary; the two attributable cached-load samples are stable at `2,032/2,033` ms and the two metadata-start-to-page-load samples are `3,178/3,188` ms. MP4/serial/events/manifest SHA-256 values are `91e702d7…/91c589db…/3ca2670b…/be741b4d…`.
- Accepted Stormlight control `x4-stormlight-warm-v2`, with one important qualification: the earlier corpus clear had removed its cache, so sample zero performed a real `160,631` ms cold rebuild. The two subsequent warm samples give the nominal medians selected across the three-run distributions: cached metadata `597` ms, metadata-start-to-page-load `1,774` ms, and open-to-ready `2,021` ms. The cold outlier remains in p90/evidence and must not be treated as warm pacing. MP4/serial/events/manifest SHA-256 values are `7fe7dedd…/9669a320…/be551de9…/e00f8688…`.
- Each isolated recorder run used the naturally upright `--rotate 90` camera path. Between captures the device was reset without flashing or modifying SD data and received a reversible Down/Up pair; another pair was sent after the final capture while the accepted evidence was analyzed. No PNG physical timing is claimed because `png-cover-calibration.epub` has not yet been copied to the SD root.
- Added `models.exactWarmByPath` with one record per measured device path. Each record retains the full physical distribution but runtime consumes only p50 for cached metadata and metadata-start-to-page-load. `Epub::load()` now passes its path to the emulator boundary; exact records are selected before the existing >=400-spine fallback, and `runtimeFinishWarmOpen()` uses the target chosen for that load. Open-to-ready is deliberately not another runtime delay because it includes the two modeled phases plus host/input scheduling.
- The first generator attempt correctly rejected Stormlight's incidental `160,631` ms cold rebuild as conflicting exact-index evidence. A new explicit `--warm-workload-run` input class now includes completed warm artifacts in profile sources and `warmWorkloads`, but prevents their incidental cold/image measurements from contaminating canonical workload models. A second launch check caught and removed the same contamination from runtime workload selection.
- The deterministic 20-source profile now contains five exact warm records and has SHA-256 `b7e4ca79971547179fc27b492466220646a3c98b79c886cd9e57254717d578ed`. Exact cached-metadata targets are Stormlight/WOT/Sun Eater/Mistborn/LOTR `597/2,032.5/755/586/595` ms; metadata-start-to-page-load targets are `1,774/3,183/1,973/1,731/1,749` ms.
- Added `replay_warm_epub.py` and replayed immutable cached fixtures twice per path. Both runs report the same whole-millisecond boundaries: Stormlight `597/1,775`, WOT `2,033/3,184`, Sun Eater `755/1,974`, Mistborn `586/1,732`, and LOTR `595/1,750` ms. The first phase is exact; the second is the configured target plus the expected `1` ms firmware-log quantization.
- After normalizing only the requested screenshot's absolute artifact path, repeat event hashes are byte-identical: Stormlight `ce5a4cc8…`, WOT `0374da56…`, Sun Eater `096501f5…`, Mistborn `1b4e7dc1…`, and LOTR `021cd40f…`. Settled warm-page PNGs are also byte-identical per pair (`167d466f…/e0ae3599…/56feb520…/14578e47…/e89fbbad…`).
- A renamed cached WOT control selects the existing fallback rather than the exact `/wot.epub` record: raw cached metadata is `677` ms (already above the `558` ms minimum) and metadata-start-to-page-load is `1,587` ms against the `1,586` ms fallback. Its settled raster matches WOT at SHA-256 `e0ae3599feef6074c40dacdbb80d644b37052ed1b2fb2889321984f3cd8354e9`.
- The native emulator builds successfully with the exact warm table. Reversible Down/Up keepalive pairs continued during profile generation and replay; the attached X4 remained responsive.
- Final exact-warm gates pass: strict Pyright over source/tests/examples reports zero errors and warnings; the parser smoke and edited-module compilation pass; both X3/X4 Python EPUB/golden/video scenarios pass; all 12 native tests pass; and the runnable emulator was rebuilt after the test target. Profile regeneration is byte-for-byte identical at SHA-256 `b7e4ca79971547179fc27b492466220646a3c98b79c886cd9e57254717d578ed`; `git diff --check` is clean.
- The ordinary production cache again reports the known phantom `5,268,367` bytes flash / `49,532` bytes RAM. A clean isolated `/tmp` build confirms unchanged production output at `5,268,359` / `49,532`, so the exact warm implementation remains emulator-only.
- Re-read `GUIDANCE.md` after logging and verification; the monitor has added no correction or blocker, so work continues. The remaining physical calibration gate is the PNG thumbnail fixture on the SD root; X3 and population/temperature profiles remain explicitly deferred.
- Audited the repeated cached-cover renders in the five accepted warm runs before adding another table. Physical p50s are WOT/Sun Eater/Mistborn/LOTR/Stormlight `125/162/148/151/150` ms; the existing full-page fallback is `147` ms, so residuals are only `+22/-15/-1/-4/-3` ms. Cached runtime hooks know output dimensions but not source path/bytes, and Sun Eater and Stormlight share the same `464x699` cache geometry despite different medians. Kept the single 147 ms bucket: an exact-dimension table would either conflict or pool away the distinction while saving at most 22 ms.
- Sent another reversible Down/Up keepalive after that audit; no new physical command or timing constant was needed.
- Invalidated the first exact-warm profile as nominal-speed evidence. Every `x4-*-warm-v1/v2` serial log enters Home low-power mode before `CAL:OPEN`; the direct calibration command bypassed the physical input path that calls `powerManager.setPowerSaving(false)`, so cached metadata and page-load phases ran at the reduced Home-idle CPU frequency. The `b7e4ca79971547179fc27b492466220646a3c98b79c886cd9e57254717d578ed` profile and its emulator replays remain historical evidence but are superseded, not production targets.
- Corrected `calibrate_warm_epub_repeats.py` to send a physical Back pulse before every marked open. Back is a no-op on Home, but it exercises the real GPIO path and restores normal CPU frequency before `CAL:OPEN`; each serial trace must now show `Restoring normal CPU frequency` before the marked load. Strict Pyright, module compilation, and `git diff --check` pass for the correction.
- Accepted corrected WOT run `x4-wot-warm-normal-v2`. All three samples show the normal-frequency restore before open and report cached metadata at exactly `160` ms (p50/MAD/p90 `160/0/160`), metadata-start-to-page-load at `306` ms p50 (`305-307`, MAD `1`, p90 `306.8`), and open-to-ready at `339` ms p50 (`339-345`, MAD `0`, p90 `343.8`). Cached image rendering remains `125` ms p50, as expected because the render task already held a normal-speed power lock. MP4/serial/events/manifest SHA-256 values are `71e33a92b499f2b57dbd884b5d2d5aebb4c0c09a7242dec53da09c95386aa132` / `97b9e5d2f2bab0ca3015b35d9ac9ced5255fed868c3236699c6307bbe9d39737` / `f7a7b3171ba4c8888ac8c1eb1a42b6f349d9c5bcbb1cf0c7c2b3891f499ce90e` / `c44d8d480b04e07db9db081430c3a8f667e98d7ce24ea122bb6f45c6b6ca93fb` under scenario SHA-256 `5600aac3ee9d1b16769fec6af6eba63a1ccc2e6ebe907132d71af16b2df5798e` and firmware `1.4.1-dev-emulator-f42919ca-calibration-dirty6b9cdc1f`.
- Rejected `x4-sun-eater-warm-normal-v2` after serial review. The second open's Back pulse restored normal speed at `68,346` ms, but `CAL:OPEN` was not serviced until `71,471` ms and Home re-entered low-power mode at `71,373` ms; its `755/1,971/4,979` ms phases therefore mixed the same low-frequency behavior into an otherwise normal run. The failure showed that an injected wake immediately before a direct command is insufficient when command service itself is delayed.
- Made activity-changing calibration commands faithful to physical input: `CAL:OPEN`, `CAL:HOME:SETTINGS`, and `CAL:HOME:FILES` now flag activity, and the ordinary main-loop activity block resets `lastActivityTime` and restores normal CPU frequency before processing the pending transition. The calibration environment builds at `5,275,287` bytes flash / `49,564` bytes RAM. A first verbose upload and its first quiet retry were interrupted before the full app image reached flash; a byte comparison exposed the truncated tail. The final quiet upload was allowed to exit cleanly, and `/tmp/x4-recover-v20/serial.log` verifies `CAL:HELLO` from `1.4.1-dev-emulator-f42919ca-calibration-dirty53758060` plus a responsive Down/Up keepalive.
- Accepted corrected Sun Eater run `x4-sun-eater-warm-normal-v3`. All three normal-speed samples report cached metadata `72` ms p50 (`71-72`, MAD `0`, p90 `72`), metadata-start-to-page-load `225` ms p50 (`224-226`, MAD `1`, p90 `225.8`), and open-to-ready `352` ms p50 (`349-355`, MAD `3`, p90 `354.4`). The CLI open explicitly restores normal speed on samples that start after Home's idle transition; sample zero began at normal speed already. MP4/serial/events/manifest SHA-256 values are `eaf2d842e9e7209f1c7c57ce77d8a2dcd289e61f188a39fbac73c6f7dc585309` / `85bd1951554299497656280a6fb971d1316d8082e27035eccb73ead2bb8d7cc7` / `d5d6702f59c5542c72e30b909c9cd542b9f4a9c0899d9d5033c032eb41c7c87b` / `a42a908f4201ffe37e53cd89e951f93aaae085155ee52e7614cff628f29f45fd` under scenario SHA-256 `b4dd2223c03ba1ec592010ad2a0d71cec480d6d916c3b046e6630cc90a73c96c`.
- Accepted corrected Mistborn run `x4-mistborn-warm-normal-v2`. Its three explicit activity restores produce cached metadata `56` ms p50 (`55-56`, MAD `0`, p90 `56`), metadata-start-to-page-load `201` ms p50 (`200-202`, MAD `1`, p90 `201.8`), and open-to-ready `314` ms p50 (`313-314`, MAD `0`, p90 `314`). MP4/serial/events/manifest SHA-256 values are `b98e6b14633e7228e712f159d09814a4ebb72b5667718219d10b8d41950c5827` / `88cd57b5e48cbe7503c2c2bcbd5e3c817a77982b32f0abbb3e366d4b5327fc0c` / `fa251b67f297fa8bc7d0809c1080b0040bdc550fe71de850901d846b5d21da81` / `9d2e7eea1d8470a99933dfff6e23e4242bd29f2b709da15f21c273659808d48d` under firmware `1.4.1-dev-emulator-f42919ca-calibration-dirty53758060` and scenario SHA-256 `b4dd2223c03ba1ec592010ad2a0d71cec480d6d916c3b046e6630cc90a73c96c`.
- Accepted corrected LOTR run `x4-lotr-warm-normal-v2`. Its three activity-restored samples report cached metadata at exactly `56` ms, metadata-start-to-page-load `202` ms p50 (`200-202`, MAD `0`, p90 `202`), and open-to-ready `335` ms p50 (`332-336`, MAD `1`, p90 `335.8`). MP4/serial/events/manifest SHA-256 values are `00e91019de6cb406009e8d2637aa327f832e784847ef5849be258e267303351b` / `d4849bc01d45ad3181e5bdc994550f5ff1b9464305640450ea3dffaf9612f9df` / `7980449c41260bc47a8a0d776da558d2057fa6bcc73c474d3fa439e95f2ff8f4` / `f7aa6621cf2e91d27b90e5a40ebd31cf29bfa3f37401c2bb7da241c810c663ba` under the same corrected firmware and scenario.
- Accepted corrected Stormlight control `x4-stormlight-warm-normal-v3`. Unlike the earlier control, all three samples are warm and normal-speed: cached metadata is exactly `58` ms, metadata-start-to-page-load is `206` ms p50 (`205-207`, MAD `1`, p90 `206.8`), and open-to-ready is `322` ms p50 (`318-328`, MAD `4`, p90 `326.8`). MP4/serial/events/manifest SHA-256 values are `6de0f0445f233ff46c3c491d5b47a0f7068e5e275843774f28dab79ad7e9d87c` / `dcbfbb1507dc071dc082dafd54fda3db28d2ce4eb18173cc20330d1b8ecca306` / `564ec0729cc02eb7729467cf23bed20142a7546402d3b796b1a3f4c9d7c24305` / `af67d4101549de76cf1cf6e46415b203df0d890dca5d30f494bd45d170ddc482` under the same corrected firmware and scenario.
- Repeated WOT as `x4-wot-warm-normal-v3` so every final warm-profile source uses the same corrected firmware and scenario. Cached metadata is `160` ms p50 (`159-160`, MAD `0`, p90 `160`), metadata-start-to-page-load is `306` ms p50 (`305-306`, MAD `0`, p90 `306`), and open-to-ready is `427` ms p50 (`426-432`, MAD `1`, p90 `431`). The first two modeled boundaries reproduce `normal-v2`; the higher open-to-ready span is unmodeled host command round-trip evidence and does not alter runtime pacing. MP4/serial/events/manifest SHA-256 values are `79eeef26b8031f0af82aff4df03522e5ac32048e19124207e36c3b3357452603` / `285d9663d99cba072837af13fced800f849ef9f053fecfb26b5d68cfc5e4d36c` / `a6bd384b209f242da1c1f5ac2e9ec8265a42dfcd7fc415d47b8d9e110f6e0f61` / `cb93b6c7fde752094b1405ebc142b854ba194e6f810d1e2557980499fc3f6940`.
- Regenerated the 20-source X4 profile exclusively from the corrected warm runs. It is byte-deterministic at SHA-256 `258a2236ccb9b2aa571a43614d82dbd02c86633581fed7c5a4aefebe8be811d8`; `models.exactWarmByPath` contains Stormlight/WOT/Sun Eater/Mistborn/LOTR cached-metadata targets `58/160/72/56/56` ms and metadata-to-page targets `206/306/225/201/202` ms. README and the plan now name the accepted artifacts and explain the low-power invalidation.
- The first corrected emulator replay showed why the new profile could not be reached by padding alone: generic bulk-SD pacing made cached metadata take `268/677/292/279/280` ms and metadata-to-page take `585/1,392/609/595/597` ms. Added one exact-warm whole-workload scope from metadata-cache load through the first page-load boundary; it suppresses duplicate generic open/transfer/close pacing only for measured exact paths and cancels immediately on a cache miss. Renamed/unmeasured paths retain the generic model and fallback.
- Final paired replays `*-normal-v3-profiled` and `*-normal-v4-profiled-repeat` reproduce cached metadata exactly and the page-load log boundary within its expected `1` ms quantization: Stormlight `58/207`, WOT `160/307`, Sun Eater `72/226`, Mistborn `56/202`, and LOTR `56/203` ms in both runs. Normalized event hashes are byte-identical per pair (`97d15769…/5e886411…/5c65857a…/5bb07c69…/c55747a2…`), as are settled PNGs (`167d466f…/e0ae3599…/56feb520…/14578e47…/e89fbbad…`). A fresh renamed-WOT control remains on the fallback at `677/1,587` ms and preserves WOT's settled raster.
- Followed the 11:20 `GUIDANCE.md` audit through every command-driven source. Stormlight cold indexing restores normal CPU frequency before cache clear/open and stays normal through indexing; every corpus book restores before its cold path; and the isolated cache-clear run restores before deletion. Those workload sources are valid. The old SD, directory, controlled panel, controlled optical, and grayscale captures had no such restore marker and were idle-clock contaminated, so they were removed from nominal profile inputs.
- Made `beginCalibrationActivity()` restore normal CPU frequency immediately before synchronous cache/SD/directory/panel/grayscale work, while retaining the ordinary inactivity reset for asynchronous activity transitions. Flashed and verified `1.4.1-dev-emulator-f42919ca-calibration-dirty37a6d99e`; subsequent accepted synchronous captures all show the restore before the benchmark boundary.
- Accepted corrected SD runs `x4-sd-small-normal-v2` and `x4-sd-512k-normal-v3`. At 512 KiB, read open/I/O/close p50 is `13,062/704,309/4 us` and write is `10,947/825,395/5 us`, replacing the invalid roughly `102.5 ms` open and `7.8-8.0 s` transfer model. At 4/64 KiB, read I/O is `5,664.5/89,045 us`; write I/O is `12,545/108,356.5 us`. The 512 KiB MP4/serial/events/manifest hashes are `e5fbca4c…/65d42bfb…/e771d924…/1148e3db…`; the small-run hashes are `c32c62cc…/4fe0cad8…/fb0ae1da…/8772c91a…`.
- Accepted corrected directory run `x4-directory-benchmark-normal-v3`: root open `12 us`, enumeration `4,649 us` over 15 entries, `291 us` per `openNextFile`, and `4,891 us` total p50. MP4/serial/events/manifest hashes are `81931843…/6e103bd1…/8ec94925…/10e768ee…`. The old `198/2,778 us` root/entry values were idle-clock artifacts.
- Accepted corrected controller run `x4-panel-modes-normal-v2`: fast/half/full operations are `743,848/2,039,848/4,060,843 us` p50 with controller BUSY `506/1,723/3,744 ms`. MP4/serial/events/manifest hashes are `a9784dfb…/e3820c70…/6f27e70b…/d4de9a2b…`.
- Accepted corrected optical run `x4-panel-optical-normal-v5`. Fast black/white onset is about `317 ms` and settles by `384/417 ms`; half onset is about `1,082 ms` and settles by `1,182/1,215 ms`; full target-dependent changes span the measured charge pulses at a `266.7 ms` median interval. Capture/serial/events/manifest/analysis hashes are `acf6401c…/fb45d795…/4a2243e4…/da7c6f0b…/2514e9e0…`.
- Accepted corrected grayscale run `x4-grayscale-optical-normal-v4`. Fast-after-primary BUSY is `63,513 us`; light/dark visible change starts at `78.806/74.136 ms` and ends at `445.436/450.136 ms` p50. Half-after-primary BUSY is `145,514 us`, but all ten controlled operations remain below the `3.0` mean-luma-change threshold; lowering it would model camera noise. Firmware already documents that half refresh sets particles too firmly for this LUT. The analysis now records `visibleChangeDetected: false`, and the native panel retains its half-settled pixels through grayscale BUSY. MP4/serial/events/manifest/analysis hashes are `3fe3b196…/26fb8c3a…/c4829d02…/ca9cfc16…/f5963a21…`.
- Kept the base-profile same-firmware gate and recaptured the remaining core inputs on `dirty37a6d99e`: `x4-stormlight-cold-normal-v3`, `x4-stormlight-xl-font-normal-v5`, `x4-stormlight-warm-quiescent-normal-v2`, and `x4-page-turns-30-normal-v2`. Their scenario hashes match the earlier scripts, all videos are naturally rotated, and MP4 hashes are `cfe7c28a…/8e5d8e8a…/6a2fcf2b…/6c88aa95…`. The current Stormlight index is `23,082 ms` (`5,118` OPF, `2,060` TOC, `15,904` book-bin) plus `1,108 ms` post-index load.
- The corrected SD data justifies one small write refinement: a `6,145 us` first-transfer setup plus linear 512 KiB payload predicts 4/64/512 KiB write totals within `0.23%`; reads stay on the simple linear model within `1%`. The setup is charged once on the first non-empty write after open and never infers final file size. Exact warm scopes remain necessary: Stormlight's 87,835-byte `book.bin` alone exceeds its measured 58 ms metadata boundary at the generic 0.71 MiB/s rate.
- Corrected directory timing exposed CPU work that the old slow-directory artifact had masked. Five physical File Browser entries are exactly `28 ms` from activity entry to display start; raw emulation became `3-4 ms`. Added `x4-library-navigation-v3` as an attributable supplemental source and a whole-activity remainder boundary. `run-v10-corrected-profile` reaches display at exactly `28 ms` on all three replays while retaining the corrected `12/291 us` storage costs; settled PNG SHA-256 is unchanged at `14b74d25…`.
- Invalidated the single cache-clear runtime constant. Stormlight clears measure `101`, `216`, and `747 ms` with different derived-file populations, and the loader was selecting one by workload key order. `clear_epub_cache()` still schedules the production mutation and makes the next open cold, but these durations are evidence-only until captures record cache entry counts.
- The current 21-source profile is SHA-256 `36a3689704ba4a6a28d77fe52d2fb75f9808e04093f1720d72700d657686abe1`. A focused half replay emits `inverted-target`, the measured target settle, then a same-raster `no-visible-change` grayscale boundary; both page renders remain exactly `2,622 ms`. Its natural PNG/MP4 hashes are `23a923ac…/81654006…`. Corrected Stormlight and WOT warm replays retain `58/160 ms` metadata boundaries and the prior settled PNGs; the latest Stormlight PNG remains `167d466f…`.
- After USB reattachment, `/tmp/x4-recover-v33` completed a reversible Down/Up keepalive against `1.4.1-dev-emulator-f42919ca-calibration-dirty37a6d99e`; its serial/events hashes are `956b6327…/f164d00f…`, and the physical input restored normal CPU frequency before rendering.
- Reviewed the deterministic X4 golden failure visually. Both new controller-native captures are byte-identical at SHA-256 `90fb1a226c4df678c08bf973ca4d0eb86a2b6b84933109aea77c7b4b983c56e7`; unlike the previous expected raster's black half-screen, they retain the half-refresh page through the explicitly undetected grayscale pass. Updated only the existing X4 golden to that physically supported result; X3 was not changed in this step.
- Closed a controller/optical-time inconsistency exposed by the comparison review. Firmware tasks still unblock at controller BUSY, but visible panel snapshots now apply only optical frames whose timestamps have elapsed, `emulator.state`/wait results expose `panelOpticalBusy`, and `wait_for_panel_idle()` advances through the last scheduled optical frame. A warm Stormlight cover replay reaches grayscale BUSY completion at `10,801,361 us`, camera-measured settle at `11,187,497 us`, and captures at `11,191,000 us`; its final natural PNG remains SHA-256 `167d466f5e5ea928602b402dbc79f1d208d7bb9c638c39dc156f859447018b` instead of borrowing the future raster early.
- Closed the old real-device cache-hash reviewer gate without touching SD contents. The host `devicePathHash32("/stormlight.epub")` computes `2952540565`; the physical normal-speed cold-open trace enters `/stormlight.epub` and emits `/.crosspoint/epub_2952540565/...`, exactly matching the native fixture directory. This confirms the fixed 32-bit host hash preserves the ESP32-C3 cache namespace.
- Captured a fresh naturally rotated webcam frame after keepalive (`artifacts/calibration/x4-live-2026-07-13-1300.png`, SHA-256 `b7170a43…`). The X4 remains correctly framed and visibly responsive on a Mistborn text page; the camera is too soft for a new pixel-fidelity claim, so the earlier perspective-corrected comparisons remain canonical.
- Final gates for this batch pass: the 21-source profile regenerates byte-for-byte at SHA-256 `36a3689704ba4a6a28d77fe52d2fb75f9808e04093f1720d72700d657686abe1`; strict Pyright and module compilation are clean; both deterministic X3/X4 Python golden/video scenarios pass; all 12 native tests pass; and the runnable emulator was rebuilt after the test target. The normal production firmware still builds at the known cached `5,268,367` bytes flash / `49,532` bytes RAM, and `git diff --check` is clean. `/tmp/x4-recover-v36` completed the last reversible keepalive while these checks ran.
- Added calibration-only recursive cache population reporting. `CACHE:CLEAR` now counts files, directories, and bytes outside the timed boundary, restores normal CPU speed immediately before production deletion, and reports the population with the elapsed microseconds. The attributable calibration firmware is `1.4.1-dev-emulator-f42919ca-calibration-dirtye9d99898`, SHA-256 `c57b8087800cc6af87ff58de39cbdf06b424ca519513e0f5afb3474470575d14`, at `5,275,743` bytes flash / `49,564` bytes RAM.
- Accepted two Stormlight population sweeps, `x4-cache-population-v1` and `x4-cache-population-v2-repeat`. The repeated minimal cache is `8` files / `3` directories / `468,618` bytes at `219,918/220,546 us`; the repeated expanded cache is `26/3/652,800` at `534,688/534,090 us`. A pre-existing `33/3/820,771` cache clears in `645,518 us`, while a missing cache takes `6,512 us`. The v1 MP4/serial/events/manifest hashes are `e088a067…/fac70a61…/38d629d5…/9c8d65a5…`; v2 hashes are `4fc32838…/00bfbdee…/65fe2eb9…/53870629…`. Both videos are naturally rotated 90 degrees.
- Accepted cross-book population run `x4-cache-population-corpus-v1`: WOT `12/3/191,249` clears in `267,880 us`, Sun Eater `8/3/429,056` in `245,757 us`, Mistborn `8/3/424,060` in `204,209 us`, and LOTR `8/3/345,020` in `206,460 us`. Its MP4/serial/events/manifest hashes are `424f148a…/4cc65f30…/a95d65be…/93d26501…`. The naturally rotated no-op repeat `x4-cache-population-corpus-v2-noop-repeat` measured `6,468/6,507/6,385/6,480 us` with hashes `4edb93b1…/29418657…/1304b02f…/e0771fa2…`.
- Chose the smallest supported cache-clear model: missing caches use the five-sample `6,480 us` p50; populated caches use least squares `78,190 + 17,338 * fileCount us`. The rounded runtime coefficients fit all nine populated observations within `11.745%` absolute relative error. Bytes are not monotonic with duration, and every populated observation has exactly three directories, so neither byte nor directory coefficients are justified yet. The measured samples and residual remain profile evidence rather than hidden constants.
- `/tmp/x4-recover-v39` completed another reversible Down/Up keepalive after USB reattachment. `GUIDANCE.md` still says the earlier CPU-frequency audit is resolved and adds no blocker.
- Regenerated the provisional X4 profile from 25 attributable sources after isolating the cache-population scenarios from indexing/image model inputs. Their incidental opens remain raw workload evidence but cannot conflict with the dedicated cold-index captures. `models.cacheClear` retains all 14 population samples, the five-sample missing-cache distribution, the nine-sample populated fit, and its residual. Regeneration is byte-deterministic at SHA-256 `b6551ad0d948905339b447fbceec58a6faae745ff3514a6826350cfc77d14b7f`.
- Applied that model to the existing idle-File-Browser `storage.clearEpubCache` boundary. The handler counts regular files through the isolated run storage, schedules the shared production `Epub::clearCache()` mutation, and advances only the unconsumed synchronous workload remainder. It reports `fileCount` and nominal target beside actual simulated duration; uncalibrated X3/native runs retain zero added delay.
- Emulator replay `x4-cache-clear-profile-v2` verifies an absent Stormlight cache at exactly `6,480 us`, with `existed=false` and `fileCount=0`. Populated replay `x4-cache-clear-profile-v3-populated` creates the ordinary eight-file ASOIAF cache, clears it in exactly `216,894 us` (`78,190 + 8 * 17,338`), and then cold-opens successfully. The first replay exposed and closed a missing-path `ENOENT` inspection edge case by using the isolated storage boundary for existence before host traversal.
- Final verification for the cache model passes: strict Pyright is clean, module/example compilation succeeds, both existing Python scenario tests pass (`2` tests, covering deterministic X3/X4 goldens and MP4 metadata), all 12 native tests pass, clang-format 21 is clean on the changed C++ files, the runnable emulator rebuilds after the test target, the normal firmware remains at the known cached `5,268,367` bytes flash / `49,532` bytes RAM, and `git diff --check` is clean. `/tmp/x4-recover-v40` completed a further reversible keepalive during these gates.
- Added a calibration-only serial fixture upload so the ignored `books-for-testing/png-cover-calibration.epub` can be provisioned without mounting or preserving the SD card. The first continuous-stream protocol failed reproducibly at unrestricted, roughly 200 KiB/s, and roughly 40 KiB/s host rates (`x4-png-thumbnail-normal-v2` through `v4`); every failure removed the temporary file and left no partially committed EPUB.
- Replaced rate guessing with a deterministic 1 KiB ready/ack protocol. `UPLOAD:BEGIN` creates `/.crosspoint/calibration-upload.tmp`; each offset/count command receives READY before its exact payload and ACK after the device writes it; the final chunk validates the declared size and FNV64 before replacing the root EPUB. The Python hardware client and PNG calibration script use this protocol, with the script automatically sourcing the local ignored fixture. Strict Pyright, module compilation, and clang-format 21 pass for the new path.
- The chunked calibration firmware builds at `5,277,829` bytes flash / `49,628` bytes RAM. Its `firmware.bin` SHA-256 is `98cb49dc22fe26d65ffbff1dacf0d90dcb2b6980c1e711ce1589eaf297c3d849`; `/tmp/x4-recover-v42` confirms the flashed device reports `1.4.1-dev-emulator-f42919ca-calibration-dirty42d4f319` and completes a reversible Down/Up keepalive at normal CPU frequency.
- Binary `readBytes()` still received zero bytes after the per-chunk READY boundary, so the protocol now keeps each chunk on the proven line-command path and hex-decodes it on device. The first 512-byte encoded attempt (`x4-png-thumbnail-normal-v6`) hit the firmware's exact 1,001 ms `readStringUntil()` timeout, consistent with a 512-byte CDC receive window truncating the roughly 1 KiB command. The host now sends 224 raw bytes per command, keeping the complete hex line, maximum offset, framing, and newline below 512 bytes; the flashed firmware already accepts that smaller size.
- The ASCII-chunk firmware remains `5,277,829` bytes flash / `49,628` bytes RAM; its `firmware.bin` SHA-256 is `239eb4ef7f8673b5abee65d010764d20519bf477b376e56d4dccc0fa699941dc`. `/tmp/x4-recover-v43` confirms `1.4.1-dev-emulator-f42919ca-calibration-dirtyca7b2857` and another normal-frequency Down/Up keepalive. Upload event records contain only offsets and byte counts, never fixture contents.
- A 224-byte raw chunk still produced a roughly 477-byte wire line and hit the same exact 1,001 ms truncation (`x4-png-thumbnail-normal-v7`), narrowing the practical CDC receive window below 512 and plausibly to 256 bytes. Reduced the raw chunk to 96 bytes, about 221 bytes with hex, maximum offset, framing, and newline. The extra acknowledgements cost time but keep every command safely below that observed ceiling.
- The 96-byte protocol succeeds, but WSL USB round trips sustain only about four commands per second: `x4-png-thumbnail-normal-v8` reached 48,000 of 1,212,436 bytes after roughly 140 seconds, projecting close to an hour. Stopped that non-timing transport run. Derived a separate ignored `png-cover-calibration-small.epub` rather than altering the original: it retains the same minimal EPUB structure and `963x1445` 8-bit RGB PNG decoder/scaler path, with a four-level grayscale cover compressed to 51,237 bytes. The EPUB is 52,178 bytes, passes `unzip -t`, is locally ignored, and has SHA-256 `288e5d5b74d1585e0f062e2ad5960cccbc2ce8384d52e6a8fa1b6a7994010061`.
- Accepted `x4-png-thumbnail-normal-v9`. The calibration upload commits `/png-cover-calibration.epub` only after verifying all `52,178` bytes and FNV64 `f83ddc4079f35a56`; cache clear is `6,471 us` with no cache. The cold open indexes the one-spine fixture normally, then PNG thumbnail generation takes exactly `3,592 ms` from the production `Generating` to successful `Generated` logs: `50,315` archive-compressed / `51,237` decoded bytes, `963x1445` RGB source, and `135x226` target. The naturally rotated 480x640, 30 FPS capture is 214.9 s / 6,447 frames. MP4/serial/events/manifest SHA-256 values are `05b3a239…/1bba8038…/2b7441b2…/11c28462…`.
- Kept the single PNG observation exact rather than fitting a content-size curve. The generated model records device path, decoded bytes, source and target dimensions, and the `3,592 ms` boundary. Native thumbnail pacing wraps the PNG production path only when the exact path/content/target record matches; unmatched PNGs retain generic fallback timing. The same scope continues to select the five-point JPEG model for JPG covers.
- Adding the attributable no-cache PNG sample changes the missing-cache distribution to six observations with `6,475.5 us` p50, rounded to `6,476 us` at runtime. The nine populated points and `78,190 + 17,338 * fileCount us` fit are unchanged. The 26-source profile regenerates at SHA-256 `333e4f3d5bf6994c51e083753ca628cea678092ae192432845b68e38868463b9` and contains 15 cache-clear samples.
- Profiled emulator runs `x4-png-thumbnail-runtime/run-v1-profiled` and `run-v2-profiled-repeat` both report exactly `3,592 ms`; normalized events/logs, the settled natural PNG, and the 19.933 s MP4 are byte-identical. The PNG hash is `872d99b26a32c20769d27d0c0afb8698213ccd66ecf7abbfd31cc89501958f15`; MP4 hash is `2449a9af2c1115e59c46c2d3296a2979900eeba7d5409fb5450fd9a826357ee2`.
- The 5.5 s / 165-frame, 30 FPS perspective-corrected comparison aligns physical generation at `206.714` s with emulator generation at `13.752` s and includes the settled repaint. Cover crop, card bounds, title/author layout, menus, and soft keys agree; hardware is lighter/softer while the emulator remains crisp and regularly halftoned. MP4/sidecar/loading/settled hashes are `abcb3826…/b2e4705c…/867fad25…/52f28ecb…`. `/tmp/x4-recover-v44` then completed another normal-frequency Down/Up keepalive.
- Final verification after applying PNG timing passes: the 26-source profile regenerates byte-for-byte at SHA-256 `333e4f3d5bf6994c51e083753ca628cea678092ae192432845b68e38868463b9`; strict Pyright and Python module compilation are clean; both deterministic X3/X4 Python scenarios pass; all 12 native tests pass; clang-format 21 and `git diff --check` are clean; and the runnable emulator rebuilds after the test target. A direct missing-cache smoke reports exactly `6,476 us`. The normal production firmware builds at the known cached `5,268,367` bytes flash / `49,532` bytes RAM report; the last isolated clean baseline remains `5,268,359` / `49,532`.
- A final scope audit caught and prevented a JPEG regression before handoff: exact-PNG activation must wait for its decoded byte count, but JPEG generic-transfer suppression must start before extraction. Moved JPEG activation back to the start of the whole workload and PNG activation to immediately after `getItemSize()`, before extraction. Final PNG control remains exactly `3,592 ms` with settled hash `872d99b2…`; Stormlight JPEG remains on the current five-point target at `3,511 ms` with its established settled hash `8e8cc9a1…`. All 12 native tests, runnable-emulator rebuild, clang-format, and the normal firmware build pass again after this correction.
- `/tmp/x4-recover-v45` completed the final reversible Down/Up keepalive after the gates, again restoring normal CPU frequency and leaving the attached X4 responsive.
- Resumed after the X4 was reattached. `/dev/ttyACM0` and `/dev/video0` are present; the first recovery invocation lacked the Python module path, and the corrected `PYTHONPATH=emulator/python/src` invocation completed a reversible keepalive without changing device state. `GUIDANCE.md` still says the CPU-frequency audit is resolved and adds no blocker.
- Chose the next non-deferred evidence gap from the profile itself: PNG Home thumbnails have one exact observation, while unmatched PNGs retain fallback timing. Derived three locally ignored, valid minimal EPUBs at controlled `321x482`, `642x963`, and `1284x1927` source dimensions. All retain the accepted fixture's 8-bit truecolor RGB/four-gray-level decoder path and fixed Home target; EPUB sizes are `10,999/29,041/68,320` bytes with SHA-256 `91d6784a…/b13941cc…/11dd002d…`. An initial interpolation-heavy set was rejected before upload because it inflated transport to `71-380` KiB without adding useful decoder coverage.
- Added one ordinary Python calibration scenario that serial-uploads all three fixtures, clears each cache, cold-opens through the production EPUB path, returns Home, waits for the successful PNG thumbnail boundary, and leaves a settled frame. Added marker parsing that keeps each path's thumbnail observation attributable instead of pooling the three into a false median; it deliberately does not promote the incidental synthetic-book indexing phases into exact indexing records. Runtime timing remains unchanged until the measurements justify either a simple PNG curve or additional exact points.
- The first series capture completed valid `321x482` and `642x963` observations, then failed near the end of the `1284x1927` upload. The device had acknowledged byte `65,472`, but that one atomic reply reached WSL as `CAL:UPLOAD:CHUNK:` and `ACK:65472:...` fragments about `3.8 s` apart; the host's `readline()` timeout incorrectly promoted both fragments to separate lines and timed out waiting for the real prefix. The raw serial log proves the device write succeeded. Changed the serial reader to preserve exact raw bytes while buffering event/reply parsing until newline, rather than weakening timeouts or retrying a possibly committed chunk. The failed run stays failed evidence; the complete series will be recaptured.
- Accepted the complete naturally rotated recapture `x4-png-thumbnail-series-normal-v2`: 480x640 at 30 FPS, 13,981 frames / 466.033 s, firmware `1.4.1-dev-emulator-f42919ca-calibration-dirtyca7b2857`. Whole PNG thumbnail boundaries are `551/1,700/5,987` ms at `321x482/642x963/1284x1927`; combined with the earlier `963x1445` point, the four-point sequence is `551/1,700/3,592/5,987` ms. Capture/serial/events/manifest SHA-256 values are `f96bf728…/ad912c19…/d98c5a18…/495bbff8…`. The newline-buffered reader completed all verified uploads without misclassifying delayed USB fragments.
- Split the PNG evidence at its actual production logs instead of forcing one predictor across unlike work. Generation-to-converter preparation fits `48,615 us + 6,975 ns * imageBytes` within `0.973%`; converter-to-success fits `120,586 us + 2,168 ns * sourcePixels` within `4.359%`. A decoded-byte fit for the full workload was rejected at `53.037%` error. Runtime suppresses duplicate generic storage only inside the matching phase: preparation requires `9,360-68,617` bytes, while conversion additionally requires RGB8, the `135x226` target, and `154,722-2,474,268` source pixels. Unsupported and extrapolated inputs retain fallback behavior.
- Four profiled replays land at `570/1,704/3,544/6,012` ms versus physical `551/1,700/3,592/5,987`, a maximum whole-boundary error of `3.45%`; all settled cover rasters are unchanged. A `160x241` RGB8 control outside both calibrated ranges remains raw fallback at `59` ms instead of inheriting the curve. Two identical largest-point video runs have byte-identical 22.333 s MP4s (SHA-256 `2048f409…`) and settled PNGs (`cdfd3e74…`); normalized event/log differences are artifact paths only.
- Preserved a 7.5 s / 225-frame extreme-point visual review at `artifacts/comparison/x4-png-thumbnail-model/review-largest-v1/png-largest-comparison.mp4`. It aligns the physical and emulator generation starts at `456.176/13.762` s, perspective-corrects corners `42,20,355,6,70,524,377,523`, and confirms matching cover crop, card geometry, title/author, menu rows, and soft keys through the settled repaint. Hardware remains lighter/softer; the emulator remains crisp/regularly halftoned. MP4/sidecar/physical-still hashes are `a43057bf…/b91c5eae…/1d55c4df…`.
- The series adds one missing-cache and two valid six-file populated-cache points. The seven missing samples now select `6,471 us` p50; eleven populated points fit `74,036 + 17,504 * fileCount us` within `12.894%`. Direct replays report exactly `6,471 us` for a missing cache and `179,060 us` for the ordinary six-file synthetic cache. The 27-source profile regenerates at SHA-256 `3e7b0e5ce37592b704d816ef5794f11fc73b4f01072d06f6b06c036e4755752c`.
- `/tmp/x4-recover-v46` completed another reversible Down/Up keepalive after the long capture and restored normal CPU frequency. Its serial/events hashes are `cbe3d5a5…/d8b57d78…`; `GUIDANCE.md` still has no new blocker.
- Final audit kept the two-phase model and narrowed its claim rather than adding another abstraction: the generated limitation now explicitly bounds both PNG preparation bytes and RGB8 conversion area, and `<array>` is compiled only in the emulator branch that reads the PNG header. The 27-source profile regenerates byte-for-byte at SHA-256 `d0afa6dfc22e2f73eccb557cee9ddb72e903ec632157180abc85c33485b31a97`.
- Final gates pass: strict Pyright and module/example compilation are clean; both existing Python scenarios pass; all 12 native tests pass; clang-format 21 and `git diff --check` are clean; and the runnable emulator was rebuilt after the test target. A fresh Stormlight JPEG control remains exactly `3,511 ms` with established settled PNG SHA-256 `8e8cc9a14f5b84acb021d659433e26f91942b5359407f0b8cc628f04e63265e9`. The normal firmware again builds at the known cached `5,268,367` bytes flash / `49,532` bytes RAM; the isolated clean baseline remains `5,268,359` / `49,532`. `/tmp/x4-recover-v47` completed a final reversible keepalive on calibration firmware `dirtyca7b2857`; serial/events hashes are `dd601164…/d0abc732…`, and `GUIDANCE.md` still adds no blocker.
- Chose the next non-deferred profile limitation rather than expanding into the deferred X3/population/temperature work: cold indexing had one observation per exact book. Added `calibrate_cold_index_repeats.py`, which physically enters File Browser, clears the complete Stormlight cache, cold-opens, waits for the first-page progress boundary, returns Home, and repeats three times. Marker-aware analysis isolates this run from image, section, and cache-clear models and aggregates exact indexing by path instead of rejecting valid repeat measurements as conflicts.
- Accepted `x4-stormlight-index-repeats-normal-v1`: three complete Stormlight totals are `23,080/23,256/22,778` ms, with OPF `5,105/5,125/5,091`, TOC `1,946/2,066/1,958`, book-bin `16,028/16,064/15,729`, and post-index `1,111/1,110/1,218` ms. Manifest/spine/TOC counts remain exactly `1,686/844/776`. The naturally rotated 480x640, 30 FPS capture is 136.133 s / 4,084 frames; MP4/serial/events/manifest hashes are `aafa1857…/e5c9df07…/31112e03…/fcb1c842…`. A sampled frame visibly confirms upright device text and the production Indexing popup.
- Combined with the accepted core run, Stormlight now has four same-device samples. The profile retains p50/p90/MAD per phase and exposes rounded runtime p50s: OPF `5,112`, TOC `2,009`, book-bin `15,966`, and post-index `1,111` ms. Their independent p50 sum is `23,087` ms, six milliseconds above the measured `23,081` ms total p50, so the runtime total is honestly floored at the phase sum. Emulator replay lands exactly on all four runtime scopes (`23,087` ms total plus `1,111` ms post-index) and preserves the settled Stormlight raster SHA-256 `02c7f3b7…`. The 28-source profile currently regenerates at SHA-256 `f572730ec0776e010945b8816e37849efd01622b57c361838f23ace756390361`.
- Final repeat-index gates pass. Two fresh cold emulator replays have byte-identical normalized stderr (`9eb912f2…`), events (`815b5500…`), and settled PNG (`02c7f3b7…`); the 28-source profile regenerates byte-for-byte at SHA-256 `f572730e…`. Strict Pyright over source/tests/examples, module compilation, both Python scenarios, all 12 native tests, runnable-emulator rebuild, normal firmware build (`5,268,367` bytes flash / `49,532` bytes RAM), and `git diff --check` all pass. `/tmp/x4-recover-v48` completed another reversible keepalive on the attached calibration device; serial/events hashes are `8d0da522…/22ed8faf…`. `GUIDANCE.md` remains unchanged and adds no blocker.
- Extended the same isolated repeated-index boundary to the other exact corpus paths without adding another runtime model. `calibrate_cold_index_corpus_repeats.py` performs two physical cold opens each for WOT, Sun Eater, Mistborn, and LOTR in one continuous run. The analyzer groups samples by the measured EPUB path and deliberately returns before collecting the run's incidental Home metadata, cache-clear, section, or image work.
- Accepted `x4-corpus-index-repeats-normal-v1`: all eight samples are complete and preserve their per-book manifest/spine/TOC counts. New total-index pairs are WOT `20,438/21,181`, Sun Eater `18,447/18,611`, Mistborn `12,929/14,812`, and LOTR `9,147/9,483` ms. The naturally rotated 480x640, 30 FPS video is 276.167 s / 8,285 frames; MP4/serial/events/manifest hashes are `ecac33f6…/bb816308…/12c7a930…/b8b09c4e…`. An inspected frame confirms upright text and the production Indexing popup, and every cold segment follows a normal-frequency physical input restore.
- Combining those pairs with the accepted core corpus gives three same-device samples for every non-Stormlight exact path. Runtime total p50s are now WOT `21,181`, Sun Eater `18,447`, Mistborn `14,192`, and LOTR `9,227` ms; observed ranges are preserved in `phaseStatistics`, not collapsed into false certainty. With four Stormlight samples, all five exact index records now carry within-device p50/p90/MAD and invariant item counts. The 29-source profile currently regenerates at SHA-256 `f6e6137ec9de6719b3efa4c84454a92c53863b377e4d8308c6bbf44ca7b41b11`.
- Applied all four updated records through fresh cold emulator runs. Every replay hits its profile's OPF/TOC/book-bin/total/post-index scopes exactly: WOT `4,804/1,659/14,047/21,181/862`, Sun Eater `4,480/1,283/11,832/18,447/417`, Mistborn `2,213/5,455/5,422/14,192/510`, and LOTR `1,972/2,431/4,753/9,227/483` ms. A second fresh run per book has byte-identical normalized stderr, events, and settled PNG; final raster hashes are `e0ae3599…/fda76ff4…/87e75f92…/7790f88e…` in the same book order.
- Final corpus-index gates pass: the 29-source profile regenerates byte-for-byte at SHA-256 `f6e6137ec9de6719b3efa4c84454a92c53863b377e4d8308c6bbf44ca7b41b11`; strict Pyright over source/tests/examples, compilation, both Python scenarios, all 12 native tests, runnable-emulator rebuild, normal firmware build (`5,268,367` bytes flash / `49,532` bytes RAM), and `git diff --check` are clean. `/tmp/x4-recover-v49` completed the closing reversible keepalive; serial/events hashes are `bb66293b…/273d1b64…`. No submodule pointer or `freeink-sdk` worktree change was introduced, and `GUIDANCE.md` still adds no blocker.
- Accepted the independent Settings-driven XL-font repeat `x4-stormlight-xl-font-normal-v6-repeat`. The script changes Medium to XL through physical controls, samples eight page turns, then restores Medium through Settings; `CAL:MARK:medium-restored` is present and the final camera frame is an upright 480x640 Home screen. The 30 FPS capture is 59.533 s / 1,786 frames. MP4/serial/events/manifest SHA-256 values are `ed59560a…/8e24fc54…/13f941ec…/169090b5…`; its scenario SHA-256 matches the accepted normal-speed run at `81441c26…`.
- The repeat confirms refresh-mode boundaries rather than a new content timing constant. Its six fast-primary samples retain `506/62` ms p50 primary/grayscale BUSY, exactly matching the prior run; three half-primary samples retain `1,722/144` ms versus the prior `1,722.5/144.5` ms. Whole fast-page p50 moves from `1,230` to `2,089.5` ms as the sampled render/display work changes, while half-page p50 remains close at `2,598/2,540` ms. Applying the repeat to runtime would double-count or broaden content-specific phases.
- Added an explicit evidence-workload analyzer input. These runs remain attributable sources with parsed raw workload metrics but are excluded from every runtime model. Adding the XL repeat leaves `renderMs`, panel, storage, directory, ordinary/warm workloads, optical evidence, video metadata, and every model byte-for-byte unchanged. The profile grows from 29 to 30 sources and regenerates at SHA-256 `b3794aa8dca81caf638bd901cce0343928f7e2405b566cca7e5135277a922c0e`.
- `/tmp/x4-recover-v50` completed a reversible Down/Up keepalive after analysis; serial/events SHA-256 values are `166eacc1…/4984e4dc…`. `GUIDANCE.md` remains unchanged and adds no blocker.
- Final XL-evidence gates pass: deterministic regeneration is byte-identical at SHA-256 `b3794aa8…`; strict Pyright over source/tests/examples reports zero diagnostics; module/example/test compilation is clean; both Python X3/X4 scenarios pass after restoring the runnable emulator; all 12 native tests pass; and the normal firmware builds with the known cached report of `5,268,367` bytes flash / `49,532` bytes RAM. `git diff --check` is clean, `books-for-testing/` and `artifacts/` remain locally ignored, and `freeink-sdk` remains clean at `bda4293` with no pointer change. `/tmp/x4-recover-v51` completed another reversible keepalive; serial/events hashes are `ec8d949c…/cc4c2c41…`.
- Reopened the X4 completion audit rather than treating the explicitly deferred X3/population/temperature profiles as the end state. The remaining material X4 gap is content-specific optical motion: the applied fast-reader waveform has 12 text-page samples, while image-heavy refreshes were only workload evidence. Offline analysis of the accepted cold Stormlight video finds three full-page image fast transitions with one large optical peak each; its later low-change pages cannot honestly pass the same threshold.
- Accepted an independent cold image sequence, `x4-stormlight-image-optical-normal-v1`, on the attached X4. It is complete on calibration firmware `dirtyca7b2857`, naturally rotated 480x640 at 30 FPS, and records 68.133 s / 2,044 frames. MP4/serial/events/manifest SHA-256 values are `51943996…/a885ac62…/0fc3f6ec…/2827a287…`; the scenario SHA-256 is `635cfe29…`. Eight full-page cover refreshes each have one camera transition above the existing threshold, spanning 67-133 ms. Inspected frame sequences are upright and visibly show the cover moving through a lighter direct-drive state into its dark settled state.
- `/tmp/x4-recover-v52` completed a reversible Down/Up keepalive immediately before that capture; serial/events hashes are `f8a47a29…/d4383605…`. The scenario's cold clear, physical page inputs, and warm reopen kept the device at normal frequency and active throughout.
- Added a dedicated image-heavy fast-waveform analyzer rather than weakening the text analyzer. It accepts only complete X4 page operations whose rendered image covers at least half the panel (`192,000` pixels), still requires exactly one optical peak, verifies decoded-frame alignment and the MP4 hash, and records the runtime target selector explicitly. The accepted eight samples have transition start `248.283` ms p50 (`228.675-266.343`, MAD `9.546`) and end `342.846` ms p50 (`331.620-362.075`, MAD `9.207`); the analysis SHA-256 is `c030e2c3…`.
- Applied the new optical record only to majority-dark fast targets. Canonical replay targets for the full cover are `72.9-75.0%` dark, while observed text/control targets stay below `7%`; a conservative profile-owned `50%` threshold cleanly separates them without an EPUB/image hook. The image drive blends the target toward white, matching the camera's lighter intermediate cover, and settles at the measured boundary. Ordinary fast text pages, half/full refresh, and grayscale retain their existing schedules.
- The 31-source profile adds only `opticalWaveform.readerImageFast`; render, panel BUSY, storage, directory, workloads, warm/evidence workloads, runtime models, and video metadata remain unchanged. It regenerates at SHA-256 `1e76763fb6d2538a4786cc1667f9b54b5f6bce9eea1ee1fae04e8db22ff1a8c4` and records the source video SHA-256 `51943996…` plus the `50%` selector.
- Two fresh profiled two-page replays are byte-identical after path normalization: MP4 `806cf503…`, settled PNG `77e86e45…`, events `5a3f5f5f…`, and stderr `a1848c86…`. The runtime emits `optical-image-drive` exactly `248,283 us` after the full-cover primary begins and settles at `342,846 us`; UI/text primaries continue to emit the ordinary `optical-drive` phase.
- Preserved a naturally oriented 0.8 s / 24-frame physical/emulator review at `artifacts/comparison/x4-image-optical-runtime-v1/physical-comparison.mp4`. It aligns decoded-camera operation `42.296226906` s with emulator operation `36.409794` s after accounting for the export timeline's initial 250 ms frame. The camera and emulator both move from the prior light screen through a lighter cover into the dark target within one 30 FPS frame; geometry agrees while the physical image remains softer/lower contrast. MP4/sidecar hashes are `5199ec5e…/2e2eac36…`.
- Simplified the optical analyzers after the replay by sharing their direct-reader extraction and peak-analysis path; the text analyzer's output contract is unchanged and the image analysis regenerates byte-for-byte at SHA-256 `c030e2c3451ab61365f2424c771db563de0ef4af041022eda81f85d3fab806eb`. The profile generator call now uses named arguments, and native configuration shares the direct-waveform loader while retaining separate selector validation.
- Kept schema-version-1 profiles backward compatible. When `readerImageFast` is absent, native configuration installs the existing reader-fast schedule with an unreachable `101%` dark threshold. `x4-image-optical-runtime-baseline-v2` loads the older 30-source profile successfully and preserves the old cover boundaries (`266,196/309,576 us`) and ordinary drive frame; the 31-source replay alone selects `248,283/342,846 us` and `optical-image-drive`.
- Closing gates pass after that compatibility correction: the 31-source profile regenerates byte-for-byte at SHA-256 `1e76763fb6d2538a4786cc1667f9b54b5f6bce9eea1ee1fae04e8db22ff1a8c4`; strict Pyright reports zero diagnostics; Python source/tests/examples compile; both Python scenarios pass; all 12 native tests pass; clang-format and `git diff --check` are clean; and the runnable emulator was rebuilt after the native test target. The normal firmware also builds at the known cached `5,268,367` bytes flash / `49,532` bytes RAM report.
- `/tmp/x4-recover-v53` and `/tmp/x4-recover-v54` completed reversible Down/Up keepalives around the final simplification and verification. Their serial/events SHA-256 values are `ee6d421ddca87316ced7dcae08733f6349851bf866e57c48d65f5b0cdb0f18a8/2e95edaee3014d31e7448fc147cd40a861656b26235659b2e86e5846ba65e50b` and `14287e2c6d31dfe3ba91bf4aaf7d833b2d1c98adf965c33789bae19b9c34cc29/dfb65b60a87e30e592071060390cd91b332a145e0d5bfc6d2a2780b195242630`. `books-for-testing/` and `artifacts/` remain locally ignored; `freeink-sdk` remains clean at `bda4293` with no pointer change.
- Re-read `GUIDANCE.md` after closing the batch. The monitor's latest note remains “Nothing further; carry on”; it adds no correction or blocker. X3 and population/temperature profiles remain explicitly deferred rather than silently treated as complete.
- `/tmp/x4-recover-v55` completed a reversible Down/Up keepalive before the next X4 profile-consumption audit on calibration firmware `dirtyca7b2857`; serial/events SHA-256 values are `9f638421…/ec723ee9…`. `GUIDANCE.md` still says “Nothing further; carry on.”
- Audited boot timing because the applied profile had no boot-specific workload. A deterministic baseline reached Home at `4,051,000 us` and completed its two fast refreshes at `4,798,000/5,548,000 us`. One non-destructive esptool hard-reset probe on the attached X4 measured Boot entry/display at `440/445` ms, full completion at `4,349` ms, Home entry at `4,513` ms, and the two Home completions at `5,179/5,946` ms. Probe serial/events/esptool hashes are `4757938d…/bed63459…/b86503ce…`.
- Found a profile-consumption bug rather than a missing boot constant. Controlled X4 panel evidence already measures whole fast/half/full operations at `743,848/2,039,848/4,060,843 us` and BUSY at `506,000/1,723,000/3,744,000 us`, but the native bus charged only BUSY for direct full `displayBuffer()` calls used by Boot and Sleep. It now charges the existing `316,843 us` full transfer/setup remainder before BUSY; configuration rejects any fast/half/full operation shorter than its BUSY interval.
- The corrected deterministic boot replay reaches Home at `4,367,843 us` and completes its two Home refreshes at `5,114,843/5,864,843 us`. Hardware residuals fall from roughly `381-462` ms to `64-145` ms without fitting a new value. Two corrected sleep/reset replays remain byte-identical: sleep/reset MP4 hashes are `ec69cee0…/6db95f69…`, retained panel PNG remains `df4847df…`, presentation sleep remains `de1d5cd0…`, and normalized main/reset event hashes are `4496171b…/9e82f03b…`. Deep-sleep timing remains governed by the measured whole sleep boundary, while its full-refresh event shifts by the intended remainder.
- Accepted the naturally rotated physical boot capture `x4-boot-reset-normal-v1`: 480x640, 30 FPS, 331 frames / 11.033 s, firmware `dirtyca7b2857`. Its serial boundaries repeat the probe exactly at `440/445/4,349/4,513/4,591/5,179/5,358/5,946` ms. MP4/serial/events/manifest/esptool SHA-256 values are `f06da399…/ee16e8ee…/a137c55b…/4f11794f…/b86503ce…`.
- Preserved the retained-panel emulator replay and perspective-corrected physical comparison at `artifacts/comparison/x4-boot-runtime-v2-retained`. Visual inspection confirms the same retained Home -> upright CrossPoint Boot -> Home sequence and matching Boot geometry; hardware remains softer/lighter and the physical Home has a Stormlight card while the emulator fixture says “No open book.” Emulator/comparison MP4 hashes are `f0ef250a…/42c007ba…`; the comparison sidecar hash is `961a5bc5…`.
- Added `crosspoint-calibrate record-boot` by sharing the existing camera arguments and manifest fields. It starts the camera, invokes the PlatformIO esptool hard reset, opens serial immediately afterward, waits through Home low-power, and sends a harmless Back input so the reader remains awake. An initial failed run is retained as `x4-boot-reset-normal-v2-repeat`: resolving PlatformIO's Python symlink had stripped its virtualenv and produced `ModuleNotFoundError: rich_click`. Preserving that symlink fixes the root cause; strict Pyright and CLI help pass.
- Accepted the CLI-driven repeat `x4-boot-reset-normal-v3-repeat`: 480x640, 30 FPS, 388 frames / 12.933 s, complete on firmware `dirtyca7b2857`. Boot/display/full/Home boundaries are `440/445/4,348/4,513` ms and Home refresh completions are `5,182/5,948` ms, within `0-5` ms of the first capture; low-power occurs at `7,513` ms and the CLI's Back input restores normal CPU frequency at `7,682` ms. MP4/serial/events/manifest/esptool hashes are `86e990ad…/b44f6834…/e15ffdc0…/a330fee3…/b86503ce…`. An inspected Boot frame is naturally upright.
- The 31-source X4 profile remains data-identical because both boot captures cross-validate its existing full operation rather than becoming another model input. Deterministic regeneration is byte-for-byte unchanged at SHA-256 `1e76763fb6d2538a4786cc1667f9b54b5f6bce9eea1ee1fae04e8db22ff1a8c4`.
- Closing boot-audit gates pass: strict Pyright reports zero diagnostics; Python source/tests/examples compile; both Python scenarios pass; all 12 native tests pass; clang-format 21 and `git diff --check` are clean; the runnable emulator was rebuilt after the test target; and the normal firmware builds at the known `5,268,367` bytes flash / `49,532` bytes RAM. `/tmp/x4-recover-v56` completed a final reversible Down/Up keepalive with serial/events hashes `5a99efb8…/011a700b…`. `books-for-testing/` and `artifacts/` remain locally ignored, and `freeink-sdk` remains clean at `bda4293` with no submodule-pointer change.
- Completed a mechanical consumer inventory for every parsed `TimingProfile` field. All nominal storage, render, workload, panel-operation, BUSY, and active optical fields reach an explicit runtime boundary. The only dead runtime state was the controlled fast/half optical schedule: native configuration parsed and stored it, then the panel model unconditionally selected the separately measured reader-content schedules. Removed those two unused C++ fields and made grayscale, half-reader, fast-reader, and full transitions four direct paths. The controlled fast/half values remain attributable profile evidence and the full schedule remains active.
- Verified the optical cleanup is behavior-neutral with the same 500 ms sleep/reset scenario used before it. Normalized events are byte-identical at SHA-256 `0932d2fd…`; sleep/reset MP4s remain exactly `ec69cee0…/6db95f69…`; and the retained sleep panel remains `df4847df…`. Strict Pyright, compilation, both Python scenarios, clang-format 21, all 12 native tests, `git diff --check`, and the runnable-emulator rebuild pass. The versioned 31-source profile remains unchanged at SHA-256 `1e76763f…`.
- `/tmp/x4-recover-v57` completed another reversible Down/Up keepalive after the consumer audit; serial/events SHA-256 values are `1e787616…/ebb7f110…`. The attached device remains on calibration firmware, and `freeink-sdk` remains clean at `bda4293`.
- Audited the actual camera cadence before treating a requested frame rate as optical resolution. `x4-boot-reset-normal-v4-60fps` encodes 783 frames at 60 FPS, but FFmpeg's pre-encoder `showinfo` exposes only 319 distinct source frames at roughly 40 ms intervals; the encoder duplicated them. Rejected it as higher-rate evidence. Its MP4/serial/events/manifest hashes are `d45e2214…/8f4363cf…/aaad20b1…/fae0cdec…`.
- Added source-cadence collection to `WebcamRecorder` and a backward-compatible reader for existing FFmpeg logs. All five accepted panel/grayscale/reader optical sources actually decode at 25.0 FPS, with 40.0 ms p50 and 40.1 ms p90 frame intervals, despite their older 30 FPS CFR metadata. Their regenerated analysis hashes are `9e1a66e5…/4052cfef…/c3ded202…/4d3d86f0…/63b51ff7…`; every nominal transition value remains unchanged. The profile and docs now distinguish requested/encoded FPS from observed source cadence and use 40 ms as the evidence quantization.
- Added `crosspoint-calibrate waveform-boot` as a natural full-refresh conformance check. It aligns video through the median of 17 pre-refresh serial clock-origin samples rather than post-refresh log delivery, then records the optical peaks without fitting another runtime schedule. The first two analyses have `3,903 ms` operations and `283.33/283.335 ms` main pulse medians; their hashes are `a11ca714…/e340b682…`.
- Accepted final naturally upright capture `x4-boot-reset-normal-v6-cadence-final`: Boot/display/full/Home boundaries are `440/445/4,349/4,514 ms`, the complete full operation is `3,904 ms`, and the main pulse median is `266.67 ms`, matching the controlled `266.7 ms` schedule at the available 40 ms camera resolution. Its source is 318 decoded frames at 25.0 FPS with `35.7/40.0/40.1/360.063 ms` min/p50/p90/max intervals. MP4/serial/events/manifest/waveform hashes are `dba81fe5…/16cb46ea…/57f089d1…/6be86c0a…/f33d47c6…`; a second waveform extraction is byte-identical.
- Regenerated the 31-source X4 profile at SHA-256 `cee46d9d852f7583072af6f2b22d83b5d0603f9462e31e96101c0a27e8258c83`. A direct old/new audit shows only cadence metadata, the 33.333-to-40 ms evidence quantization, and corresponding limitation text changed; panel, optical, storage, render, and workload model values did not. The established sleep/reset replay remains byte-identical (`ec69cee0…/6db95f69…` MP4s, `df4847df…` retained panel, `0932d2fd…` normalized events).
- Closing cadence gates pass: strict Pyright reports zero diagnostics, Python source/tests/examples compile, both Python scenarios pass, the 31-source profile and final Boot analysis regenerate byte-for-byte, `git diff --check` is clean, and the runnable native emulator rebuild succeeds. Earlier in the same batch all 12 native tests passed; no later native source changed. `/tmp/x4-recover-v58` completed another reversible Down/Up keepalive and restored normal CPU frequency; serial/events hashes are `77cc53b4…/ad35b17e…`. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293` with no pointer change, and `GUIDANCE.md` still adds no blocker. X3 and population/temperature profiles remain explicitly deferred.
- Closed the remaining X4 image/cleanup interaction gap with `calibrate_half_image_optical.py`: it changes the physical Settings refresh cadence from 15 pages to every page, cold-opens Stormlight, waits for each of eight actual rendered-page completions, leaves the reader, and restores the 15-page setting. The accepted naturally upright capture `x4-stormlight-half-image-optical-normal-v1` is complete on calibration firmware `dirtyca7b2857`; its `refresh-15-restored` marker is present at device time `930,584 ms`. MP4/serial/events/manifest/FFmpeg-log SHA-256 values are `bd10ffd2…/232c4909…/b74a9704…/d302ab0d…/4d60a505…`. The 134.033 s CFR video contains 4,021 encoded frames; FFmpeg observes 3,343 real camera frames at 25.0 FPS with `35.0/40.0/40.1/360.019 ms` min/p50/p90/max intervals. Its manifest binds the captured scenario at SHA-256 `d8759b52…`; a later source-only docstring clarification changes the current script hash without changing behavior.
- The apparent image-HALF case is not a missing waveform. All nine full-cover operations still use two `506-507 ms` FAST primaries plus `62-63 ms` grayscale, with no HALF primary. The production reader explicitly blanks and restores image bounds with FAST because HALF would prevent the following grayscale LUT from adjusting the particles, then forces the next ordinary page to take the cleanup HALF. The ordinary half-waveform analyzer correctly rejects this run instead of widening its text-page distribution.
- Independent image-fast extraction is deterministic at SHA-256 `5491256b318288e1b4e7e07222a066a36ec203cf0de9e2e0eccd35a9846c6260`. Its nine image-heavy samples have `254.012/334.228/281.349 ms` p50 start/end/dominant-change boundaries. Pooling them with the original eight samples gives `253.734/337.073/281.349 ms` p50 boundaries across 17 samples; every shift from the applied profile is below 9 ms and therefore below one 40 ms camera source frame. Retained the existing single image-FAST schedule and did not invent an image-HALF mode or retune on sub-frame movement.
- A profiled emulator fixture with `refreshFrequency: 0` reproduces the physical invariant: each full cover takes two FAST operations and `optical-image-drive`, while the next ordinary text page takes `inverted-target` HALF cleanup and its grayscale operation emits `no-visible-change`. Two three-page replays are deterministic after output-path normalization: MP4 SHA-256 `4e214647…`, final PNG `79e32f5e…`, normalized events `6ffcfa40…`, and normalized stderr `e4f23537…`. The MP4 and settled PNG are also byte-identical directly.
- Preserved an upright physical/emulator review at `artifacts/comparison/x4-image-refresh-override-v1/physical-comparison.mp4`; its MP4/sidecar SHA-256 values are `4f2b5821…/77b608f6…`. At the aligned transition both show the same Stormlight cover geometry; the camera remains softer/lighter while the emulator is crisp and regularly halftoned. The first frame honestly differs in prior state because hardware has already blanked the page while the emulator still retains its file-browser/indexing raster.
- Closing image/cleanup gates pass: strict Pyright reports zero diagnostics over source/tests/examples, Python compilation is clean, both existing Python scenarios pass, the image-fast analysis regenerates byte-for-byte, and the 31-source profile remains byte-identical at SHA-256 `cee46d9d852f7583072af6f2b22d83b5d0603f9462e31e96101c0a27e8258c83`. `git diff --check` is clean. No native source changed in this batch; both successful profiled emulator replays exercised the existing rebuilt binary. `/tmp/x4-recover-v59` completed a reversible keepalive with serial/events hashes `d691d557…/22fa7a49…`.
- `/tmp/x4-recover-v60` completed a final reversible Down/Up keepalive after the gates; serial/events SHA-256 values are `0b75c492…/aef03e00…`. `books-for-testing/` and `artifacts/` remain locally ignored, and `freeink-sdk` remains clean at `bda4293` with no submodule-pointer change. Re-read `GUIDANCE.md`; the monitor still says “Nothing further; carry on” and adds no correction or blocker. X3 and population/temperature profiles remain explicitly deferred.
- Audited control-input latency before fitting another profile field. Physical `x4-library-navigation-v3` already measures Confirm release to display start at `52 ms` p50; a fresh current-profile replay reaches the same call in `28 ms`. The remaining `24 ms` is within the existing main-loop/input sampling residuals, and the emulator already runs the production two-sample 5 ms debounce, mapping, hold, and combination path. The accepted fast/half comparisons likewise include input through optical motion. A separate input-delay constant would double-count sampling/render work and was not added.
- That replay exposed a different profile-consumption bug: the emulator charged every measured non-BUSY primary remainder before controller activation. Accepted physical FAST pages split their `747 ms` display call into about `81 ms` before the `506 ms` BUSY interval and `160 ms` after it; HALF splits `2,043 ms` into about `160 + 1,723 + 160 ms`. Boot FULL similarly reaches BUSY after about `160 ms`, not after the whole `316,843 us` non-BUSY remainder. The post-BUSY work is the production X4 single-buffer driver's BW+RED baseline re-seed.
- Split the existing measured remainder using the real driver traffic, without a new profile value or schema: FAST charges one of three plane-transfer shares before BUSY (`80,333 us`) and two afterward (`160,667 us`); HALF charges two of four before/after (`160,000/160,000 us`); FULL splits its `316,843 us` remainder as `158,421/158,422 us`. Blocking whole-operation time and optical timestamps stay unchanged. An async wait discards the post-BUSY share because `displayAsync()` deliberately omits that baseline re-seed.
- Five fresh current-profile library opens now place Confirm release to BUSY start/end at exactly `108.333/614.333 ms`, versus physical inferred `134/640 ms`; both boundaries are within `25.667 ms`. The old placement was `269/775 ms`, about `135 ms` late. Application render return remains `775 ms`, preserving the measured whole operation. Two replays have identical MP4 `0cf7ca1b…`, settled PNG `14b74d25…`, normalized events `69e4e5e4…`, and normalized stderr `4d4b3d85…`; the MP4 and PNG are also byte-identical to the pre-split current-profile replay.
- Fresh page controls preserve whole workloads while correcting BUSY placement: the half-page replay remains `2,621 ms` versus `2,622 ms` physical p50, and the fast-page replay is `1,268 ms` versus `1,268.5 ms` physical p50. Two sleep/reset replays retain the established byte-identical MP4s `ec69cee0…/6db95f69…` and retained panel `df4847df…`; current normalized events/stderr are deterministic at `456de575…/60f1856b…`. Boot-to-Home and deep-sleep application times remain unchanged, while the full controller boundary now occurs before the post-BUSY plane re-seed as on hardware.
- Closing transfer-split gates pass: all 12 native tests pass, the runnable emulator rebuilds, clang-format 21.1.1 is clean, strict Pyright reports zero diagnostics, Python source/tests/examples compile, both Python scenarios pass, and `git diff --check` is clean. The regenerated 31-source profile remains byte-identical at SHA-256 `cee46d9d852f7583072af6f2b22d83b5d0603f9462e31e96101c0a27e8258c83`; this is a more faithful consumption of existing evidence, not a retune. Production firmware sources did not change.
- The first `/tmp/x4-recover-v61` host wrapper did not return within 40 seconds and produced no artifact, so it was terminated rather than left holding the serial port. A bounded retry `/tmp/x4-recover-v62` immediately completed the reversible Down/Up keepalive, restored normal CPU frequency, and confirmed calibration firmware `dirtyca7b2857`; serial/events SHA-256 values are `3ce0e6e1…/53812087…`. `books-for-testing/` and `artifacts/` remain locally ignored, and `freeink-sdk` remains clean at `bda4293` with no pointer change.
- Audited the residual Boot/Home timing against all three accepted reset captures and the five return-to-Home cycles in `x4-library-navigation-v3`. Boot entry is `440 ms` in every reset capture, but the emulator moves from `307 ms` with an empty fixture to `333 ms` with persisted state plus one recent book. The physical device loads settings and nine recents before Boot. Rejected a universal `133 ms` reset/startup constant: the residual is fixture-dependent persisted-file and parsing work, and fitting it from one SD state would double-charge generic storage.
- The library trace does support stable paint boundaries. Its six first Home paints are `71/72/72/72/72/72 ms` and all six automatic follow-up paints are `12 ms`. The same run's five File Browser entries are `28 ms` entry-to-display with an independently logged `18 ms` paint, leaving `10 ms` for activity setup. Added these distributions to the existing attributable supplemental workload and profile models; no new physical source or schema version was needed.
- Native activity rendering now measures real host work from render-task dispatch to `GfxRenderer::displayBuffer()` and pads only the unconsumed remainder. Home selects `72 ms` for the first paint and `12 ms` thereafter. File Browser splits the existing `28 ms` whole boundary into `10 ms` pre-render and `18 ms` paint targets, preserving the exact whole duration. Older schema-version-1 profiles omit the optional paint fields and retain their previous behavior; `/tmp/x4-old-profile-compat-v1` confirms the prior cadence profile still launches and settles Home.
- Two fresh five-cycle library replays reproduce Home at exactly `72/12 ms` and File Browser at exactly `18 ms` paint / `28 ms` entry-to-display on every cycle. Their MP4 and settled PNG are byte-identical at SHA-256 `766762c7…/14b74d25…`; normalized events and stderr are deterministic at `bf7b2dfd…/cc3e84e9…`. The PNG is unchanged from the earlier transfer-split replay, so this is a timing-only correction and the existing upright physical/emulator raster review remains valid.
- The regenerated 31-source X4 profile is byte-identical to an independent output at SHA-256 `11235fea1141daeb8523589371426218f74e5740d013ba73012971ac87642680`. Two sleep/reset controls are deterministic with MP4 hashes `2ec92cf9…/e637ace1…`, retained panel `df4847df…`, normalized events `cbbe411e…`, and normalized stderr `d39c96ff…`; the intended Home paint pacing changes video timestamps while the retained pixels stay canonical.
- Closing activity-render gates pass: all 12 native tests, strict Pyright over source/tests/examples, Python compilation, both Python scenarios, clang-format 21.1.1, and `git diff --check` are clean. The normal firmware remains at `5,268,367` bytes flash / `49,532` bytes RAM, and the runnable emulator was rebuilt after the test target. `/tmp/x4-recover-v63` through `v65` completed reversible Down/Up keepalives on calibration firmware `dirtyca7b2857`; v64 serial/events hashes are `3b29240d…/81d08d28…` and v65 hashes are `4f042718…/5ce839a6…`. `books-for-testing/` and `artifacts/` remain locally ignored; `freeink-sdk` remains clean at `bda4293` with no pointer change; `GUIDANCE.md` still says “Nothing further; carry on.” X3 and population/temperature profiles remain explicitly deferred.
- `/tmp/x4-recover-v66` completed the immediate Down/Up keepalive after reattachment and confirmed X4 calibration firmware `dirtyca7b2857`. Added a one-entry recent-book removal/restore probe before risking the full mutation: `x4-home-recent-restore-probe-v1` removed `/stormlight.epub`, returned Home with eight entries, reopened the EPUB through the production reader path, and returned Home with Stormlight restored to the front. Its 28.107 s naturally upright capture is complete at an observed 25.0 FPS; MP4/serial/events/manifest hashes are `ebbfd9b6…/43652b3a…/d4f3c8b8…/4873070a…`. Eight-entry Home paints were `68/10 ms`, versus the existing nine-entry `72/12 ms` evidence.
- Accepted the complete reversible `x4-home-recents-variants-normal-v1` capture. It removed all nine original entries (`stormlight`, `lotr`, `mistborn`, `sun-eater`, `wot`, and four PNG calibration EPUBs), measured empty Home, and reopened them in reverse removal order so the final recent list and Stormlight top card were restored exactly. The naturally upright 480x640 recording is 118.5 s with 2,955 decoded camera frames at 25.0 FPS; MP4/serial/events/manifest hashes are `49301264…/45562307…/147a4d94…/543ccdde…`. The final frame visibly confirms the restored Stormlight Home card and natural review rotation.
- The capture rejects a count-linear Home timing model. Empty Home paints are `10/9 ms`; non-empty first paints for one through nine restored entries are `73/73/73/75/81/72/73/68/73 ms`, while follow-up paints are `12/12/12/12/14/11/11/11/12 ms`. The exact eight-entry `/lotr.epub` top-card result repeats the independent probe's `68 ms` first paint. Since X4 Home loads only the displayed recent subset, top-book/cover work dominates rather than total list length. Retained the current `72/12 ms` central compromise and did not add a content/population discriminator or change the 31-source profile; population-specific profiles remain explicitly deferred.
- Audited the adjacent Recent Books and Confirmation surfaces instead of assuming Home covered them. The physical full-list entry paints in `26-27 ms`; removal repaints then fall with the visible list from `25` to `6 ms`, while all nine confirmation paints are `4-5 ms`. A current-profile one-book emulator replay exposes the same production flow and canonical pixels but has `0 ms` native paint for both activities. The list result is explicitly population-dependent and therefore belongs to the deferred population/content profile; the `4-5 ms` confirmation residual is below one 40 ms camera source frame and does not justify another per-activity field.
- Added `replay_recent_removal.py` and ran it twice against the one-Stormlight fixture. Both runs have byte-identical MP4 SHA-256 `efd9954e…` and identical Recent Books, confirmation, empty-list, and empty-Home PNGs (`3f08d046…/672468eb…/9107db17…/6401ebf5…`). After output-path normalization, events and stderr are byte-identical at `d1425f18…/b643a3b7…`. This preserves a deterministic visual/control regression for the unprofiled boundary without pretending it is calibrated.
- Re-ran the Settings navigation replay twice against the current profile to ensure the new centralized activity-render scope had not disturbed its established queue behavior. Both MP4s are byte-identical at `309361e2…`; all four presentation PNGs match, and normalized events/stderr are `4e15d3bc…/2942517c…`. Current Settings paint is still raw `0 ms` versus physical `26-35 ms`, but the accepted physical-cadence trace and emulator both queue popup updates behind the 747 ms blocking panel operation; their earlier measured display-boundary residual remains `24-42 ms`. Retained the prior decision not to add a Settings-specific delay that would double-charge queued panel time.
- `/tmp/x4-recover-v67` completed another reversible Down/Up keepalive on calibration firmware `dirtyca7b2857`; serial/events hashes are `c53713ac…/cdcea719…`. The device remained responsive after the 118.5-second recents capture and analysis.
- Preserved the exact empty-state physical/emulator visual review at `artifacts/comparison/x4-recent-removal-runtime/review-v1/empty-home-comparison.mp4`. It aligns physical and simulated Home entry at `49.423/16.135 s`, perspective-corrects the camera corners `44,0,380,0,70,516,378,514`, and records 75 frames / 2.5 s at 30 FPS. Layout, empty-state copy, menu geometry, selection, icons, and soft keys match; hardware remains lighter and softer while the emulator is crisp and regularly halftoned. MP4/sidecar hashes are `fc210135…/8bbea9bb…`. The comparison also honestly exposes the accepted fixed-profile tradeoff: physical empty Home paints at `10/9 ms`, while the emulator retains the populated-screen `72/12 ms` compromise until population/content profiles are enabled.
- Final evidence-only gates pass. Strict Pyright over source/tests/examples reports zero diagnostics, Python compilation is clean, and `git diff --check` passes. The 31-source profile regenerates byte-for-byte at SHA-256 `11235fea1141daeb8523589371426218f74e5740d013ba73012971ac87642680`; the recent-book captures remain evidence rather than silently changing the applied model. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293`, and `GUIDANCE.md` still adds no correction or blocker.
- `/tmp/x4-recover-v68` completed the closing reversible Down/Up keepalive on calibration firmware `dirtyca7b2857`; serial/events hashes are `678f455e…/cc701a02…`. The attached X4 remains awake and responsive. The next profile work is the explicitly deferred X3 and population/temperature capture, not another unsupported X4 per-screen constant.
- Continued with a requirement-level profile audit instead of treating the defer boundary as project completion. It found one contradictory generated limitation: the X4 profile claimed captures predated dirty-source fingerprints, while all 31 accepted source entries already carry exact `-dirty<hash>` firmware labels across eight calibration-only revisions. Collapsed that stale claim and its redundant neighbor into the durable statement that sources span calibration-only command revisions and each entry records its exact firmware label. No timing, model, source, or schema data changed.
- Regenerated the applied profile twice. The old `11235fea…` and new `937c9126f44fab09eae7c51e64918b64edfbb4af33c1f5e4c724757254d9cd92` files differ only in that two-line provenance correction; both fresh outputs are byte-identical. Strict Pyright over source/tests/examples, Python compilation, both X3/X4 Python scenarios, all 12 native tests, the rebuilt runnable emulator, and `git diff --check` pass. The normal firmware remains isolated and builds at `5,268,367` bytes flash / `49,532` bytes RAM.
- `/tmp/x4-recover-v69` completed the batch-opening reversible Down/Up keepalive on calibration firmware `dirtyca7b2857`; serial/events hashes are `7104c4f9…/233fd880…`. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293` with no pointer change, and `GUIDANCE.md` still says to carry on.
- Chose the next non-deferred control-input gap: the earlier File Browser audit had only one Home selector sample, so it could not prove that the production GPIO/debounce path reproduced nominal button response. Added one ordinary physical scenario alternating settled Down/Up presses on Home. `x4-home-controls-normal-v1` established eleven exact `20 ms` press-acknowledgment-to-render-start samples, but one per-action marker was delayed until low-power entry and produced `116 ms`; retained it as failed nominal evidence. A tighter v2 avoided sleep but intentionally exposed overlapping panel calls, so its queue-limited samples are also not nominal evidence.
- Accepted `x4-home-controls-normal-v3`: the scenario waits only for the two initial Home refreshes, then keeps a one-second physical-input cadence so every prior 775 ms FAST operation returns before the next press and the three-second inactivity boundary is never crossed. Eleven of twelve press-acknowledgment-to-render-start samples are exactly `20 ms`; one first-input scheduler outlier is `79 ms`, giving `20/20/0 ms` p50/p90/MAD. Paint is `12 ms` p50/p90 with `0` MAD. The complete naturally upright 480x640 capture has 453 decoded frames at 25.0 FPS; MP4/serial/events/manifest hashes are `124ae3d7…/10129208…/95e410a5…/ab9caa59…`. An inspected frame confirms the restored Stormlight Home card and natural review rotation.
- Added `replay_home_controls.py` and ran it twice against the one-Stormlight fixture. All thirteen comparable simulated presses reach render start exactly `20 ms` after the injected physical control, while every paint remains exactly `12 ms`; the production ADC/GPIO, two-sample debounce, mapping, and activity path therefore already reproduce the physical nominal without a new input-delay constant. Both runs have byte-identical MP4 `7caa8fa4…` and final PNG `a579e326…`; normalized events/stderr are byte-identical at `7313fb15…/aabb0f42…`.
- Added marker-bounded control metrics to evidence-workload analysis and included the accepted v3 capture as the profile's 32nd attributable source. It retains every control, `pressAckToRenderStartMs`, render duration, p50/p90/MAD, camera cadence, scenario hash, and exact firmware label, but contributes no runtime model. A direct old/new model-object diff is empty. The applied profile regenerates byte-for-byte at SHA-256 `0c95461bd8fa33f4eeda6fef15687e1765c473c34f5df5551cd450c42a8498a0`.
- Closing control-evidence gates pass: strict Pyright over source/tests/examples reports zero diagnostics, Python compilation is clean, both X3/X4 scenarios pass, deterministic profile regeneration is byte-identical, and `git diff --check` is clean. Native and normal-firmware sources did not change after the immediately preceding 12-test native pass, runnable rebuild, and successful normal build (`5,268,367` flash / `49,532` RAM). `/tmp/x4-recover-v70` completed another reversible Down/Up keepalive; serial/events hashes are `9c420021…/f1cc75d3…`. Local ignores and the clean `freeink-sdk` state remain intact; `GUIDANCE.md` adds no blocker.
- Closed the remaining hold semantic from the existing reversible recents capture. Its nine long Confirm presses enter Confirmation `1,020/1,020/1,117/1,025/1,025/1,025/1,025/1,020/1,020 ms` after button-down: `1,025` ms p50, `1,043.4` ms p90, and `5` ms MAD. The one-book emulator replay enters at about `1,020` ms through the same production `1,000` ms hold threshold and correctly swallows the release instead of opening the selected book. No hold-delay field was added.
- Added marker-bounded hold parsing and included `x4-home-recents-variants-normal-v1` as the 33rd evidence-only source. Only its nine hold-to-Confirmation samples, scenario/camera metadata, and exact firmware label enter the profile; the run's empty/populated paint costs remain outside every model as explicitly deferred evidence. A direct old/new model-object diff remains empty. The applied 33-source profile regenerates byte-for-byte at SHA-256 `6ea6d6bb421f1c31db938ade1ef24ee2bd8fa178ffcadeb6625fb0bc9adf8bbb`.
- Preserved a 2.7 s / 81-frame aligned control review at `artifacts/comparison/x4-home-controls-runtime/review-v1/home-controls-comparison.mp4`. It aligns physical and simulated Down at `3.172/6.878 s`, perspective-corrects corners `44,0,380,0,70,516,378,514`, and shows matching Stormlight card, menu geometry, selector movement, icons, and soft keys. Hardware remains lighter/softer while the emulator is crisp and regularly halftoned. MP4/sidecar hashes are `bdd72a7b…/313f6ed5…`.
- Final press/hold gates pass: strict Pyright over source/tests/examples, Python compilation, both X3/X4 scenarios, deterministic 33-source regeneration, and `git diff --check` are clean; runtime models remain unchanged. `/tmp/x4-recover-v71` completed another reversible Down/Up keepalive on `dirtyca7b2857`; serial/events hashes are `06eade9d…/b059bb8c…`. The X4 remains responsive, local ignores and `freeink-sdk` remain clean, and `GUIDANCE.md` still adds no correction.
- Audited the applied File Browser paint target with a dedicated current-firmware scenario rather than assuming the older library-navigation revision remained representative. `x4-file-browser-controls-normal-v1` and its independent v2 repeat each enter File Browser in exactly `38 ms`, leaving `12 ms` before paint, and all 26 entry/Down/Up paints are exactly `26 ms`. Both runs stayed at normal CPU frequency. Their MP4/serial/events/manifest SHA-256 values are `310f02fa…/768b82f3…/49294f11…/44803021…` and `9e2a5af0…/7ae5fd61…/2ff93e08…/2b6e7285…`; both use the same scenario hash `7ddf403a…` on calibration firmware `dirtyca7b2857` and have naturally upright 480x640 captures at 25.0 observed FPS.
- Corrected the model provenance instead of pooling incompatible revisions. Dedicated `fileBrowserControl*` workload keys now supersede the older `x4-library-navigation-v3` File Browser samples when present, while that older source still supplies the independent `72/12 ms` Home paint distributions. The resulting 35-source profile changes only `fileBrowserActivityToDisplayMs` from `28` to `38 ms` and `fileBrowserRenderMs` from `18` to `26 ms`; its setup remainder is therefore `12 ms`, and a direct old/new model diff confirms every unrelated model is unchanged. Profile SHA-256 is `8c2cc61397971698c9a30ed33ad034c7d136b7d90de6867e0baa694cf9f6d763`.
- Added `replay_file_browser_controls.py` and replayed the same alternating selector path twice with a populated multi-book fixture. Both deterministic runs reproduce `38 ms` activity entry and all thirteen paints at exactly `26 ms`; MP4 and final PNG are byte-identical at `e46ddc37…/00e8e73b…`, while path-normalized events/stderr are byte-identical at `606e7507…/0dabd85f…`. The upright four-second physical/emulator review is `artifacts/comparison/x4-file-browser-controls-runtime/review-v1/file-browser-controls-comparison.mp4` (SHA-256 `24933e48…`): list, selection, scrollbar, borders, and soft-key geometry agree, while the physical SD has different filenames and remains visibly lighter/softer than the crisp halftoned emulator.
- The first post-control `/tmp/x4-recover-v72` wrapper hung without producing an artifact and was terminated safely. Bounded retries v73 and v74 completed reversible Down/Up keepalives; v73 serial/events hashes are `d23bbaf9…/c782456d…`, v74 hashes are `d126ac29…/7332aac0…`, and the attached X4 remains responsive on calibration firmware `dirtyca7b2857`.
- Closing File Browser correction gates pass: strict Pyright reports zero diagnostics over source/tests/examples, Python compilation is clean, both existing Python scenarios pass, all 12 native tests pass, and the runnable emulator rebuilds after the test target. Independent 35-source regeneration is byte-identical at SHA-256 `8c2cc613…`; `git diff --check` is clean. The review sidecar is SHA-256 `153cc1f6…`. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293` with no pointer change, and `GUIDANCE.md` still says to carry on. No production C++ changed in this correction, so the immediately preceding normal-firmware `5,268,367` flash / `49,532` RAM build remains the applicable gate. X3 and population/temperature profiles remain explicitly deferred.
- Reviewed the causal claim after comparing old and current physical frames. `x4-library-navigation-v3` displayed 13 root entries; the dedicated current capture displays 15 rows and a scrollbar, after the PNG calibration EPUBs were added. The `18` to `26 ms` paint shift therefore cannot honestly be assigned only to calibration-firmware revision. Corrected the docs to describe a current-SD/current-firmware nominal and kept the dedicated keys because pooling the two states would still be invalid; no count/content curve was fitted because that remains part of the explicitly deferred population profile.
- Completed the applied-model provenance sweep. File Browser was the only runtime screen boundary contradicted by a dedicated current-state repeat. Current recents/control captures independently validate Home's `72/12 ms` compromise and input/hold semantics; the corrected normal-frequency SD, directory, panel, optical, grayscale, warm, cache, indexing, and PNG capture families retain their audited sources, and all share the same `f42919ca` production commit despite calibration-only dirty revisions. No further non-deferred X4 constant was changed.
- `/tmp/x4-recover-v75` completed the closing reversible Down/Up keepalive on calibration firmware `dirtyca7b2857`; serial/events SHA-256 values are `33166707…/5cfb8602…`. The device is still awake and responsive, and `git diff --check` remains clean after the causal-doc correction.
- Reopened the previously queue-contaminated Settings timing question with the settled, cancel-only `calibrate_settings_navigation.py` scenario. Accepted current-firmware runs `x4-settings-controls-normal-v1` and v2 both enter Settings with a `27 ms` full paint. Their twelve later full Settings paints are `35/36/35/36/36/35` and `36/36/35/36/36/35 ms`, giving pooled `36/36/0 ms` p50/p90/MAD. Both runs cancel the Reader Font Size popup, return Home without changing the setting, restore normal CPU frequency on physical input, and have naturally upright 480x640 video at 25.0 observed FPS. MP4/serial/events/manifest hashes are `28b9c669…/2f835fc6…/68e2a888…/ce2b6d04…` and `ed8ae7b1…/d737cfb5…/dbeec1df…/5ef9c27e…`; their scenario hash is `95d02ce4…`.
- The first immediate artifact inspection raced FFmpeg finalization and briefly saw no manifest/moov; a bounded poll found both complete manifests and valid finalized MP4s, so neither run is failed evidence. The apparent popup paint values (`2,121-7,763 ms`) are deliberately not timings: `OptionPopup` reuses the framebuffer without `clearScreen`, so the existing diagnostic accumulates from the preceding full render. Parser grouping retains only the attributable sub-100 ms clear-and-redraw Settings samples.
- Added optional `settingsFirstRenderMs` and `settingsSubsequentRenderMs` profile models and one generic render fact: the emulated `clearScreen()` marks that the current activity render is a full redraw. Settings consumes `27/36 ms` only when that mark is present; popup overlays retain native cost and the already calibrated panel queue. Older schema-version-1 profiles omit both fields and `/tmp/x4-old-profile-settings-compat-v2` still launches, reaches settled Home, and reports its calibrated profile normally.
- Settled emulator runs `run-v9-settings-profile` and `run-v10-settings-profile-repeat` reproduce `27 ms` on entry and `36 ms` on every later full redraw, while the three popup logs remain cumulative and unpadded. Both runs have byte-identical MP4 `bbabc62e…`, identical four presentation PNGs, and path-normalized events/stderr `a50704c2…/086b4680…`. The separate overlapping `run-v11-settings-profile-physical-cadence` retains the established render order; its MP4/events/stderr hashes are `2af8becb…/8d556820…/3f4ad659…`.
- Preserved a current naturally upright Settings-entry review at `artifacts/comparison/x4-settings-navigation-runtime/review-v4-current-profile/settings-entry-comparison.mp4`. It uses the decoded-first-frame timestamp to align physical/emulator activity entry at `4.782/12.422 s` for 45 frames / 1.5 s. Layout, tabs, values, selection, and soft keys agree; the hardware remains lighter/softer while the emulator retains stronger prior-Home ghosting and crisp halftone. MP4/sidecar hashes are `bb38ff3c…/a24fbca3…`.
- Closing Settings gates pass: strict Pyright reports zero diagnostics over source/tests/examples, Python compilation is clean, both existing scenarios pass, all 12 native tests pass, clang-format 21.1.1 is clean, and the runnable native emulator rebuilds after the test target. The normal firmware build succeeds at the known cached `5,268,367` bytes flash / `49,532` bytes RAM, confirming the generic clear marker compiles out. Independent 37-source regeneration is byte-identical at SHA-256 `a99cb93318cd8860da17cb09b74b6cc6b7abd8cd94adb20961d509162cb03f06`; a direct model diff adds only the two Settings distributions, and `git diff --check` is clean. `/tmp/x4-recover-v76` completed a reversible keepalive with serial/events hashes `180e887e…/6d7e6a48…`; local ignores, clean `freeink-sdk` state, and unchanged monitor guidance are intact.
- Added a reversible Reader navigation scenario and accepted two current-firmware captures, `x4-reader-navigation-controls-normal-v1` and v2. Each opens cached Stormlight, alternates three Down/Up pairs in the Reader menu and chapter list, cancels the chapter list, and returns Home without changing settings, progress, cache, or SD content. Reader menu entry paints are `20/21 ms` and all twelve later menu paints are `19 ms`; chapter-list entry paints are `75/75 ms` and later paints are `74-75 ms`. Both captures are naturally upright 480x640 at 25.0 observed FPS and share scenario SHA-256 `23214e90…`; MP4/serial/events/manifest hashes are `03122dc0…/c030a3c2…/045043eb…/95ad3e4f…` and `32e2f225…/9846ae86…/b83236e9…/c976ac6d…`.
- Inspected settled frames from both runs: Reader menu and Select Chapter screens are fully visible, naturally oriented, and repeat visually. Only the current Stormlight menu boundary becomes a runtime model. The `74-75 ms` Stormlight chapter-list result remains attributable evidence because visible row titles and TOC population are book-dependent; fitting that would violate the explicit defer on population profiles. `/tmp/x4-recover-v78` completed another reversible Down/Up keepalive on `dirtyca7b2857` while the captures were analyzed.
- Added optional `readerMenuFirstRenderMs` and `readerMenuSubsequentRenderMs` models from the pooled repeats: `20.5/20.9/0.5 ms` p50/p90/MAD for two first paints and exact `19/19/0 ms` for twelve later paints. The 39-source profile retains the `75/74.5 ms` Stormlight chapter-list distributions under their source workloads but exposes no chapter-list runtime model. After the final optional-row qualification its SHA-256 is `bdb69bac393dce0dc2120227349279d7e5fed6c75cdcfa1937d4daeac0cfb8d1`; independent regeneration is byte-identical, and a direct old/new model diff adds only the two Reader menu records.
- Exposed nested emulator activity IDs `reader.epub.menu` and `reader.epub.chapters` and added `replay_reader_navigation.py`. One initial diagnostic incorrectly assumed Confirm from the fixture's default Home selector would open File Browser and timed out; the accepted replay explicitly moves to Files first. Runs `run-v2-profiled` and `run-v3-profiled-repeat` are byte-identical: MP4 `bf837b54…`, four menu/chapter screenshots, path-normalized events `63f748d6…`, and stderr `c43c4b6c…`. Reader menu activity work consumes exactly `20.5 ms` first and `19 ms` thereafter; the integer-millisecond GFX log displays `20/19 ms`. Chapter-list paints remain at their native `2 ms`, proving the evidence-only boundary is not applied. A pre-Reader profile still launches and settles Home in `/tmp/x4-old-profile-reader-menu-compat-v1`.
- Preserved two naturally upright, perspective-corrected 0.9 s entry reviews under `artifacts/comparison/x4-reader-navigation-runtime/review-v2-entry`. Reader menu and chapter-list text, rows, selection, scrollbar, borders, and soft keys match; the physical panel remains lighter/softer while the emulator is crisp with stronger prior-screen ghosting. Menu/chapter MP4 hashes are `0ea45c63…/4f7d1f03…`; the final sidecar hash is `14d5c8fc…`. Each clip ends before the first navigation input so both sides compare the same selected row.
- Closing Reader-navigation gates pass: strict Pyright over source/tests/examples reports zero diagnostics, Python compilation is clean, both existing Python scenarios pass, all 12 native tests pass, clang-format 21.1.1 is clean, and the runnable emulator was rebuilt after the test target. The normal firmware builds at `5,268,367` bytes flash / `49,532` bytes RAM, confirming the new activity mappings compile out. `git diff --check` is clean. `/tmp/x4-recover-v79` and v80 completed reversible keepalives; v80 serial/events hashes are `08518ad3…/85b3c8be…`. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293`, and `GUIDANCE.md` still says to carry on. X3 and population/temperature profiles remain explicitly deferred.
- Final scope audit qualified the Reader-menu model: the captured ten-row Stormlight state has neither optional Footnotes nor saved-bookmark rows. The `20.5/19 ms` values remain the current nominal, while optional-row variants join chapter lists in deferred population/content evidence. Runtime padding consumes only the unspent remainder, so a heavier native variant is never shortened to this target.
- `/tmp/x4-recover-v81` completed the closing reversible Down/Up keepalive on `dirtyca7b2857`; serial/events hashes are `162ca3ee…/580fd6b6…`. The attached X4 remains responsive at handoff.
- Audited another nested Reader surface without crossing the deferred content/population boundary. `Go to %` has a fixed layout and a cancel path, so the physical scenario opens cached Stormlight, selects that row, exercises `0/1/10%` through paired fine/coarse controls, restores `0%`, cancels, and returns Home. The first complete v1 run preceded the controller-idle wait correction and is excluded from pooling. V2 is failed evidence: its final Down command was acknowledged over USB but never sampled by the production input loop, so the 60-second paint wait expired. Recovery v83 canceled the selector and returned Home without committing progress.
- Accepted corrected current-firmware runs `x4-reader-percent-controls-normal-v3` and v4, both with scenario SHA-256 `a14452c0…` on `dirtyca7b2857`. Both first paints are exactly `7 ms`; their 24 pooled later paints are `6` ms p50, `7` ms p90, and `0` ms MAD. V3 MP4/serial/events/manifest hashes are `8bb8b86d…/6f312661…/98b6f5af…/cb267b2e…`; v4 hashes are `4089090b…/484ed31a…/501a2118…/8baf8ab8…`. Both captures are complete, naturally upright 480x640 video at 25.0 observed FPS, restore normal CPU frequency as needed, cancel at the initial value, and leave the device at Home.
- Added marker-bounded percent-selector parsing and optional `readerPercentFirstRenderMs` / `readerPercentSubsequentRenderMs` models. The 41-source profile now exposes `7/7/0 ms` p50/p90/MAD for entry and `6/7/0 ms` for later redraws, with SHA-256 `39d5984b6f2ef608e574f7c70fff12ab1f5bb2122c42661ceaf12f40ff1f6f94`. Runtime maps `EpubReaderPercentSelection` to stable `reader.epub.percent` automation and consumes only its clear-and-redraw remainder. Profiles omitting both optional fields still launch and settle Home in `/tmp/x4-old-profile-reader-percent-compat-v1`.
- Added `replay_reader_percent_navigation.py` and ran it twice against the one-Stormlight fixture. Both runs reproduce exactly `7 ms` entry and `6 ms` later paints; MP4 is byte-identical at `7a3dc2d4…`, `0/1/10%` presentation PNGs are byte-identical at `5572e43b…/0a9bad25…/e4a3f9e8…`, and path-normalized events/stderr are byte-identical at `d7815090…/e808e1f1…`. The aligned 21-frame / 0.7 s entry review is `artifacts/comparison/x4-reader-percent-runtime/review-v1-entry/reader-percent-entry-comparison.mp4` (SHA-256 `f57c20b8…`): header, value, slider, hints, and soft keys agree; hardware remains lighter/softer while the emulator is crisp and retains stronger Reader-menu ghosting.
- Closing Reader-percent gates pass: strict Pyright reports zero diagnostics, Python source/tests/examples compile, both X3/X4 golden/video scenarios pass, all 12 native tests pass, clang-format 21.1.1 is clean, and the runnable emulator rebuilds after the test target. Independent 41-source regeneration is byte-identical at SHA-256 `39d5984b…`; the normal firmware builds at the known cached `5,268,367` bytes flash / `49,532` bytes RAM, and `git diff --check` is clean. The comparison sidecar hash is `4ac6c2ee…`. Keepalives v84-v87 completed on `dirtyca7b2857`; v87 serial/events hashes are `e32b0f32…/3189aacb…`. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293` with no pointer change, and `GUIDANCE.md` still adds no correction. X3, translated/content variants, and population/temperature profiles remain deferred.
- Audited the remaining fixed Reader overlays without crossing the deferred content/population boundary. `OptionPopup` does not call `clearScreen`, so its legacy GFX diagnostic accumulates from the preceding menu and cannot isolate paint cost. Added a calibration-only activity-render timer before `currentActivity->render()`; the first `displayBuffer()` logs the elapsed CPU paint before panel I/O and clears the scope. The hook compiles out of normal firmware and deliberately leaves multi-display page phase logging to the existing instrumentation. Calibration firmware built at `5,277,957` bytes flash / `49,636` bytes RAM as `1.4.1-dev-emulator-f42919ca-calibration-dirtye0ed86e0`. The first upload lacked dialout access and failed before mutation; the retry under `sg dialout` completed, hard-reset, and `/tmp/x4-recover-v88` verified the new firmware and render marker.
- The initial `x4-reader-popups-normal-v1` capture is failed evidence, not a timing source. The physical popup handles Back on press, then the same release exits the Reader menu; the script's next Down therefore turned a Reader page while its repaint was still running. Its MP4/serial/events/manifest hashes are `f0bfaf0d…/1a3899a3…/ea4a491e…/3ee068a7…`. Recovery v89 returned Home, and the corrected scenario waits for the full Reader repaint before reopening the menu. It returns every selector to its original value and changes neither orientation nor auto-turn.
- Accepted `x4-reader-popups-normal-v2` and v3 on the same scenario SHA-256 `1f727bd1…` and calibration firmware. Orientation entry plus six controls are `8/8/8/9/8/8/8` and `8/8/8/9/8/9/8 ms`; Auto Turn is exactly `7 ms` for all fourteen paints. Pooled over 28 samples this is `7.5` ms p50, `8.3` ms p90, and `0.5` ms MAD, within `0.5` ms of either popup's nominal without popup-type state. Both videos are naturally upright 480x640 with 25.0 observed FPS. V2 MP4/serial/events/manifest hashes are `4e80d1bb…/26b4520c…/fef2681e…/95ed5661…`; v3 hashes are `8767950c…/d386e0a0…/69694c1e…/5c18468c…`.
- Added marker-bounded Reader-popup analysis, rejecting incomplete inner marker sets explicitly, and the optional `readerMenuPopupRenderMs` model. The current English Orientation and Auto Turn overlays share the pooled `7.5` ms target; translated labels remain deferred. Runtime selects it only for a non-clearing `reader.epub.menu` render, while clear menu renders retain `20.5/19 ms` first/later targets and Back fallthrough retains the full menu repaint. The independently generated 43-source profile is SHA-256 `270c7d01e31cc84a66baa71225251db3fa8fb4fed2292722d27d55b15c1a1e4e`; removing the new model leaves the previous model object byte-for-byte equivalent. A copied schema-v1 profile with the field removed completed the entire popup replay under `/tmp/x4-popup-old-profile-v2`; its popup movements remain native/unpadded at `0 us`, proving backward compatibility.
- Added `replay_reader_popups.py` and ran it twice against the one-Stormlight fixture. Both runs consume exactly `7,500 us` from popup Confirm release to panel command and from each of the twelve popup-move presses to panel command; menu-row moves remain exactly `19,000 us`. The two MP4s are byte-identical at SHA-256 `6e689b1d…`, Orientation/Auto Turn presentation PNGs are identical at `c52792af…/e84b4bfd…`, and path-normalized events/stderr are identical at `e4925b61…/85c52511…`.
- Preserved two aligned 0.8 s / 24-frame, naturally upright popup-entry reviews under `artifacts/comparison/x4-reader-popups-runtime/review-v1-entry`. Both normalize the 25 FPS camera to 30 FPS, align at the CPU-paint-to-panel boundary, and perspective-correct corners `44,0,380,0,70,516,378,514`. Popup bounds, titles, option rows, selected state, underlying Reader menu, and soft keys agree through the transition. Hardware is gray, softer, and uneven; the emulator is crisp, higher-contrast, and retains stronger menu ghosting. Orientation/Auto Turn MP4 hashes are `df09e367…/ea36a6c1…`; sidecars are `43172f4e…/5b574d28…`. Keepalives v90-v91 completed on `dirtye0ed86e0` while replay and review artifacts were checked.
- Two closing check invocations were corrected without weakening a gate. Running Pyright from the repository root ignored the package's configured venv and produced only missing-import diagnostics; rerunning from `emulator/python` over `src`, `tests`, and `examples` reports zero diagnostics. The requested `clang-format-21` alias was absent and the installed 22.1.8 formatter was deliberately not accepted as the repository version; `uvx` supplied pinned 21.1.1, which found and mechanically fixed one stale ternary line wrap in `HalStorageNative.cpp` before the complete changed-C++ check passed.
- Closing Reader-popup gates pass: strict Pyright is clean, Python source/tests/examples compile, both Python X3/X4 scenarios pass, all 12 native tests pass, pinned clang-format 21.1.1 is clean, and the runnable emulator rebuilds after the test target. Independent 43-source regeneration is byte-identical at SHA-256 `270c7d01…`; the normal firmware builds at the unchanged `5,268,367` bytes flash / `49,532` bytes RAM, proving the calibration timer compiles out. `git diff --check` is clean. `books-for-testing/` and `artifacts/` remain locally ignored, `freeink-sdk` remains clean at `bda4293` with no pointer change, and `GUIDANCE.md` still adds no correction. `/tmp/x4-recover-v93` completed the closing reversible keepalive on `dirtye0ed86e0`; serial/events hashes are `0282944a…/9ef5f505…`. X3, translated/content variants, and population/temperature profiles remain explicitly deferred.
- Reused the calibration-only render boundary for the adjacent fixed Settings gap. `calibrate_settings_navigation.py` now exercises three Down/Up pairs in the Reader Font Size popup, returns to Medium before cancel, and leaves the setting unchanged. Accepted `x4-settings-popup-controls-normal-v1` and v2 share scenario SHA-256 `026d90d0…` on `dirtye0ed86e0`. V1 entry plus moves are `7/6/6/7/7/6/7 ms`; v2 is `7/6/7/7/7/7/7 ms`. The pooled 14 paints are `7` ms p50/p90, `0` ms MAD, range `6-7` ms. Their incidental first/later full Settings paints are `28/36 ms` evidence only, so the accepted `27/36` ms parent model does not drift. Both captures are naturally upright 480x640 at 25.0 observed FPS. V1 MP4/serial/events/manifest hashes are `e8603b4e…/e5d6d828…/f478837d…/29fa482f…`; v2 hashes are `a07f6f1b…/662ada62…/680ecce3…/f007e13b…`.
- Added marker-bounded `settingsPopupRenderMs` analysis and an optional `7` ms floor for non-clearing `settings` renders. Clear parent paints retain their existing first/later targets; other or translated option sets are uncalibrated but share the floor and retain heavier native work. No popup-type discriminator was added. The 45-source profile changes only the new Settings-popup model and is SHA-256 `78caf54b6ab146709dc05eb62d6a43e4c3381522f5e2577d9e75f2a72a23ed18`. A direct model diff after deleting that field is empty. The preceding 43-source schema-v1 profile completed the updated Settings replay under `/tmp/x4-old-profile-settings-popup-compat-v2`; all popup paints remain native/unpadded at `0 us`, proving backward compatibility. An initial one-line compatibility probe read the handshake as an object instead of its actual dictionary shape and exited before inspection; the public replay path replaced it.
- Updated `replay_settings_navigation.py` to accept an explicit timing profile and mirror the paired, value-preserving popup controls. Profiled runs v12/v13 consume exactly `7,000 us` from popup entry and all six move presses to panel command; full Settings redraws remain exactly `36,000 us`. MP4 is byte-identical at SHA-256 `0b875aa9…`; all six presentation PNGs match, and path-normalized events/stderr are byte-identical at `cd8e6471…/410ce9f5…`. The initial/Large/restored popup PNG hashes are `0e13ce31…/e7284158…/bcaebe66…`.
- Preserved a decoded-frame-aligned 0.9 s / 27-frame review at `artifacts/comparison/x4-settings-navigation-runtime/review-v5-settings-popup-profile/settings-popup-entry-comparison.mp4`. It aligns physical/emulator panel entry at `18.173/30.555 s`, perspective-corrects corners `44,0,380,0,70,516,378,514`, and matches popup bounds, title, four option rows, Medium selection, underlying Settings geometry, and soft keys through the transition. The physical SD uses Noto Sans while the emulator fixture uses Noto Serif, so the underlying value difference is fixture scope rather than a raster defect. Hardware remains lighter/softer; the emulator is crisp and more strongly ghosted. MP4/sidecar hashes are `0201744e…/bbd6f598…`. `/tmp/x4-recover-v94` completed another reversible keepalive during the deterministic replays.
- Closing Settings-popup gates pass: strict Pyright reports zero diagnostics, Python source/tests/examples compile, both Python X3/X4 scenarios pass, all 12 native tests pass, and pinned clang-format 21.1.1 is clean. Independent 45-source regeneration is byte-identical at SHA-256 `78caf54b…`; `git diff --check` is clean. Production firmware sources did not change after the immediately preceding successful normal build at `5,268,367` bytes flash / `49,532` bytes RAM. The post-test runnable rebuild was initially prevented twice before process creation by the outside-sandbox approval reviewer timing out; a sandbox attempt then confirmed only the expected read-only global `platforms.lock` failure. A writable `/tmp/crosspoint-platformio-core` facade with read-only links to the already installed packages/platforms moved lock/cache writes into `/tmp`, rebuilt successfully, and `--help`/binary strings confirm `.pio/build/emulator/program` is the CLI runner rather than Unity's test executable. The same approval outage prevented v95 from opening serial before process creation, and the sandbox correctly denied the audit interface, so no false keepalive was claimed and the device may sleep. Local book/artifact ignores remain intact, `freeink-sdk` remains clean at `bda4293` with no pointer change, and `GUIDANCE.md` adds no correction. X3, translated/content variants, and population/temperature profiles remain deferred.
- Serial access recovered: `/tmp/x4-recover-v96` completed a reversible Down/Up keepalive, restored normal CPU frequency, and confirmed X4 calibration firmware `1.4.1-dev-emulator-f42919ca-calibration-dirtye0ed86e0`. Prepared a separate value-preserving Settings popup-variant scenario covering English Sleep Screen (7 options), Refresh Frequency (5), Line Spacing (3), and Paragraph Alignment (5). Each popup performs three paired Down/Up movements and is cancelled; the scenario never confirms a value. Marker-bounded analysis retains per-variant distributions and a pooled `settingsPopupRenderSamples` input so the provisional shared 7 ms floor can be validated without adding popup-type runtime state. Capture is intentionally not claimed yet. Strict Pyright over source/tests/examples, Python compilation, both Python tests, and `git diff --check` pass before the requested checkpoint commit.
- Closed the popup-variant experiment without changing timing. Runs `x4-settings-popup-variants-normal-v1` and v2 completed, but the scenario's four Reader moves selected Screen Margin rather than the documented third-row Line Spacing; v2 exposed the mismatch with `26-27 ms` full Settings paints. A three-move correction still failed to open every intended popup in v3, which has no marker-bounded Paragraph Alignment render samples. All three runs are invalid scenario evidence and are excluded from profile generation. Their MP4/serial/events/manifest hashes are `e75f979d…/40edbbb9…/76d973a6…/3db703e2…`, `267cc150…/2177c7ec…/8fa58d34…/8807df87…`, and `8d7d8b8a…/8023e22e…/50ea6766…/9877d34a…`. The webcam was then removed, so physical capture stopped rather than attempting another variant.
- Removed the invalid popup-variant scenario and its unconsumed analyzer branch. The accepted 14-sample English Font Size evidence remains the sole Settings-popup source; the shared X4 floor stays `7 ms`, and the 45-source profile is frozen unchanged at SHA-256 `78caf54b6ab146709dc05eb62d6a43e4c3381522f5e2577d9e75f2a72a23ed18`. X3, translated/content variants, and population/temperature profiles remain deferred.
- The installability review found two repository-only assumptions: `Emulator.launch()` located the runner and X4 profile by a fixed four-parent walk from `client.py`, and the calibrated JSON lived outside the Python package. Moved the unchanged profile under package data, added explicit `CROSSPOINT_EMULATOR_EXECUTABLE` / `CROSSPOINT_EMULATOR_TIMING_PROFILE` overrides, and changed runner discovery to search a surrounding source checkout. Missing runner/profile paths now fail before process startup with actionable errors.
- Kept automation as ordinary self-contained Python rather than adding a scenario DSL. The new `crosspoint-emulator` command executes a script as `__main__`, preserves script-directory imports and arguments, and only supplies optional runner/profile overrides. Scripts continue to own `Emulator.launch()`, fixture/artifact paths, point-in-time `screenshot()` calls, and post-close `export_video()`. Added a small `review_home.py` example with optional MP4 output.
- Split the public guide from the physical calibration notebook and added the distribution/extension decision to `docs/emulator-plan.md`. The short README now documents checkout, persistent `uv tool`, and isolated Git-source `uvx` use; calibrated JSON ships in wheel/sdist, while the current 10 MB Linux native runner remains a separately built platform artifact rather than being mislabeled inside a `py3-none-any` wheel. CI now builds the distribution and smoke-runs the installed wheel without publishing it.
- Verified both source and installed-package paths. A source X4 run and isolated-wheel X4 run each produced the same upright `480x800` Home PNG (SHA-256 `a5147168…`) and byte-identical 5.7 s MP4 (`5d40c8f8…`); the installed manifest resolves timing from the wheel's `site-packages/crosspoint_emulator/profiles/` path. The documented checkout command also completed an X3 PNG run. Final wheel/sdist SHA-256 values are `4d50bc4d…/67822b16…`; wheel metadata carries MIT, both commands, and the bundled 189,112-byte profile.
- Closing distribution gates pass: strict Pyright over source/tests/examples has zero diagnostics, Python compilation and `uv lock --check` are clean, both Python scenarios pass, all 12 native tests pass after PlatformIO downloaded its missing Unity test dependency, and the runnable emulator rebuild succeeds after the test target. `git diff --check` is clean. The direct GitHub-source `uvx` command cannot be live-tested until this commit exists on the fork; wheel-based `uvx --isolated` verifies the same installation boundary locally. No release, package upload, or push was performed.
